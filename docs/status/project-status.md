# Project Status Board (Source of Truth)

**Status:** ACTIVE  
**Last Updated (UTC):** 2026-03-12
**Owner:** Engineering  
**Scope:** Live delivery status for features, quality gates, and release readiness.

---

## Rules of Use

1. This file is the only live status source of truth.
2. Any status change must include fresh evidence in this file (tests, workflow run, or commit/PR).
3. Update this board in the same PR/commit as the implementation change whenever possible.
4. If no status changed, add a short timestamped "No Change" entry in `Status Change Log`.
5. Keep detailed implementation narratives in `docs/plans/*`; keep this board concise and evidence-linked.

---

## Consolidated Source Inputs

This board consolidates progress from:

- `docs/plans/2026-02-26-full-rebuild.md` (target scope roadmap)
- `docs/plans/2026-03-05-current-state-phase-plan.md` (evidence-based implementation state)
- `docs/runbooks/preprod-readiness-checklist.md` (operational pre-prod gate)
- `docs/runbooks/release-promotion.md` (promotion sequence and commands)
- `docs/runbooks/rollback.md` (rollback procedure)
- `.github/workflows/ci.yml`, `.github/workflows/preprod-readiness.yml`, `.github/workflows/deploy-preprod.yml`, `.github/workflows/promote-prod.yml` (delivery gates)

---

## Workstream Matrix

| Workstream | Status | Target Scope | Current Reality | Evidence | Next Milestone |
|---|---|---|---|---|---|
| Foundation platform | DONE | Docker stack, health checks, CI baseline | In place and stable | `docker-compose.yml`, `.github/workflows/ci.yml`, `backend/app/api/health.py` | Maintain |
| Auth + core vocabulary | PARTIAL | Register/login/refresh/me/logout, protected routes, lookup fallback | Register/login/me/refresh/logout implemented with refresh rotation + access-token revocation; frontend protected routes + logout UX + 401 lifecycle handling are in place; dictionary lookup fallback still pending | `backend/app/api/auth.py`, `backend/app/services/auth_tokens.py`, `frontend/src/lib/api-client.ts`, `frontend/src/middleware.ts`, `e2e/tests/smoke/auth-contract.smoke.spec.ts`, `e2e/tests/smoke/auth-guard.smoke.spec.ts` | Implement dictionary API fallback for `/api/words/lookup` misses and add coverage |
| Word list + ePub import | DONE | Word-list domain + import jobs + progress channel | Import pipeline hardened for real runtime: worker no longer hard-fails when `en_core_web_sm` is missing (fallback NLP path), backend/worker now share upload storage path, and full E2E now asserts terminal `completed` status with a valid EPUB fixture | `backend/app/tasks/epub_processing.py`, `backend/app/core/uploads.py`, `backend/app/api/word_lists.py`, `backend/app/api/imports.py`, `backend/tests/test_epub_processing.py`, `e2e/tests/full/import-terminal.full.spec.ts`, `e2e/tests/helpers/import-jobs.ts`, `e2e/tests/fixtures/epub/valid-minimal.epub` | Add object-storage upload path + temp-resource lifecycle cleanup (success/failure/TTL) with automated coverage |
| Review + SM-2 queue | PARTIAL | Queue add/due/submit/stats + full integration | Queue API/service/frontend implemented; broader roadmap depth still pending | `backend/app/api/reviews.py`, `backend/app/services/review.py`, `frontend/src/app/review/page.tsx` | Close remaining roadmap gaps and harden flows |
| E2E + CI quality gates | DONE (baseline) | Required smoke gate on PR + broader suite | Smoke required on PR; auth contract + protected-route smoke coverage added; full suite runs on main/dispatch | `.github/workflows/ci.yml`, `e2e/tests/smoke/*`, `e2e/tests/full/*` | Keep smoke minimal and non-flaky |
| Pre-prod readiness gate | DONE | Disposable rehearsal: rollback drill + smoke + observability validation across backend + learner/admin frontends | Disposable rehearsal gate automated with learner + admin frontend coverage; persistent preprod verification is separately documented and required before production promotion | `.github/workflows/preprod-readiness.yml`, `docs/runbooks/preprod-readiness-checklist.md`, `docs/runbooks/real-preprod-verification.md` | Keep rehearsal green on release tags and require real preprod verification evidence before promote |
| Promotion automation wiring | DEFERRED | Real preprod deploy + prod promote via workflows | Workflows implemented; real infra command/URL wiring deferred to beta release | `.github/workflows/deploy-preprod.yml`, `.github/workflows/promote-prod.yml` | Wire real commands/URLs and run tagged dry-run |
| Concept learning (synsets, R/U/L) | PENDING | Phase 4 concepts/mastery system | Not started | `docs/plans/2026-02-26-full-rebuild.md` | Design + implement phase slice |
| Lexicon admin enrichment rollout | PARTIAL | Learner-first offline lexicon builder produces staged common-word entries plus learner-facing enrichment for local DB import | The words slice now supports shared entry metadata (`entry_id`, `entry_type`, `normalized_form`, `source_provenance`), a `wordfreq` top-words inventory path via `build-base --top-words` / `--rollout-stage`, deterministic pre-build canonicalization that collapses obvious inflectional duplicates into canonical headwords while preserving lexicalized forms like `left` as separate linked entries, snapshot-side registry/status artifacts plus `lookup-entry` / `status-entry` operator commands, and learner-translation generation for `zh-Hans`, `es`, `ar`, `pt-BR`, and `ja`. Compiled sense rows now preserve translation payloads and `import-db` writes meaning-level translated definitions into the DB `translations` table, while translated example/usage-note fields remain artifact-only in this slice. | `tools/lexicon/models.py`, `tools/lexicon/build_base.py`, `tools/lexicon/canonical_forms.py`, `tools/lexicon/canonical_registry.py`, `tools/lexicon/cli.py`, `tools/lexicon/wordfreq_utils.py`, `tools/lexicon/enrich.py`, `tools/lexicon/import_db.py`, `tools/lexicon/compile_export.py`, `tools/lexicon/validate.py`, `tools/lexicon/README.md`, `tools/lexicon/OPERATOR_GUIDE.md`, `backend/app/core/config.py` | Finish PR 2 verification, then move to PR 3 for optional ambiguous-form LLM adjudication |
| AI/media/listening/stories/admin | PENDING | Phases 5-9 product expansion | Not started | `docs/plans/2026-02-26-full-rebuild.md` | Sequence after beta core readiness |

---

## Current Top Gaps (Priority Order)

1. Implement dictionary API fallback for `/api/words/lookup` misses + tests.
2. Move import source storage from local/container temp paths to object storage (or equivalent shared ephemeral layer) with guaranteed cleanup on completion/failure and periodic TTL cleanup.
3. Beta-release activation: wire real deploy/promote variables and pass full tagged promotion drill.
4. Concept learning (synsets, R/U/L) phase design and first implementation slice.

---

## Release Readiness Snapshot

| Gate | Required | Current | Evidence |
|---|---|---|---|
| Backend lint + tests | Yes | Green | `CI / Backend (lint + test)` |
| Frontend lint + tests | Yes | Green | `CI / Frontend (lint + test)` |
| E2E smoke on PR | Yes | Green | `CI / E2E Smoke (required)` |
| Preprod readiness rehearsal workflow | Yes (for release) | Available with learner + admin frontend coverage | `.github/workflows/preprod-readiness.yml` |
| Deploy preprod workflow | Yes (for release) | Available (placeholder vars) | `.github/workflows/deploy-preprod.yml` |
| Production promote workflow | Yes (for release) | Available (placeholder vars) | `.github/workflows/promote-prod.yml` |
| Rollback runbook | Yes | Ready | `docs/runbooks/rollback.md` |

---

## Required Update Checklist (Every Significant Change)

1. Update relevant workstream row(s) in this board.
2. Add or refresh evidence link(s) (test command, workflow run, PR/commit).
3. Re-check release-readiness table if CI/workflows/runbooks changed.
4. Append one line in `Status Change Log`.

Suggested verification commands before marking a row as improved:

```bash
# Backend
cd backend && pytest -q

# Frontend
cd ../frontend && npm run lint && npm test -- --runInBand --watch=false

# CI workflow syntax (local sanity)
ruby -e 'require "yaml"; YAML.load_file(".github/workflows/ci.yml"); puts "ci.yml OK"'
```

---

## Status Change Log

| 2026-03-12 | Completed the lexicon learner-translation PR 2 slice on feature branch `feat_lexicon_translations_20260312`. The lexicon enrichment schema now requires learner translations for `zh-Hans`, `es`, `ar`, `pt-BR`, and `ja`; `compile-export` preserves them at the sense level; and `import-db` writes meaning-level translated definitions into the DB `translations` table while leaving translated example/usage-note fields in artifacts for now. This keeps the current PR tool/import-focused without expanding backend/admin API exposure in the same slice. | Codex | `/Users/johnson/AI/src/words-v2/.venv-lexicon/bin/python -m pytest tools/lexicon/tests -q` (`156` passed), plus real placeholder CLI smoke via `/Users/johnson/AI/src/words-v2/.venv-lexicon/bin/python -m tools.lexicon.cli build-base run --output-dir /tmp/lexicon-translations-pr2-smoke`, `/Users/johnson/AI/src/words-v2/.venv-lexicon/bin/python -m tools.lexicon.cli enrich --snapshot-dir /tmp/lexicon-translations-pr2-smoke --provider-mode placeholder --output /tmp/lexicon-translations-pr2-smoke/enrichments.jsonl`, `/Users/johnson/AI/src/words-v2/.venv-lexicon/bin/python -m tools.lexicon.cli validate --snapshot-dir /tmp/lexicon-translations-pr2-smoke`, and `/Users/johnson/AI/src/words-v2/.venv-lexicon/bin/python -m tools.lexicon.cli compile-export --snapshot-dir /tmp/lexicon-translations-pr2-smoke --output /tmp/lexicon-translations-pr2-smoke/words.enriched.jsonl` (compiled row contained all 5 locales). |
| 2026-03-12 | Completed the lexicon canonical-registry PR 1 slice on feature branch `feat_lexicon_canonical_registry_20260312`. `build-base` now emits canonical registry/status sidecars, deterministically collapses obvious inflectional duplicates before sense selection, preserves lexicalized forms as linked standalone entries, and the CLI grows `lookup-entry` / `status-entry` so operators can resolve `things -> thing`-style mappings and check whether the canonical word has reached base-build/enrich/compile stages or the local DB. The multi-PR continuation path for PR 2 (translations) and PR 3 (ambiguous-form LLM adjudication) remains anchored in `docs/plans/2026-03-12-lexicon-canonical-registry-translations-multi-pr-plan.md` instead of chat-only context. | Codex | `/Users/johnson/AI/src/words-v2/.venv-lexicon/bin/python -m pytest tools/lexicon/tests -q` (`152` passed) and real CLI smoke via `/Users/johnson/AI/src/words-v2/.venv-lexicon/bin/python -m tools.lexicon.cli build-base gives giving things left --output-dir /tmp/lexicon-canonical-pr1-smoke`, `/Users/johnson/AI/src/words-v2/.venv-lexicon/bin/python -m tools.lexicon.cli lookup-entry --snapshot-dir /tmp/lexicon-canonical-pr1-smoke things`, and `/Users/johnson/AI/src/words-v2/.venv-lexicon/bin/python -m tools.lexicon.cli status-entry --snapshot-dir /tmp/lexicon-canonical-pr1-smoke left` (confirmed `give`, `thing`, and lexicalized `left` behavior). |
| 2026-03-11 | Shipped the first learner-first common-words slice in the lexicon tool. `build-base` now supports `--top-words` and staged rollout aliases, snapshot/compiled rows now carry shared entry metadata, word inventory filtering removes obvious junk tokens, and per-word enrichment now treats WordNet as grounding context while allowing the LLM to keep a bounded learner-friendly subset of meanings by frequency band. | Codex | `../../.venv-lexicon/bin/python -m pytest tools/lexicon/tests/test_models.py tools/lexicon/tests/test_build_base.py tools/lexicon/tests/test_cli.py tools/lexicon/tests/test_enrich.py tools/lexicon/tests/test_compile_export.py tools/lexicon/tests/test_validate.py -q` (`110` passed), `../../.venv-lexicon/bin/python -m pytest tools/lexicon/tests -q` (`141` passed), and bounded local smoke via `../../.venv-lexicon/bin/python -m tools.lexicon.cli build-base --top-words 10 --output-dir /tmp/lexicon-words-slice-smoke`, `enrich --snapshot-dir /tmp/lexicon-words-slice-smoke --provider-mode placeholder --mode per_word --max-concurrency 4`, `validate --snapshot-dir /tmp/lexicon-words-slice-smoke`, `compile-export --snapshot-dir /tmp/lexicon-words-slice-smoke --output /tmp/lexicon-words-slice-smoke/words.enriched.jsonl`, `validate --compiled-input /tmp/lexicon-words-slice-smoke/words.enriched.jsonl`, and `import-db --input /tmp/lexicon-words-slice-smoke/words.enriched.jsonl --dry-run` (all passed). |
| 2026-03-12 | Hardened the lexicon per-word real-enrichment path and closed two live operator failures. The word prompt now repeats the hard 8/6/4 learner-meaning cap, requires a JSON object only, and retries once with a repair prompt when the first word-level payload is invalid. During live smoke verification, re-imported meanings exposed a duplicate-example autoflush issue; `import-db` now flushes deletions before re-inserting examples/relations and de-duplicates repeated sentences/relations within a meaning. The backend settings loader also now ignores unrelated extra env keys so `import-db` can run from the normal repo-root flow even when the repository `.env` contains non-backend keys. | Codex | `../../.venv-lexicon/bin/python -m pytest tools/lexicon/tests/test_enrich.py -q` (`28` passed), `../../.venv-lexicon/bin/python -m pytest tools/lexicon/tests/test_enrich.py tools/lexicon/tests/test_import_db.py -q` (`34` passed), `../../.venv-lexicon/bin/python -m pytest tools/lexicon/tests -q` (`145` passed), `PYTHONPATH=backend ../../.venv-backend/bin/python -m pytest backend/tests/test_config.py -q` (`4` passed), and real local smoke with `build-base run set play --output-dir /tmp/lexicon-per-word-real-20260312-set-repair`, `enrich --snapshot-dir /tmp/lexicon-per-word-real-20260312-set-repair --provider-mode openai_compatible_node --mode per_word --max-concurrency 1 --model gpt-5.1 --reasoning-effort low`, `validate --snapshot-dir /tmp/lexicon-per-word-real-20260312-set-repair`, `compile-export --snapshot-dir /tmp/lexicon-per-word-real-20260312-set-repair --output /tmp/lexicon-per-word-real-20260312-set-repair/words.enriched.jsonl`, `validate --compiled-input /tmp/lexicon-per-word-real-20260312-set-repair/words.enriched.jsonl`, and `import-db --input /tmp/lexicon-per-word-real-20260312-set-repair/words.enriched.jsonl --source-reference lexicon-per-word-real-20260312-set-repair-rootflow` (pass; `updated_words=3`, `updated_meanings=18`, `created_examples=36`, `deleted_examples=36`, `created_relations=175`, `deleted_relations=175`, `created_enrichment_runs=18`). Direct DB verification via `docker exec words-postgres psql ...` showed `6` meanings each for `run`, `set`, and `play` under `lexicon-per-word-real-20260312-set-repair:%`. |
| 2026-03-12 | Fixed lexicon DB provenance for compiled per-word imports so one shared `generation_run_id` now persists as one `lexicon_enrichment_run` row per word request instead of one row per meaning. After the fix, the local app DB was wiped and a fresh real 100-word rollout was run into `data/lexicon/snapshots/words-100-20260312-fresh`, then imported successfully. Post-import verification showed `100` words, `284` meanings, `633` examples, `2707` relations, `100` enrichment jobs, and `100` enrichment runs, and sampled words like `go`, `good`, `know`, `make`, `time`, and `well` each now show `run_count = 1`. | Codex | `../../.venv-lexicon/bin/python -m pytest tools/lexicon/tests/test_import_db.py -q` (`6` passed), `../../.venv-lexicon/bin/python -m pytest tools/lexicon/tests -q` (`144` passed), local DB reset via `docker exec words-postgres psql ... TRUNCATE TABLE ... RESTART IDENTITY CASCADE` (all app tables back to `0` rows), and real rollout via `build-base --rollout-stage 100 --output-dir data/lexicon/snapshots/words-100-20260312-fresh`, `enrich --snapshot-dir data/lexicon/snapshots/words-100-20260312-fresh --provider-mode openai_compatible_node --mode per_word --max-concurrency 1 --model gpt-5.1 --reasoning-effort low`, `validate`, `compile-export`, `validate --compiled-input`, and `import-db --input data/lexicon/snapshots/words-100-20260312-fresh/words.enriched.jsonl --source-reference words-100-20260312-fresh` (pass; `created_words=100`, `created_meanings=284`, `created_examples=633`, `created_relations=2707`, `created_enrichment_jobs=100`, `created_enrichment_runs=100`). Direct DB verification via `docker exec words-postgres psql ...` confirmed sampled words each have `run_count = 1`. |
| Date (UTC) | Change | Editor | Evidence |
|---|---|---|---|
| 2026-03-09 | Tightened repository agent workflow governance so merged PR branches/worktrees are no longer treated as informal scratchpads. `AGENTS.md` now explicitly requires post-merge cleanup or immediate re-branching for follow-up work, plus intentional preservation (`commit`, `stash`, or patch backup) before discarding dirty worktrees. | Codex | Governance/docs-only change verified via direct file inspection in `AGENTS.md` and local cleanup audit evidence from the same session. |
| 2026-03-09 | Added an in-app lexicon admin portal at `/admin/lexicon` so staged review is no longer API-only. The frontend now exposes staged review batches/items, item-decision editing, publish preview/publish actions, and a local-DB inspector backed by the existing word search/enrichment APIs; auth/nav protection now also covers `/admin/*`. This makes it much easier to understand what still needs human review versus what is already published into the local DB, while leaving backend admin-role enforcement and stricter final review/import gating as follow-up work. | Codex | `cd frontend && npm test -- --runInBand src/app/admin/lexicon/__tests__/page.test.tsx src/app/__tests__/layout-auth-nav.test.tsx src/app/__tests__/page.test.tsx src/lib/__tests__/lexicon-reviews-client.test.ts src/lib/__tests__/words-client.test.ts` (`25` passed), `cd frontend && npm run lint -- src/app/admin/lexicon/page.tsx src/app/admin/lexicon/__tests__/page.test.tsx src/lib/lexicon-reviews-client.ts src/lib/words-client.ts src/lib/__tests__/lexicon-reviews-client.test.ts src/lib/__tests__/words-client.test.ts src/lib/auth-nav.tsx src/lib/auth-route-guard.ts src/middleware.ts src/app/__tests__/layout-auth-nav.test.tsx src/app/__tests__/page.test.tsx` (pass), and plan added at `docs/plans/2026-03-09-lexicon-admin-portal-implementation.md`. |
| 2026-03-06 | Initialized canonical project status board and consolidated tracking sources. | Codex | `docs/plans/2026-02-26-full-rebuild.md`, `docs/plans/2026-03-05-current-state-phase-plan.md` |
| 2026-03-06 | Auth lifecycle hardening implemented (backend refresh/logout with token lifecycle controls, frontend protected routes/logout/401-refresh behavior, auth smoke coverage). | Codex | `docker compose -f docker-compose.test.yml run --rm --build test sh -lc "pip install -q -r requirements-test.txt && pytest -q"` (113 passed), `npm --prefix frontend run lint` (pass), `npm --prefix frontend test -- --runInBand` (7 suites/26 tests passed), `docker compose -f docker-compose.yml --profile tests exec -T playwright ... npm run test:smoke:ci` (6 passed), `docker compose -f docker-compose.yml --profile tests exec -T playwright ... npm run test:full` (7 passed) |
| 2026-03-06 | Word-list import domain delivered: new domain tables/models/APIs/tasks + `/imports` frontend + import-domain smoke/full verification. | Codex | `docker compose -f docker-compose.test.yml run --rm --build test sh -lc "pip install -q -r requirements-test.txt && pytest -q"` (127 passed), `npm --prefix frontend run lint` (pass), `npm --prefix frontend test -- --runInBand` (9 suites/35 tests passed), `docker compose -f docker-compose.yml --profile tests exec -T backend alembic upgrade head` (to 005), `docker compose -f docker-compose.yml --profile tests exec -T playwright sh -lc "cd /workspace/e2e && npm run test:smoke:ci"` (7 passed), `docker compose -f docker-compose.yml --profile tests exec -T playwright sh -lc "cd /workspace/e2e && npm run test:full"` (8 passed) |
| 2026-03-06 | Import completion hardening delivered: fallback NLP for missing spaCy model, shared upload directory for backend/worker, and terminal-state full E2E with valid EPUB fixture. | Codex | `docker compose -f docker-compose.test.yml run --rm --build test sh -lc "pip install -q -r requirements-test.txt && pytest tests/test_epub_processing.py tests/test_word_lists_api.py tests/test_imports_api.py -q"` (17 passed), `docker compose -f docker-compose.test.yml run --rm --build test sh -lc "pip install -q -r requirements-test.txt && pytest -q"` (129 passed), `npm --prefix frontend run lint` (pass), `npm --prefix frontend test -- --runInBand` (9 suites/35 tests passed), `docker compose -f docker-compose.yml --profile tests exec -T backend alembic upgrade head` (to 005), `docker compose -f docker-compose.yml --profile tests exec -T playwright sh -lc "cd /workspace/e2e && npm run test:smoke:ci"` (7 passed), `docker compose -f docker-compose.yml --profile tests exec -T playwright sh -lc "cd /workspace/e2e && npm run test:full"` (9 passed) |
| 2026-03-06 | Added explicit TODO for import storage lifecycle: move upload/temp artifacts to object storage (or equivalent shared temp layer) and enforce cleanup guarantees. | Codex | `docs/status/project-status.md` |
| 2026-03-07 | Lexicon tool foundation implemented in worktree: Python CLI scaffold, WordNet/wordfreq base builder, validation/compiler modules, stable ID helpers, local DB importer foundation, and provenance-ready model/migration changes. | Codex | `python3 -m unittest discover -s tools/lexicon/tests -p "test_*.py"` (27 tests passed), `python3 -m unittest tools.lexicon.tests.test_import_db` (2 tests passed), `PYTHONPYCACHEPREFIX=/tmp/pycache python3 -m py_compile tools/lexicon/import_db.py tools/lexicon/cli.py tools/lexicon/build_base.py tools/lexicon/compile_export.py tools/lexicon/validate.py backend/app/models/word.py backend/app/models/meaning.py backend/alembic/versions/006_add_lexicon_import_provenance.py` (pass); backend runtime model import smoke blocked locally because `sqlalchemy` is not installed in this shell. |
| 2026-03-07 | Lexicon tool snapshot file-I/O + CLI wiring delivered: normalized snapshot write support, snapshot validation from disk, compiled export generation, and `import-db --dry-run` CLI summary path. | Codex | `python3 -m unittest discover -s tools/lexicon/tests -p "test_*.py"` (31 tests passed), `python3 -m unittest tools.lexicon.tests.test_cli` (5 tests passed), `PYTHONPYCACHEPREFIX=/tmp/pycache python3 -m py_compile tools/lexicon/jsonl_io.py tools/lexicon/build_base.py tools/lexicon/validate.py tools/lexicon/compile_export.py tools/lexicon/cli.py tools/lexicon/import_db.py` (pass) |
| 2026-03-07 | Lexicon tool admin import path hardened: real sync-session DB import wiring now mirrors backend patterns, CLI docs/tests reflect `--compiled-input`/`--compiled-path` and `--language`, and provenance columns are ready for compiled snapshot loads. | Codex | `python3 -m unittest discover -s tools/lexicon/tests -p "test_*.py"` (35 tests passed), `python3 -m unittest tools.lexicon.tests.test_cli` (9 tests passed), `PYTHONPYCACHEPREFIX=/tmp/pycache python3 -m py_compile tools/lexicon/import_db.py tools/lexicon/cli.py tools/lexicon/build_base.py tools/lexicon/compile_export.py tools/lexicon/validate.py backend/app/models/word.py backend/app/models/meaning.py backend/alembic/versions/006_add_lexicon_import_provenance.py` (pass); real DB-path smoke still depends on local backend DB deps/settings. |
| 2026-03-07 | Lexicon provider/enrichment slice landed: `build-base` now resolves real WordNet + `wordfreq` providers with fail-loud dependency errors on the operator path, and a separate offline `enrich` command now writes `enrichments.jsonl` before validation/compile/import. | Codex | `python3 -m unittest discover -s tools/lexicon/tests -p "test_*.py"` (40 tests passed), `python3 -m unittest tools.lexicon.tests.test_cli tools.lexicon.tests.test_enrich` (13 tests passed), `PYTHONPYCACHEPREFIX=/tmp/pycache python3 -m py_compile tools/lexicon/build_base.py tools/lexicon/wordnet_provider.py tools/lexicon/wordfreq_provider.py tools/lexicon/enrich.py tools/lexicon/cli.py tools/lexicon/compile_export.py tools/lexicon/validate.py tools/lexicon/import_db.py backend/app/models/word.py backend/app/models/meaning.py backend/alembic/versions/006_add_lexicon_import_provenance.py` (pass); live operator smoke with installed `nltk`/WordNet and `wordfreq` is not run in this shell because those packages/corpora are absent locally. |
| 2026-03-07 | Lexicon operator smoke verified in a repo-local venv: installed `tools/lexicon/requirements.txt`, downloaded WordNet corpora, and ran `build-base -> enrich -> validate -> compile-export` successfully against a real snapshot directory. | Codex | `./.venv-lexicon/bin/python -m pip install -r tools/lexicon/requirements.txt` (pass), `./.venv-lexicon/bin/python -m nltk.downloader wordnet omw-1.4` (pass), `./.venv-lexicon/bin/python -m tools.lexicon.cli build-base run set lead --output-dir /tmp/lexicon-smoke` (3 lexemes / 12 senses / 12 concepts), `./.venv-lexicon/bin/python -m tools.lexicon.cli enrich --snapshot-dir /tmp/lexicon-smoke` (12 enrichments), `./.venv-lexicon/bin/python -m tools.lexicon.cli validate --snapshot-dir /tmp/lexicon-smoke` (0 errors), `./.venv-lexicon/bin/python -m tools.lexicon.cli compile-export --snapshot-dir /tmp/lexicon-smoke --output /tmp/lexicon-smoke/words.enriched.jsonl` (3 compiled rows), `python3 -m unittest discover -s tools/lexicon/tests -p "test_*.py"` (40 tests passed), `PYTHONPYCACHEPREFIX=/tmp/pycache python3 -m py_compile tools/lexicon/build_base.py tools/lexicon/wordnet_provider.py tools/lexicon/wordfreq_provider.py tools/lexicon/enrich.py tools/lexicon/cli.py tools/lexicon/compile_export.py tools/lexicon/validate.py tools/lexicon/import_db.py backend/app/models/word.py backend/app/models/meaning.py backend/alembic/versions/006_add_lexicon_import_provenance.py` (pass). |
| 2026-03-07 | Added dedicated lexicon CI coverage: a new workflow job now installs tool-local deps, caches/downloads WordNet corpora, runs `tools/lexicon` unit tests, and exercises the offline `build-base -> enrich -> validate -> compile-export` smoke flow without requiring a real LLM key. | Codex | `ruby -e 'require "yaml"; YAML.load_file(".github/workflows/ci.yml"); puts "ci.yml OK"'` (pass), `python3 -m unittest discover -s tools/lexicon/tests -p "test_*.py"` (40 tests passed), local repo-local-venv smoke flow (pass). |
| 2026-03-07 | Added a separate manual/nightly lexicon OpenAI-compatible smoke workflow: it validates secret-backed endpoint config in a protected environment, installs tool-local deps, downloads WordNet corpora, and runs a bounded offline smoke flow without exposing the real LLM key in PR CI. | Codex | `ruby -e 'require "yaml"; YAML.load_file(".github/workflows/lexicon-openai-compatible-smoke.yml"); puts "lexicon-openai-compatible-smoke.yml OK"'` (pass), `python3 -m unittest discover -s tools/lexicon/tests -p "test_*.py"` (40 tests passed). |
| 2026-03-07 | Real endpoint-backed lexicon enrichment landed: `enrich` now supports `auto|placeholder|openai_compatible` provider modes, `LEXICON_LLM_BASE_URL` config for OpenAI-compatible Responses endpoints, backward-compatible fallback from `LEXICON_LLM_PROVIDER`, and a manual/nightly workflow that exercises the real endpoint path while PR CI stays explicit placeholder-only. | Codex | `python3 -m unittest tools.lexicon.tests.test_enrich tools.lexicon.tests.test_config tools.lexicon.tests.test_cli` (22 tests passed), `python3 -m unittest discover -s tools/lexicon/tests -p "test_*.py"` (47 tests passed), `PYTHONPYCACHEPREFIX=/tmp/pycache python3 -m py_compile tools/lexicon/config.py tools/lexicon/enrich.py tools/lexicon/cli.py tools/lexicon/build_base.py tools/lexicon/wordnet_provider.py tools/lexicon/wordfreq_provider.py tools/lexicon/compile_export.py tools/lexicon/validate.py tools/lexicon/import_db.py` (pass), `ruby -e 'require "yaml"; YAML.load_file(".github/workflows/ci.yml"); YAML.load_file(".github/workflows/lexicon-openai-compatible-smoke.yml"); puts "workflow yaml OK"'` (pass). |
| 2026-03-07 | Added a tiny local real-endpoint smoke command: `smoke-openai-compatible` now runs `build-base -> enrich(openai_compatible) -> validate -> compile-export` for a tiny seed set, and was verified end-to-end against a local OpenAI-compatible mock endpoint. | Codex | `python3 -m unittest tools.lexicon.tests.test_cli tools.lexicon.tests.test_enrich tools.lexicon.tests.test_config` (24 tests passed), `python3 -m unittest discover -s tools/lexicon/tests -p "test_*.py"` (49 tests passed), `PYTHONPYCACHEPREFIX=/tmp/pycache python3 -m py_compile tools/lexicon/config.py tools/lexicon/enrich.py tools/lexicon/cli.py tools/lexicon/build_base.py tools/lexicon/wordnet_provider.py tools/lexicon/wordfreq_provider.py tools/lexicon/compile_export.py tools/lexicon/validate.py tools/lexicon/import_db.py` (pass), local mock smoke for `python -m tools.lexicon.cli smoke-openai-compatible --output-dir /tmp/lexicon-openai-compatible-local-smoke run set` (2 compiled rows, pass). |
| 2026-03-07 | OpenAI-compatible lexicon enrichment now validates required learner-facing response fields before record creation, failing loudly with field-specific errors for malformed `definition`, `examples`, or `confidence` payloads instead of silently falling back. | Codex | `python3 -m unittest tools.lexicon.tests.test_enrich` (9 tests passed), `python3 -m unittest tools.lexicon.tests.test_cli tools.lexicon.tests.test_enrich tools.lexicon.tests.test_config` (27 tests passed), `python3 -m unittest discover -s tools/lexicon/tests -p "test_*.py"` (52 tests passed), `PYTHONPYCACHEPREFIX=/tmp/lexicon-pycache python3 -m py_compile tools/lexicon/enrich.py tools/lexicon/cli.py tools/lexicon/config.py` (pass). |
| 2026-03-07 | OpenAI-compatible lexicon enrichment P1 schema hardening landed: the real provider path now validates CEFR/register enums, list-of-strings fields, `forms` shape, and `confusable_words` item structure before record creation, surfacing field-specific payload errors instead of silent coercion. | Codex | `python3 -m unittest tools.lexicon.tests.test_enrich` (14 tests passed), `python3 -m unittest tools.lexicon.tests.test_cli tools.lexicon.tests.test_enrich tools.lexicon.tests.test_config` (32 tests passed), `python3 -m unittest discover -s tools/lexicon/tests -p "test_*.py"` (57 tests passed), `PYTHONPYCACHEPREFIX=/tmp/lexicon-pycache python3 -m py_compile tools/lexicon/enrich.py tools/lexicon/cli.py tools/lexicon/config.py` (pass). |
| 2026-03-07 | Added lexicon pytest support for local admin/dev use and CI: a dedicated dev requirements file now installs pytest into the repo-local lexicon venv, both lexicon workflows run `python -m pytest tools/lexicon/tests -q`, and the existing bounded smoke flow remains in place after tests. | Codex | `PIP_NO_CACHE_DIR=1 ./.venv-lexicon/bin/python -m pip install -r tools/lexicon/requirements-dev.txt` (pass), `./.venv-lexicon/bin/python -m pytest tools/lexicon/tests -q` (57 passed), repo-local venv smoke flow for `build-base -> enrich --provider-mode placeholder -> validate -> compile-export` (3 lexemes / 12 enrichments / 3 compiled rows, pass), `ruby -e 'require "yaml"; YAML.load_file(".github/workflows/ci.yml"); YAML.load_file(".github/workflows/lexicon-openai-compatible-smoke.yml"); puts "workflow yaml OK"'` (pass). |
| 2026-03-07 | Real local DB import smoke verified for the lexicon tool against an isolated temporary Postgres instance: the worktree migration chain applied cleanly, a compiled export imported successfully, a second import updated in place without duplicate rows, and persisted provenance fields matched the expected `source_type` / `source_reference` values. | Codex | isolated DB smoke on alternate local ports with `alembic upgrade head` (001 -> 006 applied), `python -m tools.lexicon.cli import-db --input /tmp/lexicon-db-import-smoke/words.enriched.jsonl --source-type lexicon_snapshot --source-reference lexicon-smoke-20260307 --language en` (first run: 2 words / 4 meanings created), second identical import (2 words / 4 meanings updated), direct DB inspection confirmed 2 `words` rows and 4 `meanings` rows with `lexicon_snapshot` provenance. |
| 2026-03-07 | Added a Node-backed OpenAI-compatible lexicon transport for Cloudflare-fronted custom gateways: `openai_compatible_node` now uses the official Node OpenAI SDK, `LEXICON_LLM_TRANSPORT=node` can auto-select it, the manual/nightly smoke workflow installs tool-local Node deps when needed, and a real smoke against `https://api.nwai.cc` compiled 2 words successfully without changing the offline/admin pipeline shape. | Codex | `python3 -m unittest discover -s tools/lexicon/tests -p "test_*.py"` (63 tests passed), `npm --prefix tools/lexicon ci` (pass), `ruby -e 'require "yaml"; YAML.load_file(".github/workflows/ci.yml"); YAML.load_file(".github/workflows/lexicon-openai-compatible-smoke.yml"); puts "workflow yaml OK"'` (pass), real smoke via `/tmp/lexicon-db-smoke-venv/bin/python -m tools.lexicon.cli smoke-openai-compatible --provider-mode openai_compatible_node --output-dir /tmp/lexicon-openai-node-smoke run set` with `LEXICON_LLM_BASE_URL=https://api.nwai.cc`, `LEXICON_LLM_MODEL=gpt-5.1`, and env-provided key (lexeme_count 2 / compiled_count 2, pass). |
| 2026-03-07 | Added operator-facing lexicon setup docs: a tool-local `tools/lexicon/.env.example` now shows safe offline/admin env configuration, `tools/lexicon/OPERATOR_GUIDE.md` documents setup and the full `build-base -> enrich -> validate -> compile-export -> import-db` flow, and the root `.env.example` now points operators to those tool-local references instead of duplicating the full admin configuration. | Codex | `python3 -m unittest discover -s tools/lexicon/tests -p "test_*.py"` (63 tests passed), `git diff --check` (pass). |
| 2026-03-07 | Lexicon base selection now uses learner-oriented, frequency-aware sense ranking with adaptive `4/6/8` coverage inside an `--max-senses` ceiling of `8`: WordNet lemma counts and gloss heuristics still surface high-value noun meanings, and a new soft adjective/adverb viability layer now expands broad mixed-POS lemmas when useful adjective/adverb senses would otherwise be squeezed out, without hard POS quotas. Specialized adjective senses are also demoted more aggressively for cases like `direct`. | Codex | `python3 -m unittest discover -s tools/lexicon/tests -p "test_*.py"` (77 tests passed), `PYTHONPYCACHEPREFIX=/tmp/lexicon-ranking-pycache python3 -m py_compile tools/lexicon/wordnet_utils.py tools/lexicon/wordnet_provider.py tools/lexicon/build_base.py tools/lexicon/cli.py` (pass), 50-word `build-base --max-senses 8` sweep (distribution: 11×4 / 33×6 / 7×8; mixed-POS coverage improved for `right`, `clear`, `open`, `direct`, `present`, `light`, while `break` remained noun/verb-only), real gateway smoke via `/tmp/lexicon-db-smoke-venv/bin/python -m tools.lexicon.cli smoke-openai-compatible --provider-mode openai_compatible_node --max-senses 8 --output-dir /tmp/lexicon-ranking-openai-smoke-pos right open direct break` (4 lexemes / 32 enrichments / 4 compiled rows, pass), isolated temp Postgres import smoke on that compiled output (first run: 4 words / 32 meanings created; second run: 4 words / 32 meanings updated; provenance persisted as `lexicon_snapshot` / `lexicon-ranking-pos-20260307`). |
| 2026-03-07 | Dedicated lexicon model benchmarks now document low-effort enrichment quality and latency across `gpt-5.1`, `gpt-5.2`, `gpt-5.3`, and `gpt-5.4`: both the 4-prompt and 14-prompt runs concluded `gpt-5.4 ≈ gpt-5.3 > gpt-5.1 > gpt-5.2`, with `gpt-5.2` also the weakest speed/quality tradeoff. Operator-facing details, artifact locations, and 20k-word planning caveats now live in `tools/lexicon/MODEL_BENCHMARKS.md`. | Codex | 4-prompt validated benchmark artifacts under `/tmp/lexicon-model-compare`, 14-prompt validated benchmark artifacts under `/tmp/lexicon-model-compare-14`, schema validation passed for all generated JSON payloads, and shared-load 14-prompt timing rerun completed for all four models (`gpt-5.1`: 106s, `gpt-5.2`: 218s, `gpt-5.3`: 172s, `gpt-5.4`: 134s). |
| 2026-03-07 | Lexicon benchmark controls and sense ranking were refined further: the admin tool now supports optional `LEXICON_LLM_REASONING_EFFORT` plus per-run `--model` / `--reasoning-effort` overrides for `enrich` and `smoke-openai-compatible`, the OpenAI-compatible Python and Node transports forward reasoning effort correctly, and learner-facing sense ranking now boosts core concrete meanings while demoting low-value control senses, body-part-only adjective senses, and tournament-style specialized meanings. | Codex | `python3 -m unittest tools.lexicon.tests.test_config tools.lexicon.tests.test_enrich tools.lexicon.tests.test_cli` (44 tests passed), `python3 -m unittest tools.lexicon.tests.test_build_base` (20 tests passed), `python3 -m unittest discover -s tools/lexicon/tests -p "test_*.py"` (83 tests passed), `PYTHONPYCACHEPREFIX=/tmp/lexicon-ranking-pycache python3 -m py_compile tools/lexicon/wordnet_utils.py tools/lexicon/wordnet_provider.py tools/lexicon/build_base.py tools/lexicon/cli.py tools/lexicon/enrich.py tools/lexicon/config.py` (pass), synthetic selector spot-check after patch (`break`: `break.v.core` outranks `break.v.obedience`; `open`: `open.a.general` is retained while `open.n.tournament` drops out of the first four). |
| 2026-03-08 | Lexicon admin tooling now supports a truly bounded real smoke path plus grounded selector-vs-rerank evaluation: `smoke-openai-compatible` is capped by default to `1` word and `2` senses per word, the selector was further hardened for alias-like tails and practical learner meanings, `rerank-senses` now asks the LLM to choose only from bounded WordNet candidate `wn_synset_id`s, and `compare-selection` reports deterministic vs reranked swaps without altering the base snapshot schema. Operator docs, workflow smoke bounds, and a learner-priority rubric now match the new flow. | Codex | `python3 -m unittest discover -s tools/lexicon/tests -p "test_*.py"` (98 tests passed), `PYTHONPYCACHEPREFIX=/tmp/lexicon-rerank-pycache python3 -m py_compile tools/lexicon/wordnet_utils.py tools/lexicon/wordnet_provider.py tools/lexicon/build_base.py tools/lexicon/cli.py tools/lexicon/enrich.py tools/lexicon/rerank.py tools/lexicon/compare_selection.py` (pass), real bounded smoke via `./.venv-lexicon/bin/python -m tools.lexicon.cli smoke-openai-compatible --provider-mode openai_compatible_node --output-dir /tmp/lexicon-bounded-smoke-20260308 --max-words 1 --max-senses 2 --model gpt-5.1 --reasoning-effort low run` (1 lexeme / 2 senses / 2 enrichments / 1 compiled row, pass), real rerank + compare on `check` via `/tmp/lexicon-rerank-check-20260308` (`rerank-senses` wrote 1 row; `compare-selection` reported 1 changed lexeme, dropping deterministic tail `check.n.09` and adding grounded candidate `check.v.22`), `ruby -e 'require "yaml"; YAML.load_file(".github/workflows/lexicon-openai-compatible-smoke.yml"); puts "workflow yaml OK"'` (pass). |
| 2026-03-08 | Real selector-vs-rerank benchmark on tricky lemmas suggests the new grounded `rerank-senses` step is useful as a quality mode, but not a full replacement for deterministic ranking. On the 10-word batch (`right`, `direct`, `common`, `fine`, `fair`, `plain`, `check`, `charge`, `scale`, `break`), rerank changed all 10 selections and often removed obvious deterministic tails, but it also introduced some debatable substitutes, so the best near-term path is to keep improving deterministic ranking and use rerank selectively for high-polysemy review or premium-quality builds. | Codex | deterministic baseline via `./.venv-lexicon/bin/python -m tools.lexicon.cli build-base --max-senses 6 --output-dir /tmp/lexicon-rerank-benchmark-20260308 right direct common fine fair plain check charge scale break` (10 lexemes / 52 senses, pass), real rerank via `./.venv-lexicon/bin/python -m tools.lexicon.cli rerank-senses --snapshot-dir /tmp/lexicon-rerank-benchmark-20260308 --provider-mode openai_compatible_node --model gpt-5.4 --reasoning-effort low --candidate-limit 8 right direct common fine fair plain check charge scale break` (10 rerank rows, pass), compare via `./.venv-lexicon/bin/python -m tools.lexicon.cli compare-selection --snapshot-dir /tmp/lexicon-rerank-benchmark-20260308 --rerank-file /tmp/lexicon-rerank-benchmark-20260308/sense_reranks.jsonl --output /tmp/lexicon-rerank-benchmark-20260308/comparison.json` (`10/10` lexemes changed; clearer wins included removing `complain.v.01` from `plain`, dropping `check.n.09`, and replacing `break.v.40` / `break.n.11` tails). |

| 2026-03-08 | Lexicon benchmarking and rerank controls now cover unseen holdout evaluation too: the admin tool ships built-in `tuning` and `holdout` word lists plus a new `benchmark-selection` command, `rerank-senses` exposes explicit `selected_only`, `candidates`, and `full_wordnet` candidate-source modes, and the deterministic selector picked up another generic scoring pass for finance/document boosts plus sports/legal/religious/geometry tail demotions. The fresh real gateway run suggests `candidates` is the best practical rerank mode, `selected_only` is safest for audit-only review, and `full_wordnet` is useful mainly as an exploratory stress mode because it more often introduces debatable substitutions. | Codex | `python3 -m unittest discover -s tools/lexicon/tests -p "test_*.py"` (112 tests passed), `PYTHONPYCACHEPREFIX=/tmp/lexicon-holdout-benchmark-pycache python3 -m py_compile tools/lexicon/wordnet_utils.py tools/lexicon/wordnet_provider.py tools/lexicon/build_base.py tools/lexicon/cli.py tools/lexicon/enrich.py tools/lexicon/rerank.py tools/lexicon/compare_selection.py tools/lexicon/benchmark_selection.py` (pass), real deterministic holdout rebuild via `./.venv-lexicon/bin/python -m tools.lexicon.cli build-base --max-senses 6 --output-dir /tmp/lexicon-holdout-det-20260308 $(jq -r '.[]' tools/lexicon/benchmarks/holdout_words.json)` (20 lexemes / 106 senses, with visible improvements such as `file.n.01` surfacing and sports/biblical tails dropping), real mode comparison via `./.venv-lexicon/bin/python -m tools.lexicon.cli benchmark-selection --output-dir /tmp/lexicon-bench-suite-20260308 --dataset tuning --dataset holdout --with-rerank --provider-mode openai_compatible_node --model gpt-5.4 --reasoning-effort low --candidate-source selected_only --candidate-source candidates --candidate-source full_wordnet` (tuning: `selected_only 9/10`, `candidates 10/10`, `full_wordnet 10/10` changed; holdout: `selected_only 18/20`, `candidates 20/20`, `full_wordnet 20/20` changed; representative `candidates` wins included `case` dropping robbery/enclose tails and `change` dropping clothing/change-coverings tails, while `full_wordnet` also surfaced noisier substitutions like `event.n.02`, `travel_by.v.01`, and `orderliness.n.02`). |
| 2026-03-08 | Documented the next lexicon review architecture and deferred UI path: new design/spec docs now define a staged lexicon-review workflow in the main stack, exact `selection_decisions.jsonl` contract, the first-pass risk-score formula, bounded rerank routing and auto-accept rules, and the decision to keep a future admin review UI as TODO until the rebuild actually includes `admin-frontend/`. The accepted architecture is offline generation -> staged review storage -> targeted human review -> explicit publish, not raw JSONL inspection and not direct publish from snapshot files. | Codex | Design doc added at `docs/plans/2026-03-08-lexicon-review-staging-design.md`, implementation/spec plan added at `docs/plans/2026-03-08-lexicon-review-staging-implementation.md`, ADR added at `docs/decisions/ADR-003-lexicon-review-staging.md`, and live status updated here after verifying the new files exist and reference the current lexicon/admin integration anchors. |
| 2026-03-08 | Implemented the first offline review-staging slice for lexicon sense selection: the tool now ships `score-selection-risk` to write `selection_decisions.jsonl` from deterministic WordNet choices plus risk metadata, and `prepare-review` to rerank only risky lexemes, auto-accept stable changes, and emit `review_queue.jsonl` for remaining human review. Operator docs now describe the staged deterministic -> rerank -> human-review flow, and the review contract includes generation metadata plus stricter review gating for substantial high-frequency rerank changes. | Codex | `python3 -m unittest discover -s tools/lexicon/tests -p "test_*.py"` (120 tests passed), `PYTHONPYCACHEPREFIX=/tmp/lexicon-selection-review-pycache python3 -m py_compile tools/lexicon/selection_review.py tools/lexicon/cli.py tools/lexicon/rerank.py tools/lexicon/benchmark_selection.py tools/lexicon/wordnet_utils.py` (pass), real `score-selection-risk` smoke via `./.venv-lexicon/bin/python -m tools.lexicon.cli score-selection-risk --snapshot-dir /tmp/lexicon-holdout-det-20260308 --output /tmp/lexicon-holdout-det-20260308/selection_decisions.jsonl` (20 decisions; 19 `rerank_and_review_candidate`, 1 `deterministic_only`, pass), bounded offline `prepare-review` smoke via `./.venv-lexicon/bin/python -m tools.lexicon.cli prepare-review --snapshot-dir /tmp/lexicon-holdout-det-20260308 --decisions /tmp/lexicon-holdout-det-20260308/selection_decisions.deterministic_only.jsonl --output /tmp/lexicon-holdout-det-20260308/selection_decisions.deterministic_only.reviewed.jsonl --review-queue-output /tmp/lexicon-holdout-det-20260308/review_queue.deterministic_only.jsonl` (1 deterministic-only decision; 0 reranked; 0 review rows, pass). |
| 2026-03-08 | Implemented backend staging for lexicon review artifacts before final publish/import: dedicated `lexicon_review_batches` and `lexicon_review_items` tables now store imported `selection_decisions.jsonl` rows, and new owner-scoped API endpoints support batch import, batch list/detail, item list with filters, and per-item review decisions. This keeps staged lexicon review separate from the current direct `import-db` path, with publish-to-main tables still intentionally deferred. | Codex | `PYTHONPYCACHEPREFIX=/tmp/lexicon-review-backend-pycache python3 -m py_compile backend/app/api/lexicon_reviews.py backend/app/main.py backend/app/models/lexicon_review_batch.py backend/app/models/lexicon_review_item.py backend/alembic/versions/007_add_lexicon_review_staging.py backend/tests/test_lexicon_review_models.py backend/tests/test_lexicon_reviews_api.py` (pass), independent container verification via `docker run --rm -v /Users/johnson/AI/src/words-v2/.worktrees/lexicon-sense-ranking/backend:/app -w /app python:3.11-slim bash -lc "pip install -q fastapi sqlalchemy pydantic pydantic-settings email-validator redis python-multipart slowapi structlog 'python-jose[cryptography]' bcrypt asyncpg psycopg2-binary pytest pytest-asyncio httpx ebooklib beautifulsoup4 celery && PYTHONPATH=/app pytest -q tests/test_lexicon_review_models.py tests/test_lexicon_reviews_api.py"` (`13` passed), backend design doc added at `docs/plans/2026-03-08-lexicon-review-backend-staging-design.md`, implementation plan added at `docs/plans/2026-03-08-lexicon-review-backend-staging-implementation.md`. |
| 2026-03-08 | Added `publish-review-batch` on top of the staged lexicon review backend: approved staged items can now publish into the main `Word` / `Meaning` tables using a replace-by-source strategy that updates or creates `Word` rows, replaces only prior `lexicon_review_publish` meanings, and preserves meanings from other sources. This keeps staged review as the gate before publish while still supporting iterative lexicon improvement on the current schema. | Codex | `PYTHONPYCACHEPREFIX=/tmp/lexicon-review-publish-pycache python3 -m py_compile backend/app/api/lexicon_reviews.py backend/app/main.py backend/app/models/lexicon_review_batch.py backend/app/models/lexicon_review_item.py backend/alembic/versions/007_add_lexicon_review_staging.py backend/tests/test_lexicon_review_models.py backend/tests/test_lexicon_reviews_api.py` (pass), focused publish verification via `docker run --rm -v /Users/johnson/AI/src/words-v2/.worktrees/lexicon-sense-ranking/backend:/app -w /app python:3.11-slim bash -lc "pip install -q fastapi sqlalchemy pydantic pydantic-settings email-validator redis python-multipart slowapi structlog 'python-jose[cryptography]' bcrypt asyncpg psycopg2-binary pytest pytest-asyncio httpx ebooklib beautifulsoup4 celery && PYTHONPATH=/app pytest -q tests/test_lexicon_reviews_api.py -k publish"` (`4` passed), full staged-review backend verification via `docker run --rm -v /Users/johnson/AI/src/words-v2/.worktrees/lexicon-sense-ranking/backend:/app -w /app python:3.11-slim bash -lc "pip install -q fastapi sqlalchemy pydantic pydantic-settings email-validator redis python-multipart slowapi structlog 'python-jose[cryptography]' bcrypt asyncpg psycopg2-binary pytest pytest-asyncio httpx ebooklib beautifulsoup4 celery && PYTHONPATH=/app pytest -q tests/test_lexicon_review_models.py tests/test_lexicon_reviews_api.py tests/test_lexicon_review_publish_api.py"` (`20` passed), publish design doc added at `docs/plans/2026-03-08-lexicon-review-publish-design.md`, implementation plan added at `docs/plans/2026-03-08-lexicon-review-publish-implementation.md`. |
| 2026-03-08 | Added a read-only lexicon publish preview plus the first learner-facing enrichment schema slice. The backend now exposes `GET /api/lexicon-reviews/batches/{batch_id}/publish-preview` to dry-run staged publish impact without mutating the DB, and the schema now includes `meaning_examples`, `word_relations`, `lexicon_enrichment_jobs`, `lexicon_enrichment_runs`, plus `words.phonetic_source`, `words.phonetic_confidence`, and `words.phonetic_enrichment_run_id` for later LLM enrichment persistence. This slice also adds confidence-range constraints on new enrichment confidence fields; richer public APIs and the full enrichment pipeline remain deferred. | Codex | `PYTHONPYCACHEPREFIX=/tmp/lexicon-preview-enrichment-pycache python3 -m py_compile backend/app/api/lexicon_reviews.py backend/tests/test_lexicon_reviews_api.py backend/app/models/word.py backend/app/models/__init__.py backend/app/models/meaning_example.py backend/app/models/word_relation.py backend/app/models/lexicon_enrichment_job.py backend/app/models/lexicon_enrichment_run.py backend/alembic/versions/008_add_lexicon_enrichment_schema.py backend/tests/test_lexicon_enrichment_models.py` (pass), container verification via `docker run --rm -v /Users/johnson/AI/src/words-v2/.worktrees/lexicon-sense-ranking/backend:/app -w /app python:3.11-slim bash -lc "pip install -q fastapi sqlalchemy pydantic pydantic-settings email-validator redis python-multipart slowapi structlog 'python-jose[cryptography]' bcrypt asyncpg psycopg2-binary pytest pytest-asyncio httpx ebooklib beautifulsoup4 celery && PYTHONPATH=/app pytest -q tests/test_lexicon_review_models.py tests/test_lexicon_reviews_api.py tests/test_lexicon_review_publish_api.py tests/test_lexicon_enrichment_models.py"` (`28` passed). |
| 2026-03-08 | Extended the offline lexicon compiled/import path so learner-facing enrichment now writes through to the local DB instead of stopping at core words/meanings. `compile-export` now preserves sense-level provenance (`wn_synset_id`, enrichment/model/prompt/run/confidence metadata), and `import-db` now upserts `words`/`meanings`, replaces source-scoped `meaning_examples`, replaces source-scoped `word_relations` for `synonym`/`antonym`/`collocation`, creates or reuses word-scoped enrichment jobs plus sense-scoped enrichment runs, and reports dry-run example/relation counts for operators. Phrase linking, raw generator payload storage, and fuller learner-facing meaning fields remain deferred. | Codex | `python3 -m unittest tools.lexicon.tests.test_cli tools.lexicon.tests.test_compile_export tools.lexicon.tests.test_import_db` (40 tests passed), `python3 -m unittest discover -s tools/lexicon/tests -p 'test_*.py'` (122 tests passed), `PYTHONPYCACHEPREFIX=/tmp/lexicon-import-writeback-pycache python3 -m py_compile tools/lexicon/import_db.py tools/lexicon/compile_export.py tools/lexicon/cli.py tools/lexicon/tests/test_import_db.py tools/lexicon/tests/test_compile_export.py tools/lexicon/tests/test_cli.py` (pass), design doc added at `docs/plans/2026-03-08-lexicon-enrichment-import-writeback-design.md`, implementation plan added at `docs/plans/2026-03-08-lexicon-enrichment-import-writeback-implementation.md`. |
| 2026-03-08 | Added a dedicated read-only enrichment inspection endpoint at `GET /api/words/{word_id}/enrichment` so imported learner-facing data can be inspected without querying tables directly or changing the main word-detail contract. The endpoint returns word-level phonetic provenance, meanings, meaning examples, meaning-linked relations, and referenced enrichment runs; it follows the repo’s existing authenticated read pattern, while explicit role-based admin gating remains deferred. | Codex | `PYTHONPYCACHEPREFIX=/tmp/word-enrichment-api-pycache python3 -m py_compile backend/app/api/words.py backend/tests/test_words.py` (pass), container verification via `docker run --rm -v /Users/johnson/AI/src/words-v2/.worktrees/lexicon-sense-ranking/backend:/app -w /app python:3.11-slim bash -lc "pip install -q fastapi sqlalchemy pydantic pydantic-settings email-validator redis python-multipart slowapi structlog 'python-jose[cryptography]' bcrypt asyncpg psycopg2-binary pytest pytest-asyncio httpx ebooklib beautifulsoup4 celery && PYTHONPATH=/app pytest -q tests/test_words.py"` (`10` passed), design doc added at `docs/plans/2026-03-08-word-enrichment-inspection-api-design.md`, implementation plan added at `docs/plans/2026-03-08-word-enrichment-inspection-api-implementation.md`. |
| 2026-03-08 | Closed the lexicon tool as a working local-DB admin tool by finishing the three closure slices: the canonical final learner-enrichment DB write path is now explicitly `compile-export -> import-db`, a clean isolated end-to-end smoke proved `build-base -> enrich -> validate -> compile-export -> validate --compiled-input -> import-db -> API inspection`, and a reusable working-gate runbook plus future-improvements TODO list now define what is in-scope for closure vs deferred hardening. Staged review remains the review/decision layer; the narrower staged-review publish path is transitional until/unless it delegates to importer-equivalent writeback. | Codex | isolated DB creation via `docker exec words-postgres psql -U vocabapp -d postgres -c "DROP DATABASE IF EXISTS lexicon_working_gate_20260308;"` and `... -c "CREATE DATABASE lexicon_working_gate_20260308;"` (pass), isolated migration via `docker run --rm --network host -e DATABASE_URL=postgresql+asyncpg://vocabapp:devpassword@localhost:5432/lexicon_working_gate_20260308 -e DATABASE_URL_SYNC=postgresql://vocabapp:devpassword@localhost:5432/lexicon_working_gate_20260308 -v /Users/johnson/AI/src/words-v2/.worktrees/lexicon-sense-ranking/backend:/app -w /app python:3.11-slim bash -lc "pip install -q -r requirements.txt -r requirements-test.txt && alembic upgrade head"` (pass), offline smoke via `./.venv-lexicon/bin/python -m tools.lexicon.cli build-base run set lead --output-dir /tmp/lexicon-working-gate-20260308`, `... enrich --snapshot-dir /tmp/lexicon-working-gate-20260308 --provider-mode placeholder`, `... validate --snapshot-dir /tmp/lexicon-working-gate-20260308`, `... compile-export --snapshot-dir /tmp/lexicon-working-gate-20260308 --output /tmp/lexicon-working-gate-20260308/words.enriched.jsonl`, `... validate --compiled-input /tmp/lexicon-working-gate-20260308/words.enriched.jsonl` (all pass; 3 lexemes / 18 senses / 18 enrichments / 3 compiled rows / 0 validation errors), real import via `docker run --rm --network host -e DATABASE_URL=postgresql+asyncpg://vocabapp:devpassword@localhost:5432/lexicon_working_gate_20260308 -e DATABASE_URL_SYNC=postgresql://vocabapp:devpassword@localhost:5432/lexicon_working_gate_20260308 -v /Users/johnson/AI/src/words-v2/.worktrees/lexicon-sense-ranking:/workspace -v /tmp:/tmp -w /workspace python:3.11-slim bash -lc "pip install -q -r backend/requirements.txt && python -m tools.lexicon.cli import-db --input /tmp/lexicon-working-gate-20260308/words.enriched.jsonl --source-type lexicon_snapshot --source-reference lexicon-working-gate-20260308 --language en"` (3 words, 18 meanings, 18 examples, 3 jobs, 18 runs), API verification via temporary backend on `:18081` plus auth/search/enrichment read returned `run` with `6` meanings, `1` example on first meaning, and `6` referenced enrichment runs, docs added at `docs/decisions/ADR-004-lexicon-canonical-final-ingestion-path.md`, `docs/runbooks/lexicon-working-gate.md`, and `docs/plans/2026-03-08-lexicon-future-improvements-todo.md`. |
| 2026-03-08 | Implemented the learner-facing lexicon schema slice on top of the working admin pipeline: new DB columns now persist word-level CEFR/POS/confusables/timestamps plus meaning-level WordNet/domain/register/grammar/usage metadata and example difficulty, `import-db` now writes those compiled learner fields through on create/update paths, and `GET /api/words/{word_id}/enrichment` now exposes the enriched learner-facing shape for inspection. This closes the focused schema/API persistence gap for compiled lexicon output while leaving broader phrase/idiom expansion and fuller review/publish unification as future work. | Codex | `/Users/johnson/AI/src/words-v2/.venv-lexicon/bin/python -m pytest tools/lexicon/tests/test_import_db.py -q` (`4` passed), `PYTHONPATH=backend /Users/johnson/AI/src/words-v2/.venv-backend/bin/python -m pytest backend/tests/test_lexicon_enrichment_models.py backend/tests/test_words.py -q` (`16` passed), migration added at `backend/alembic/versions/009_add_lexicon_learner_fields.py`, and focused implementation landed in `tools/lexicon/import_db.py`, `backend/app/models/word.py`, `backend/app/models/meaning.py`, `backend/app/models/meaning_example.py`, and `backend/app/api/words.py`. |
| 2026-03-08 | Verified the learner-facing lexicon schema slice against real local infrastructure, not just unit tests: an isolated Postgres DB was migrated to revision `009`, a real OpenAI-compatible bounded enrichment smoke (`https://api.nwai.cc`, `gpt-5.1`, Node transport, low reasoning) was compiled and imported, and the imported learner-facing fields were read back successfully through `GET /api/words/{word_id}/enrichment`. This confirms the branch’s schema/import/API wiring works end to end on a populated local backend path. | Codex | isolated DBs `lexicon_learner_schema_smoke_20260308` and `lexicon_learner_schema_real_smoke_20260308` created locally, migration via `DATABASE_URL_SYNC=postgresql://vocabapp:devpassword@localhost:5432/... /Users/johnson/AI/src/words-v2/.venv-backend/bin/python -m alembic upgrade head` (pass to `009`), placeholder smoke import via `.../.venv-lexicon/bin/python -m tools.lexicon.cli build-base/enrich/validate/compile-export` plus Docker-host import (3 words / 18 meanings / 18 examples), real smoke via `LEXICON_LLM_BASE_URL=https://api.nwai.cc LEXICON_LLM_MODEL=gpt-5.1 LEXICON_LLM_TRANSPORT=node .../.venv-lexicon/bin/python -m tools.lexicon.cli smoke-openai-compatible --provider-mode openai_compatible_node --output-dir /tmp/lexicon-real-openai-smoke-20260308 --max-words 1 --max-senses 2 --model gpt-5.1 --reasoning-effort low run` (1 word / 2 senses / 2 enrichments / 1 compiled row), real import via Docker-host path (1 word / 2 meanings / 9 examples / 27 relations / 2 enrichment runs), and real API readback returned `run` with `cefr_level=A1`, two meanings, populated confusables, and example difficulties. |
| 2026-03-08 | Completed the first real staged lexicon rollout on local infrastructure: a checkpointed 100-word batch was built from WordNet+wordfreq, enriched against the custom OpenAI-compatible gateway (`https://api.nwai.cc`, `gpt-5.1`, Node transport, low reasoning), validated/compiled per chunk, and incrementally imported into an isolated DB for API readback. The run surfaced two real operator hardening gaps in `tools/lexicon/enrich.py` (JSON-with-wrapper parsing and non-critical confidence robustness); both were fixed with regression coverage before resuming and finishing Stage 1. | Codex | parser/confidence hardening verified via `/Users/johnson/AI/src/words-v2/.venv-lexicon/bin/python -m pytest tools/lexicon/tests/test_enrich.py tools/lexicon/tests/test_import_db.py -q` (`28` passed); isolated DB `lexicon_stage100_20260308` reset + migrated to `009` via `docker exec words-postgres psql -U vocabapp -d postgres -c "DROP DATABASE IF EXISTS lexicon_stage100_20260308;"`, `... -c "CREATE DATABASE lexicon_stage100_20260308;"`, and `DATABASE_URL_SYNC=postgresql://vocabapp:devpassword@localhost:5432/lexicon_stage100_20260308 /Users/johnson/AI/src/words-v2/.venv-backend/bin/python -m alembic upgrade head` (pass); chunked real rollout via `.../.venv-lexicon/bin/python -m tools.lexicon.cli build-base --max-senses 4`, `... enrich --provider-mode openai_compatible_node --model gpt-5.1 --reasoning-effort low`, `... validate --snapshot-dir`, `... compile-export`, `... validate --compiled-input`, and backend-venv `import-db` across `chunk01`-`chunk10` (final aggregate: `100` words / `377` meanings / `1731` examples / `5491` relations / `100` enrichment jobs / `377` enrichment runs); API verification against temporary backend on `:18081` returned imported learner-facing enrichment for `run` (`A1`, `4` meanings, `4` runs in early chunk check) and `teacher` (`A1`, `2` meanings, `2` runs) from the isolated DB. |
| 2026-03-08 | Implemented the per-word concurrent lexicon enrichment path for staged rollouts: `enrich` now supports `--enrichment-unit word` plus bounded concurrency/retry/resume/force controls, writes `word_enrichments.jsonl` + `enrich.manifest.json`, `validate` understands word-mode selected senses, and `compile-export` consumes the per-word output while preserving the canonical `words.enriched.jsonl -> import-db` path. A tiny real-gateway smoke also passed on the new path, so the next rollout gate is Stage 1.5 (`100` words) before the clean-reset `vocabapp_dev` Stage 2 run. | Codex | `/Users/johnson/AI/src/words-v2/.venv-lexicon/bin/python -m pytest tools/lexicon/tests -q` (`133` passed), placeholder word-mode smoke via `build-base -> enrich --enrichment-unit word -> validate -> compile-export -> validate` on `/tmp/lexicon-per-word-smoke-*` (pass), real gateway smoke via `LEXICON_LLM_BASE_URL=https://api.nwai.cc LEXICON_LLM_MODEL=gpt-5.1 LEXICON_LLM_TRANSPORT=node ... /Users/johnson/AI/src/words-v2/.venv-lexicon/bin/python -m tools.lexicon.cli enrich --snapshot-dir /tmp/lexicon-per-word-real-smoke-*/ --provider-mode openai_compatible_node --model gpt-5.1 --reasoning-effort low --enrichment-unit word --max-concurrency 2` followed by `validate`, `compile-export`, and compiled validation (2 words / 2 `word_enrichments` / 2 compiled rows). |
| 2026-03-09 | Executed the real Stage 1.5 proving run on the new per-word path using the exact 100-word Stage 1 corpus. The per-word gateway batch completed with zero failed lexemes and produced `100` word enrichments; snapshot validation and compiled validation both stayed clean, and the compiled dry-run shape was `100` words / `478` senses / `824` examples / `3879` relations. The staged review gate also ran end to end at the artifact layer: deterministic risk scoring initially marked `61` direct review candidates and `24` rerank-recommended lexemes, bounded rerank auto-accepted `35`, and `50` lexemes still remained in `review_queue.jsonl`, so Stage 2 is not yet a clear go without review triage or more selector/risk tuning. | Codex | `/Users/johnson/AI/src/words-v2/.venv-lexicon/bin/python -m tools.lexicon.cli build-base --snapshot-id lexicon-stage15-20260309-word --output-dir /tmp/lexicon-stage15-20260309/snapshot $(cat /tmp/lexicon-stage15-20260309/words.txt)` (`100` lexemes / `481` senses / `481` concepts), real enrichment via `LEXICON_LLM_BASE_URL=https://api.nwai.cc LEXICON_LLM_MODEL=gpt-5.1 LEXICON_LLM_TRANSPORT=node ... /Users/johnson/AI/src/words-v2/.venv-lexicon/bin/python -m tools.lexicon.cli enrich --snapshot-dir /tmp/lexicon-stage15-20260309/snapshot --provider-mode openai_compatible_node --model gpt-5.1 --reasoning-effort low --enrichment-unit word --max-concurrency 4 --max-retries 1 --resume` (`100` `word_enrichments` / `0` failures), validation + compile + dry-run via `validate`, `compile-export`, `validate --compiled-input`, and `import-db --dry-run` (clean; `100` rows / `478` senses / `824` examples / `3879` relations), risk scoring via `score-selection-risk` (`61` `rerank_and_review_candidate`, `24` `rerank_recommended`, `15` `deterministic_only`), and bounded rerank via `prepare-review --candidate-source candidates --candidate-limit 8` (`35` auto-accepted / `50` still review-required). |
| 2026-03-09 | Investigated why staged review smoke failed against the local dev stack and found two separate operational blockers, not a route-definition bug: the shared `words-backend` container was mounted from a different worktree (`word-lookup-attestation`), so it returned `404` for `/api/lexicon-reviews/*`, and once a temporary backend was started from the current worktree on `127.0.0.1:18082`, `publish-preview` failed against `vocabapp_dev` because that DB is schema-drifted and missing `words.source_type`. Review import and item update now work against the current worktree backend, but Stage 2 should not reuse the current `vocabapp_dev` blindly; it needs the planned clean reset/current backend pairing first. | Codex | `docker inspect words-backend` showed mount `/Users/johnson/AI/src/words-v2/.worktrees/word-lookup-attestation/backend -> /app`, while current source still mounts the review router at `backend/app/main.py`; temporary current-worktree backend launched via `/Users/johnson/AI/src/words-v2/.venv-backend/bin/python -m alembic upgrade head` plus `uvicorn app.main:app --host 127.0.0.1 --port 18082`, review import succeeded on `POST /api/lexicon-reviews/batches/import`, item patch succeeded on `PATCH /api/lexicon-reviews/items/{item_id}`, and `GET /api/lexicon-reviews/batches/{batch_id}/publish-preview` failed with `asyncpg.exceptions.UndefinedColumnError: column words.source_type does not exist` against `vocabapp_dev`. |
| 2026-03-09 | Rebuilt the shared docker dev stack cleanly from the current lexicon worktree and proved the staged-review backend flow against the fresh database. The old `words-*` containers and attached data volumes were removed, a clean `words-v2` stack was started from `feat_lexicon_batch_rollout_plan_20260308`, migrations ran through `009`, and the review smoke then succeeded on the rebuilt backend: importing the Stage 1.5 reviewed decisions, listing the `50` flagged items, approving one pending item, and computing a publish preview that would publish `36` approved items on the clean DB. This closes the stale-backend/schema-drift blocker; the remaining Stage 2 blocker is content/review burden, not local infra correctness. | Codex | destructive reset via `docker rm -f words-playwright words-frontend words-worker words-backend words-postgres words-redis` plus volume/network removal for `word-lookup-attestation_*`, `words-v2_*`, and stale words networks; clean stack start via `docker compose -p words-v2 -f docker-compose.yml up -d --build postgres redis backend worker frontend`; migration via `docker compose -p words-v2 -f docker-compose.yml exec -T backend alembic upgrade head` (pass through `009`); rebuilt backend verification via `curl http://localhost:8000/api/health` (`ok`) and `docker inspect words-backend` showing bind mount `/Users/johnson/AI/src/words-v2/.worktrees/lexicon-batch-rollout-plan/backend -> /app`; review smoke via `POST /api/lexicon-reviews/batches/import`, `GET /api/lexicon-reviews/batches/{batch_id}/items?review_required=true`, `PATCH /api/lexicon-reviews/items/{item_id}`, and `GET /api/lexicon-reviews/batches/{batch_id}/publish-preview` on `localhost:8000` (batch `47fd68b1-3c93-407a-9ceb-631abe0eba5f`, `50` review-required, `35` auto-accepted, preview `36` publishable create-word items after one manual approval). |
| 2026-03-09 | Applied the first selector/rerank hardening pass for the Stage 1.5 holdout: deterministic scoring now penalizes sports/team-player tails, geographic/gully-region tails, abstract inquiry tails, and alias-label drift more aggressively, while causative core verbs are demoted less harshly; review prep also now blocks auto-accept when rerank introduces a new suspicious tail flag even if total suspicious-flag count stays flat. On a fresh rebuild of the same 100-word Stage 1.5 corpus, this cleaned up several known misses (`direct` dropped alias `calculate.v.05`, `field` promoted `field.v.03`, `draw` dropped `draw.n.01`/`draw.v.09`, `move` demoted the game-turn noun tail) and shifted the deterministic risk mix from `61/24/15` to `59/26/15` (`rerank_and_review_candidate` / `rerank_recommended` / `deterministic_only`). The real rerank/review rerun then improved the queue from `50` review-required / `35` auto-accepted to `46` / `39`; among the 15 high-polysemy trouble words, `direct`, `house`, `left`, and `pass` moved out of review, while `field`, `draw`, `move`, `near`, `still`, and `turn` remain useful tuning targets. Stage 2 stays on hold, but the burden is lower and the next selector pass can focus on the remaining stubborn patterns instead of the already-fixed ones. | Codex | `/Users/johnson/AI/src/words-v2/.venv-lexicon/bin/python -m pytest tools/lexicon/tests` (`136` passed), targeted selector/rerank regressions for `field`, `draw`, and suspicious-tail-introduction (pass), local rebuild via `xargs /Users/johnson/AI/src/words-v2/.venv-lexicon/bin/python -m tools.lexicon.cli build-base --output-dir /tmp/lexicon-stage15b-20260309/snapshot < /tmp/lexicon-stage15-20260309/words.txt` (`100` lexemes / `475` senses / `475` concepts), risk rerun via `/Users/johnson/AI/src/words-v2/.venv-lexicon/bin/python -m tools.lexicon.cli score-selection-risk --snapshot-dir /tmp/lexicon-stage15b-20260309/snapshot --output /tmp/lexicon-stage15b-20260309/snapshot/selection_decisions.jsonl` (`59` review-candidate / `26` rerank-recommended / `15` deterministic-only), real rerank review via `LEXICON_LLM_BASE_URL=https://api.nwai.cc`, `LEXICON_LLM_MODEL=gpt-5.1`, `LEXICON_LLM_TRANSPORT=node`, and `/Users/johnson/AI/src/words-v2/.venv-lexicon/bin/python -m tools.lexicon.cli prepare-review --snapshot-dir /tmp/lexicon-stage15b-20260309/snapshot --decisions /tmp/lexicon-stage15b-20260309/snapshot/selection_decisions.jsonl --output /tmp/lexicon-stage15b-20260309/snapshot/selection_decisions.reviewed.jsonl --review-queue-output /tmp/lexicon-stage15b-20260309/snapshot/review_queue.jsonl --provider-mode openai_compatible_node --model gpt-5.1 --reasoning-effort low --candidate-limit 8 --candidate-source candidates` (`85` reranked / `46` review-required / `39` auto-accepted), plus before/after comparison against `/tmp/lexicon-stage15-20260309/snapshot/selection_decisions.reviewed.jsonl`. |
| 2026-03-09 | Applied a second Stage 1.5 rerank-hardening pass focused on the remaining review queue: `rerank` now exposes `canonical_label` to the model, explicitly instructs exact-label preference and mixed-POS preservation, and reorders `candidates` mode toward safer exact-label non-tail senses before alias/tail candidates. On the same 100-word Stage 1.5 snapshot, the real rerank/review rerun improved the queue from `46` review-required / `39` auto-accepted to `45` / `40`, while materially reducing alias-driven regressions (`label_drift_introduced` fell from `12` in the original tuned run to `5` here). This pass also shifted several words out of review (`around`, `care`, `check`, `early`, `fair`, `late`, `light`, `mean`, `space`, `world`) but surfaced some new high-frequency-only review cases (`day`, `free`, `left`, `match`, `order`, `scale`, `watch`), so the next step should likely separate rerank-label/tail prevention from high-frequency auto-accept policy rather than widening rerank freedom further. | Codex | `/Users/johnson/AI/src/words-v2/.venv-lexicon/bin/python -m pytest tools/lexicon/tests` (`139` passed), new rerank prompt/candidate regressions in `tools/lexicon/tests/test_rerank.py` (pass), and real rerank review via `LEXICON_LLM_BASE_URL=https://api.nwai.cc`, `LEXICON_LLM_MODEL=gpt-5.1`, `LEXICON_LLM_TRANSPORT=node`, and `/Users/johnson/AI/src/words-v2/.venv-lexicon/bin/python -m tools.lexicon.cli prepare-review --snapshot-dir /tmp/lexicon-stage15b-20260309/snapshot --decisions /tmp/lexicon-stage15b-20260309/snapshot/selection_decisions.jsonl --output /tmp/lexicon-stage15c-20260309.selection_decisions.reviewed.jsonl --review-queue-output /tmp/lexicon-stage15c-20260309.review_queue.jsonl --provider-mode openai_compatible_node --model gpt-5.1 --reasoning-effort low --candidate-limit 8 --candidate-source candidates` (`85` reranked / `45` review-required / `40` auto-accepted), compared against `/tmp/lexicon-stage15b-20260309/snapshot/selection_decisions.reviewed.jsonl` with `/tmp/compare_stage15_review_runs.py`. |
| 2026-03-09 | Applied a third Stage 1.5 hardening pass that combined deterministic cleanup with a very narrow high-frequency auto-accept exception. Deterministic scoring now boosts exact-label high-frequency adverbs, demotes biographical/eponym noun glosses, and treats distillation-style noun tails as specialized; review prep now auto-accepts only a mixed-POS, exact-label, near-cutoff high-frequency reshuffle when the top two senses are preserved and no new flags are introduced. On a fresh rebuild of the same 100-word Stage 1.5 corpus, the real review queue improved from `45` review-required / `40` auto-accepted to `41` / `44`. Concrete deterministic wins on the rebuilt snapshot include `still.r.01` being selected, `low.n.02` (the person-name sense) disappearing, and `back.r.01` / `back.r.02` surfacing into the selected set; the remaining hardest deterministic words are now mainly `near` and `field`, while the remaining review-policy burden concentrates in a smaller set of high-frequency reshuffles. | Codex | `/Users/johnson/AI/src/words-v2/.venv-lexicon/bin/python -m pytest tools/lexicon/tests` (`142` passed), fresh rebuild via `xargs /Users/johnson/AI/src/words-v2/.venv-lexicon/bin/python -m tools.lexicon.cli build-base --output-dir /tmp/lexicon-stage15d-20260309/snapshot < /tmp/lexicon-stage15-20260309/words.txt` (`100` lexemes / `471` senses / `471` concepts), risk rerun via `/Users/johnson/AI/src/words-v2/.venv-lexicon/bin/python -m tools.lexicon.cli score-selection-risk --snapshot-dir /tmp/lexicon-stage15d-20260309/snapshot --output /tmp/lexicon-stage15d-20260309/snapshot/selection_decisions.jsonl` (`59` review-candidate / `26` rerank-recommended / `15` deterministic-only), and real rerank review via `LEXICON_LLM_BASE_URL=https://api.nwai.cc`, `LEXICON_LLM_MODEL=gpt-5.1`, `LEXICON_LLM_TRANSPORT=node`, and `/Users/johnson/AI/src/words-v2/.venv-lexicon/bin/python -m tools.lexicon.cli prepare-review --snapshot-dir /tmp/lexicon-stage15d-20260309/snapshot --decisions /tmp/lexicon-stage15d-20260309/snapshot/selection_decisions.jsonl --output /tmp/lexicon-stage15d-20260309/snapshot/selection_decisions.reviewed.jsonl --review-queue-output /tmp/lexicon-stage15d-20260309/snapshot/review_queue.jsonl --provider-mode openai_compatible_node --model gpt-5.1 --reasoning-effort low --candidate-limit 8 --candidate-source candidates` (`85` reranked / `41` review-required / `44` auto-accepted), compared against `/tmp/lexicon-stage15c-20260309.selection_decisions.reviewed.jsonl` with `/tmp/compare_stage15_review_runs_d.py`. |
| 2026-03-09 | Applied a fourth Stage 1.5 hardening pass that keeps a conservative stance but reduces review pain: `prepare_review` now auto-accepts only exact-label, no-flag, same-POS high-frequency reshuffles when a six-sense deterministic set keeps four of the six senses and the new senses were already near the deterministic cutoff. The latest real rerank run on `/tmp/lexicon-stage15e-20260309/snapshot` nudged the queue from `41` / `44` to `40` / `45`; replaying that saved rerank artifact through the tightened policy then reduced the queue further to `34` review-required / `51` auto-accepted, specifically removing `change`, `check`, `line`, `order`, `time`, and `watch` from human review without any new LLM calls. This leaves Stage 2 still best treated as staged rather than fully unattended, but the auto-accepted-first path is now much more practical. | Codex | `/Users/johnson/AI/src/words-v2/.venv-lexicon/bin/python -m pytest tools/lexicon/tests` (`145` passed), real stage15e artifacts at `/tmp/lexicon-stage15e-20260309/snapshot/selection_decisions.reviewed.jsonl` and `/tmp/lexicon-stage15e-20260309/snapshot/review_queue.jsonl` (`40` review-required / `45` auto-accepted across `85` reranked lexemes), and no-network policy replay via `prepare_review` patched against `/tmp/lexicon-stage15e-20260309/snapshot/sense_reranks.review.jsonl` producing `/tmp/lexicon-stage15e-20260309/snapshot/selection_decisions.reviewed.policy-replay.jsonl` plus `/tmp/lexicon-stage15e-20260309/snapshot/review_queue.policy-replay.jsonl` (`34` review-required / `51` auto-accepted). |
| 2026-03-09 | Executed the Stage 2 auto-accepted-first rollout into reset `vocabapp_dev` from the current worktree. A `1,000`-word top-frequency WordNet-backed seed produced a real review gate of `399` review-required / `313` auto-accepted / `288` deterministic-only, yielding `601` importable lexemes. Those accepted decisions were materialized into a rerank-aware published snapshot, enriched in per-word mode (`601` completed after one validator hardening fix), compiled cleanly, then filtered down to `585` imported words by removing `16` obvious short-form abbreviation/proper-name contaminants such as `it -> information technology`, `me -> Maine`, and `us -> United States`. The dev DB is now populated and verified from Postgres plus container-local app health, but Stage 2 also surfaced a concrete next fix: seed-list generation should exclude or quarantine these short-form contaminants before enrichment so we do not spend LLM calls on them. | Codex | `/Users/johnson/AI/src/words-v2/.venv-lexicon/bin/python -m pytest tools/lexicon/tests` (`146` passed), stage2 base build + risk on `/tmp/lexicon-stage2-20260309/snapshot` (`1000` lexemes / `3893` senses; `712` rerank-needed with `382` review-candidate and `330` rerank-recommended), real rerank review on `/tmp/lexicon-stage2-20260309/snapshot/selection_decisions.reviewed.jsonl` (`399` review-required / `313` auto-accepted / `288` deterministic-only), materialized publish subset at `/tmp/lexicon-stage2-20260309/published-autoaccepted` (`601` lexemes / `2189` senses / `2062` concepts), real word enrichment to `word_enrichments.jsonl` (`601` completed), compiled export validation on `words.enriched.jsonl` (pass), filtered import file `/tmp/lexicon-stage2-20260309/published-autoaccepted/words.enriched.filtered.jsonl` (`585` kept / `16` removed), real import summary (`585` words / `2111` meanings / `3595` examples / `17370` relations / `585` enrichment jobs / `2111` enrichment runs), Postgres verification via `docker compose -f docker-compose.yml exec -T postgres psql ...` (same counts), and backend health via `docker compose -f docker-compose.yml exec -T backend python ...` (`/api/health` => `status=ok`, `database=ok`, `redis=ok`). |
| 2026-03-09 | Added first-class seed hardening for staged lexicon rollouts. The new `generate-seed-words` CLI emits a reproducible WordNet-backed seed file plus optional rejected-candidate JSONL with explicit reason codes, filtering the known short-form contamination bucket before `build-base` and enrichment. A real `1,000`-word seed-generation run accepted `1000` lemmas and rejected `265`, and the previously observed short-form junk set (`it`, `an`, `me`, `who`, `us`, `mr`, `oh`, `de`, `co`, `tv`, `al`, `ok`, `uk`, `ii`, `hi`, plus one-letter fragments) was absent from the accepted output. | Codex | Focused verification via `/Users/johnson/AI/src/words-v2/.venv-lexicon/bin/python -m pytest tools/lexicon/tests/test_seed_words.py tools/lexicon/tests/test_cli.py -q` (`38` passed), real seed run via `/Users/johnson/AI/src/words-v2/.venv-lexicon/bin/python -m tools.lexicon.cli generate-seed-words --count 1000 --scan-limit 12000 --output /tmp/lexicon-stage3-seed-20260309/words.txt --rejected-output /tmp/lexicon-stage3-seed-20260309/rejected.jsonl` (`1000` accepted / `265` rejected), and accepted-output presence checks returning `False` for `it`, `an`, `me`, `who`, `us`, `mr`, `oh`, `de`, `co`, `tv`, `al`, `ok`, `uk`, `ii`, `hi`, and one-letter fragments. |
| 2026-03-09 | Hardened lexicon review preparation for larger staged runs. `prepare-review` now supports bounded rerank previews via `--max-lemmas`, resume semantics via `--resume-rerank`, and rerank checkpoint persistence/progress reporting so operators no longer need to wait for a single opaque end-written rerank pass before estimating Stage 3 review burden. No new review endpoints were required because the existing backend lexicon review API already covers the minimal import/list/items/update/publish-preview/publish path for MVP staged review. | Codex | Focused verification via `/Users/johnson/AI/src/words-v2/.venv-lexicon/bin/python -m pytest tools/lexicon/tests/test_selection_review.py tools/lexicon/tests/test_cli.py tools/lexicon/tests/test_rerank.py -q` (`57` passed), with new coverage for bounded `prepare_review()` rerank scope, CLI flag forwarding, rerank checkpoint writes, and rerank resume skipping checkpointed lemmas. |
| 2026-03-09 | Added the first in-app lexicon admin portal at `/admin/lexicon` so operators can review staged lexicon batches/items, save review decisions, preview/publish approved staged rows, import staged review JSONL into the review tables, and inspect learner-facing word enrichment already imported into the local DB. Frontend auth protection now covers `/admin/*`, navigation links to the portal, and the operator docs now make the import semantics explicit: `import-db` imports the compiled JSONL it is given and does not automatically exclude review-needed rows. | Codex | `cd frontend && npm test -- --runInBand src/lib/__tests__/lexicon-reviews-client.test.ts src/lib/__tests__/words-client.test.ts src/app/admin/lexicon/__tests__/page.test.tsx src/app/__tests__/layout-auth-nav.test.tsx src/app/__tests__/page.test.tsx src/app/review/__tests__/page.test.tsx src/lib/__tests__/api-client.auth-lifecycle.test.ts` (`7` suites / `36` tests passed), `cd frontend && npm run lint` (pass), docs updated in `tools/lexicon/OPERATOR_GUIDE.md` and `docs/plans/2026-03-09-lexicon-admin-portal-implementation.md`. |
| 2026-03-09 | No change to lexicon rollout status. Consolidated the lexicon tool testing/evaluation evidence into a single document covering automated milestones, benchmark/rerank comparisons, real Stage 1.5/2/3 runs, low-poly review-burden results, conclusions, and the release-readiness split between internal/admin alpha vs broader unattended rollout; also refreshed the future-improvements TODO list to reflect what is now implemented versus the next hardening priorities. | Codex | Documentation-only update in `docs/plans/2026-03-09-lexicon-testing-and-evaluation-summary.md` and `docs/plans/2026-03-08-lexicon-future-improvements-todo.md`; evidence sources consolidated from `docs/status/project-status.md`, `tools/lexicon/OPERATOR_GUIDE.md`, and saved real-run artifacts under `/tmp/lexicon-stage15e-20260309`, `/tmp/lexicon-stage2-20260309`, `/tmp/lexicon-stage3-20260309`, `/tmp/lexicon-stage3b-20260309`, and `/tmp/lexicon-stage3-lowpoly-20260309`. |
| 2026-03-09 | Documentation correction only: the consolidated lexicon evaluation summary now also captures the real provider/model benchmark evidence, and the live lexicon status/operator docs now distinguish the historical learner-app `/admin/lexicon` MVP from the current split-in-progress `admin-frontend` implementation. This is a docs/status alignment pass only; runtime verification for the split admin app is still pending. | Codex | Fresh lexicon-suite verification via `/Users/johnson/AI/src/words-v2/.venv-lexicon/bin/python -m pytest tools/lexicon/tests -q` (`154` passed), plus docs/status updates in `docs/plans/2026-03-09-lexicon-testing-and-evaluation-summary.md`, `docs/plans/2026-03-08-lexicon-future-improvements-todo.md`, `tools/lexicon/OPERATOR_GUIDE.md`, and `docs/status/project-status.md`. |
| 2026-03-09 | Completed the dedicated admin-frontend integration slice for lexicon operations. The admin portal now runs as its own Next app on port `3001`, CI verifies both learner and admin frontends with lint/test/build jobs, compose wiring includes the separate admin service plus backend CORS coverage for `http://admin-frontend:3001`, and the local compose stack was runtime-smoked successfully (`/api/health` returned OK and `http://localhost:3001/login` returned `200`). Historical learner-app `/admin/lexicon` references remain only as evidence of the earlier MVP. | Codex | `npm --prefix admin-frontend run lint` (pass), `npm --prefix admin-frontend test -- --runInBand` (`7` suites / `26` tests passed), `NEXT_PUBLIC_API_URL=http://backend:8000/api npm --prefix admin-frontend run build` (pass), `npm --prefix frontend run lint` (pass), `npm --prefix frontend test -- --runInBand` (`9` suites / `36` tests passed), `NEXT_PUBLIC_API_URL=http://backend:8000/api npm --prefix frontend run build` (pass), `ruby -e 'require "yaml"; YAML.load_file(".github/workflows/ci.yml"); puts "ci.yml OK"'` (pass), `docker compose -f docker-compose.yml config` (pass), and local runtime smoke via `docker compose -f docker-compose.yml up -d --build postgres redis backend admin-frontend && docker compose -f docker-compose.yml exec -T backend alembic upgrade head` with `curl http://localhost:8000/api/health` (`status=ok`) and `curl -I http://localhost:3001/login` (`HTTP/1.1 200 OK`). |
| 2026-03-08 | Clarified the pre-prod governance split: the existing GitHub Actions `preprod-readiness` workflow is now documented as a disposable rehearsal against an ephemeral Docker stack, while a separate real pre-prod verification path is defined for the actual deployed environment and persistent populated DB. The runbooks now explicitly call out where lexicon-specific bounded smoke imports belong in real pre-prod verification rather than in ordinary PR CI or disposable rehearsal runs. | Codex | Workflow wording updated in `.github/workflows/preprod-readiness.yml`, checklist clarified in `docs/runbooks/preprod-readiness-checklist.md`, real-environment runbook added at `docs/runbooks/real-preprod-verification.md`, and release-promotion links updated in `docs/runbooks/release-promotion.md`. |
| 2026-03-09 | Added the first admin-specific Playwright smoke for the split operator app. The smoke suite now boots `admin-frontend` alongside the learner app, verifies unauthenticated `/lexicon` redirects to `/login?next=%2Flexicon`, verifies real login reaches the admin dashboard and lexicon shell, and keeps the same smoke command path for local and CI runs. The client-side auth-failure redirect helper was also hardened so `next` is preserved even when the fallback redirect path is triggered after hydration. | Codex | red-green verification via `npm --prefix admin-frontend test -- --runInBand admin-frontend/src/lib/__tests__/auth-redirect.test.ts` (first failed, then passed), targeted admin smoke via `docker compose -f docker-compose.yml --profile tests up -d --build postgres redis backend worker frontend admin-frontend playwright` plus `npx playwright test tests/smoke/admin-auth.smoke.spec.ts --project=chromium` (`2` passed), full local smoke via `E2E_SMOKE_CLEANUP=1 ./e2e/scripts/run-local-smoke.sh` (`9` smoke tests passed), plus fresh admin app regression checks: `npm --prefix admin-frontend run lint` (pass), `npm --prefix admin-frontend test -- --runInBand` (`8` suites / `28` tests passed), `NEXT_PUBLIC_API_URL=http://backend:8000/api npm --prefix admin-frontend run build` (pass), `ruby -e 'require "yaml"; YAML.load_file(".github/workflows/ci.yml"); puts "ci.yml OK"'` (pass), and `docker compose -f docker-compose.yml config` (pass). |
| 2026-03-09 | Expanded the admin Playwright smoke from shell/auth checks into a real staged-review interaction. The admin smoke suite now uploads a minimal `selection_decisions.jsonl` batch through the real UI, approves the imported item, saves the review decision, and verifies persisted review state via the review-items API; the full smoke pack remains green with the admin app included in the test stack. | Codex | targeted review-flow smoke via `docker compose -f docker-compose.yml --profile tests up -d --build postgres redis backend worker frontend admin-frontend playwright` plus `npx playwright test tests/smoke/admin-review-flow.smoke.spec.ts --project=chromium` (`1` passed), full local smoke via `E2E_SMOKE_CLEANUP=1 ./e2e/scripts/run-local-smoke.sh` (`10` smoke tests passed), and the existing admin redirect-helper regression remains green in `admin-frontend/src/lib/__tests__/auth-redirect.test.ts`. |
| 2026-03-09 | Expanded the preprod readiness gate so the workflow now starts `admin-frontend`, waits for learner + admin frontend readiness, passes `E2E_ADMIN_URL` into the Playwright smoke container, and aligns the preprod checklist wording with the dual-frontend stack. | Codex | Local no-network verification recorded in this change set: workflow YAML parse, `docker compose -f docker-compose.yml --profile tests config --services`, and targeted `rg` assertions against the updated workflow/runbook. |
| 2026-03-09 | Completed the admin hardening slice for the split lexicon operator app. Lexicon-review APIs now require admin role, the admin frontend adds a client-side auth guard as defense in depth, the staged-review smoke now covers import -> approve -> publish-preview -> publish, and the preprod readiness workflow now boots/tests `admin-frontend` with the same dual-frontend URLs used locally. | Codex | `docker compose -f docker-compose.yml exec -T backend pip install --no-cache-dir -r requirements-test.txt` (pass), `docker compose -f docker-compose.yml exec -T backend pytest tests/test_lexicon_reviews_api.py` (`21` passed), `npm --prefix admin-frontend test -- --runInBand` (`8` suites / `28` tests passed), `npm --prefix admin-frontend run lint` (pass), `NEXT_PUBLIC_API_URL=http://backend:8000/api npm --prefix admin-frontend run build` (pass), targeted admin smoke via Playwright container (`admin-auth` + `admin-review-flow`, pass), full local smoke via `E2E_SMOKE_CLEANUP=1 NEXT_PUBLIC_API_URL=http://backend:8000/api ALLOWED_ORIGINS=http://localhost:3000,http://localhost:3001,http://frontend:3000,http://admin-frontend:3001 ./e2e/scripts/run-local-smoke.sh` (`10` passed), `docker compose -f docker-compose.yml --profile tests config` (pass), and `ruby -e 'require "yaml"; YAML.load_file(".github/workflows/ci.yml"); YAML.load_file(".github/workflows/preprod-readiness.yml"); puts "workflow yaml OK"'` (pass). |
| 2026-03-10 | Completed the lexicon per-word enrichment and review-usability slice. The lexicon CLI now supports `enrich --mode per_word --max-concurrency N` for one LLM call per lexeme with bounded parallelism, `compile-export --decisions ... --decision-filter mode_c_safe` for direct safe-only exports after review prep, and the admin review API/UI now expose reviewer-readable selected-sense/source/candidate detail instead of forcing raw metadata inspection. Added small `.dockerignore` files for app build contexts so local smoke builds do not drag repo-local `node_modules` into Docker images. | Codex | `../../.venv-lexicon/bin/python -m pytest tools/lexicon/tests -q` (`130` passed), `PYTHONPATH=backend ../../.venv-backend/bin/python -m pytest backend/tests/test_lexicon_reviews_api.py -q` (`21` passed), `npm --prefix admin-frontend test -- --runInBand` (`8` suites / `28` tests passed), `npm --prefix admin-frontend run lint` (pass), `NEXT_PUBLIC_API_URL=http://backend:8000/api npm --prefix admin-frontend run build` (pass), `ruby -e 'require "yaml"; YAML.load_file(".github/workflows/ci.yml"); puts "ci.yml OK"'` (pass), `docker compose -f docker-compose.yml --profile tests config` (pass), and targeted admin smoke via clean local Docker stack plus `docker compose -f docker-compose.yml exec -T backend alembic upgrade head` and `docker compose -f docker-compose.yml exec -T playwright sh -lc "cd /workspace/e2e && E2E_BASE_URL=http://frontend:3000 E2E_API_URL=http://backend:8000/api E2E_ADMIN_URL=http://admin-frontend:3001 npm exec playwright test tests/smoke/admin-review-flow.smoke.spec.ts --project=chromium"` (`1` passed). |
| 2026-03-10 | Completed a small post-merge hardening follow-up for the lexicon review/export slice. Local `main` was safely reconciled by preserving the pre-sync dirty checkout on `backup/main-dirty-20260310` plus `/tmp/words-v2-dirty-main-backup.patch`, then the code path was hardened so `compile-export` coerces boolean-like decision values safely, rejects `--decisions` without `--decision-filter`, and the review API/publish-preview paths preserve compatibility with legacy staged candidate metadata that only had `label` / `gloss`. | Codex | `../../.venv-lexicon/bin/python -m pytest tools/lexicon/tests/test_compile_export.py tools/lexicon/tests/test_cli.py -q` (`41` passed), `PYTHONPATH=backend ../../.venv-backend/bin/python -m pytest backend/tests/test_lexicon_reviews_api.py -q` (`23` passed), and `../../.venv-lexicon/bin/python -m pytest tools/lexicon/tests -q` (`132` passed). |
| 2026-03-10 | Added two stable local-development test users for manual QA and admin/frontend testing: `admin@admin.com` / `12345678` (admin role) and `user@user.com` / `12345678` (user role). The backend now supports explicit dev-only seeding behind `DEV_TEST_USERS_ENABLED`, the local Docker backend enables it by default, and seeding happens lazily on the first request so a fresh pre-migration DB does not break backend startup. | Codex | `PYTHONPATH=backend ../../.venv-backend/bin/python -m pytest backend/tests/test_config.py backend/tests/test_dev_test_users.py backend/tests/test_auth.py -q` (`22` passed), plus live Docker verification on this branch: `POST /api/auth/login` and `GET /api/auth/me` both passed for `admin@admin.com` and `user@user.com` with expected roles. |
| 2026-03-10 | Finalized the salvaged admin lexicon review usability slice as a fresh integrated PR branch from current `main`. The admin review API now normalizes legacy and richer staged candidate metadata into stable reviewer-facing `candidate_entries` without losing selected override senses, the split admin UI renders clearer selected-source/candidate detail, and the targeted admin smoke now drives the real UI for save + preview + publish instead of depending on a transient confirmation toast. | Codex | `PYTHONPYCACHEPREFIX=/tmp/pycache-admin-review PYTHONPATH=backend ../../.venv-backend/bin/python -m pytest backend/tests/test_lexicon_reviews_api.py -q` (`25` passed), `npm --prefix admin-frontend test -- --runInBand src/app/lexicon/__tests__/page.test.tsx` (`7` passed), `npm --prefix admin-frontend test -- --runInBand` (`8` suites / `28` tests passed), `npm --prefix admin-frontend run lint` (pass), `NEXT_PUBLIC_API_URL=http://backend:8000/api npm --prefix admin-frontend run build` (pass), and targeted Docker smoke via `docker compose -f docker-compose.yml --profile tests up -d --build postgres redis backend worker frontend admin-frontend playwright`, `docker compose -f docker-compose.yml exec -T backend alembic upgrade head`, and `docker compose -f docker-compose.yml exec -T playwright sh -lc "cd /workspace/e2e && E2E_BASE_URL=http://frontend:3000 E2E_API_URL=http://backend:8000/api E2E_ADMIN_URL=http://admin-frontend:3001 npm exec playwright test tests/smoke/admin-review-flow.smoke.spec.ts --project=chromium"` (`1` passed). |
