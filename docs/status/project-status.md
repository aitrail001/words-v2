# Project Status Board (Source of Truth)

**Status:** ACTIVE  
**Last Updated (UTC):** 2026-03-23
**Owner:** Engineering  
**Scope:** Live delivery status for features, quality gates, and release readiness.

---

## Rules of Use

1. This file is the only live status source of truth.
2. Any status change must include fresh evidence in this file (tests, workflow run, or commit/PR).
3. Update this board in the same PR/commit as the implementation change whenever possible.
4. If no status changed, add a short timestamped "No Change" entry in `Status Change Log`.
5. Keep detailed implementation narratives in `docs/plans/*`; keep this board concise and evidence-linked.

---

## Consolidated Source Inputs

This board consolidates progress from:

- `docs/plans/2026-02-26-full-rebuild.md` (target scope roadmap)
- `docs/plans/2026-03-05-current-state-phase-plan.md` (evidence-based implementation state)
- `docs/runbooks/preprod-readiness-checklist.md` (operational pre-prod gate)
- `docs/runbooks/release-promotion.md` (promotion sequence and commands)
- `docs/runbooks/rollback.md` (rollback procedure)
- `.github/workflows/ci.yml`, `.github/workflows/preprod-readiness.yml`, `.github/workflows/deploy-preprod.yml`, `.github/workflows/promote-prod.yml` (delivery gates)

---

## Workstream Matrix

| Workstream | Status | Target Scope | Current Reality | Evidence | Next Milestone |
|---|---|---|---|---|---|
| Foundation platform | DONE | Docker stack, health checks, CI baseline | In place and stable | `docker-compose.yml`, `.github/workflows/ci.yml`, `backend/app/api/health.py` | Maintain |
| Auth + core vocabulary | PARTIAL | Register/login/refresh/me/logout, protected routes, lookup fallback | Register/login/me/refresh/logout implemented with refresh rotation + access-token revocation; frontend protected routes + logout UX + 401 lifecycle handling are in place; dictionary lookup fallback still pending | `backend/app/api/auth.py`, `backend/app/services/auth_tokens.py`, `frontend/src/lib/api-client.ts`, `frontend/src/middleware.ts`, `e2e/tests/smoke/auth-contract.smoke.spec.ts`, `e2e/tests/smoke/auth-guard.smoke.spec.ts` | Implement dictionary API fallback for `/api/words/lookup` misses and add coverage |
| Word list + ePub import | DONE | Word-list domain + import jobs + progress channel | Import pipeline hardened for real runtime: worker no longer hard-fails when `en_core_web_sm` is missing (fallback NLP path), backend/worker now share upload storage path, and full E2E now asserts terminal `completed` status with a valid EPUB fixture | `backend/app/tasks/epub_processing.py`, `backend/app/core/uploads.py`, `backend/app/api/word_lists.py`, `backend/app/api/imports.py`, `backend/tests/test_epub_processing.py`, `e2e/tests/full/import-terminal.full.spec.ts`, `e2e/tests/helpers/import-jobs.ts`, `e2e/tests/fixtures/epub/valid-minimal.epub` | Add object-storage upload path + temp-resource lifecycle cleanup (success/failure/TTL) with automated coverage |
| Review + SM-2 queue | PARTIAL | Queue add/due/submit/stats + full integration | Queue API/service/frontend implemented; broader roadmap depth still pending | `backend/app/api/reviews.py`, `backend/app/services/review.py`, `frontend/src/app/review/page.tsx` | Close remaining roadmap gaps and harden flows |
| E2E + CI quality gates | DONE (baseline) | Required smoke gate on PR + broader suite | Smoke required on PR; auth contract + protected-route smoke coverage added; full suite runs on main/dispatch | `.github/workflows/ci.yml`, `e2e/tests/smoke/*`, `e2e/tests/full/*` | Keep smoke minimal and non-flaky |
| Pre-prod readiness gate | DONE | Disposable rehearsal: rollback drill + smoke + observability validation across backend + learner/admin frontends | Disposable rehearsal gate automated with learner + admin frontend coverage; persistent preprod verification is separately documented and required before production promotion | `.github/workflows/preprod-readiness.yml`, `docs/runbooks/preprod-readiness-checklist.md`, `docs/runbooks/real-preprod-verification.md` | Keep rehearsal green on release tags and require real preprod verification evidence before promote |
| Promotion automation wiring | DEFERRED | Real preprod deploy + prod promote via workflows | Workflows implemented; real infra command/URL wiring deferred to beta release | `.github/workflows/deploy-preprod.yml`, `.github/workflows/promote-prod.yml` | Wire real commands/URLs and run tagged dry-run |
| Concept learning (synsets, R/U/L) | PENDING | Phase 4 concepts/mastery system | Not started | `docs/plans/2026-02-26-full-rebuild.md` | Design + implement phase slice |
| Lexicon admin enrichment rollout | PARTIAL | Learner-first offline lexicon builder produces staged common-word entries plus learner-facing enrichment for local DB import | The words slice now supports shared entry metadata (`entry_id`, `entry_type`, `normalized_form`, `source_provenance`), a `wordfreq` top-words inventory path via `build-base --top-words` / `--rollout-stage`, deterministic pre-build canonicalization that collapses only morphology-backed variants into canonical headwords while keeping semantic neighbors separate and preserving learner-worthy related forms as linked standalone entries, snapshot-side registry/status artifacts plus `lookup-entry` / `status-entry` operator commands, learner-translation generation for `zh-Hans`, `es`, `ar`, `pt-BR`, and `ja`, an optional bounded ambiguous-form adjudication path via `detect-ambiguous-forms`, `adjudicate-forms`, and `build-base --adjudications`, direct per-word enrichment writes to `words.enriched.jsonl` plus checkpoint/decision/failure sidecars for resumable 1K -> 5K -> 30K runs, and a safer ambiguous-tail operator gate that defers unresolved variant-form tails from base outputs until adjudication while exposing pending state in lookup/status and validation. The phrase slice now has reviewed-CSV phrase inventory ingestion, a bounded `senses[]` phrase enrichment schema, realtime phrase enrichment into compiled artifacts, phrase-aware compile/review/import persistence, and admin review panels that surface structured phrase details instead of leaving phrase payloads only in raw JSON. Realtime enrichment now uses lower configurable retry budgets via `--transient-retries` and `--validation-retries`, phrase translation `usage_note` validation is conditional on whether the source phrase note exists, operators get shared lexicon CLI runtime logging via `--log-level` / `--log-file` across the broader tool surface with per-command progress plus per-lexeme retry/failure tracing in `enrich`, and the active toolchain no longer exposes legacy `per_sense` mode or treats `enrichments.jsonl` as a supported realtime artifact path. Successful per-word realtime completions now append immediately to `words.enriched.jsonl`, `enrich.checkpoint.jsonl`, and `enrich.decisions.jsonl` even after earlier failures, while `enrich.failures.jsonl` is preserved as append-only failure history across resumes and checkpoint remains the only skip ledger for completed lexemes. The batch lexicon slice is now wired through submit/status/ingest/retry/QC commands over JSONL ledgers, with deterministic request/result lineage, append-only ingest for accepted rows, a separate failure sidecar, retry attempt bumping, and review-queue generation for failed rows. The admin tool is now structured around the current compiled-artifact workflow instead of the older staged-selection flow: `/lexicon/ops` serves as the snapshot-first hub, `/lexicon/compiled-review` remains the DB-backed review staging path, `/lexicon/jsonl-review` remains the file-backed path, `/lexicon/import-db` exposes dry-run and explicit final DB import for approved compiled artifacts, `/lexicon/db-inspector` gives post-import verification against the live DB, and the old selection-review page has been demoted to `/lexicon/legacy`. A separate compiled-review admin tool now sits between compiled JSONL export and DB import: admins can import immutable compiled artifacts, inspect item-level validator/QC metadata in `/lexicon/compiled-review`, persist approve/reject/reopen decisions plus regeneration requests in dedicated `lexicon_artifact_review_*` tables, export canonical decision overlays for `review-materialize`, and export approved/rejected/regenerate JSONL directly from the backend, with Playwright smoke now covering compiled review, JSONL review, and the new Ops -> Import DB -> DB Inspector loop. The 30K rollout path now uses tracked irregular-form datasets for audited noun/plural, compound irregular plural, and irregular verb misses; curated surface-form drops for bounded inventory noise like `childrens`, `womens`, `dont`, `atleast`, `bl`, `seperate`, `longterm`, and `lyin`; anomaly rows for lexicalized plural false positives such as `clothes`, `goods`, `spirits`, `arms`, `quarters`, and `regards`; explicit entity-category data for non-general rows (`name`, `place`, `brand`, `entity_other`); bounded tail exclusions for concrete bad admissions without widening canonical heuristics; and distinct-derived variant metadata for words like `building`, `charming`, and `ceiling`, driven by tracked data first with bounded inference fallback. The current per-word enrichment prompt is now both entity-category-aware and distinct-variant-aware, and has been further hardened to compress high-frequency function-word sense sprawl while preserving important closed-class grammar words and preferring base-word linkage for kept inflected special cases. The current exact deterministic 30K snapshot is `data/lexicon/snapshots/words-30000-20260314-main-real-entity-tail-hardened` with `requested_top_words=39485`, `30000` lexemes, `63126` senses, `56507` concepts, `0` unresolved ambiguous rows, `0` snapshot validation errors, and audit-side explicit entity counts `name=36`, `place=11`, `brand=5`, `entity_other=4`. Lexicon-owned persisted data now lives in the dedicated Postgres `lexicon` schema while runtime/app tables stay separate in the same database; translated example/usage-note fields remain artifact-only in this slice. The structured-output schema for the enrichment path has now been hardened to the gateway's strict requirements, and a completed fixed-schema `curated_100` live matrix shows `word_only` clearly outperforming `grounded` operationally. `gpt-5-nano word_only` remains the recommended 30K default (`399` valid rows, `0` failed lexemes, `4` repairs, `21.965s` average latency), with `gpt-5.1 word_only` as the conservative fallback (`398` valid rows, `0` failed lexemes, `3` repairs, `23.816s` average latency). A follow-up grouped-request benchmark on `curated_16` was then used to test batched enrichment and was rejected as a rollout path: `g4` and `g8` runs failed badly under 60-second transport timeouts, and the only non-failing grouped case with plausible speed (`gpt-5.1 g2`) was too small and fragile to justify the extra operational complexity or larger failure blast radius. The rollout contract has now been switched to the approved `wordfreq + LLM` direction for the 30K path: `build-base --top-words` snapshots are truly WordNet-free, `word_only` enrichment now validates a word-level decision schema (`discard`, `keep_standard`, `keep_derived_special`) instead of LLM-selected `sense_id` rows, compile/export synthesizes internal meaning IDs after validation so DB import compatibility stays intact, and the tail-exclusion dataset now drops out-of-scope admissions such as `a`, `an`, and single-letter alphabet rows before enrichment. The final bounded live pilot at `data/lexicon/snapshots/words-100-20260316-wordfreq-only-live-pilot-v4-final/snapshot` finished with `93` completed lexemes, `93` explicit decision rows, `368` enrichment rows, `89` compiled words, and `0` failures; compared with the earlier `v2` pilot, compiled sense density dropped from mean `5.295` / median `7` / max `8` to mean `4.135` / median `5` / max `5` while preserving the intended boundary behavior for tested edge cases (`is/are/has` discard, `their/its/our` keep, `said` derived-special in focused recheck). The tool is now operationally ready for the resumable 30K run, with the main residual risk shifted from durability to normal live-model semantic variance on borderline form-family words such as `could`. | `tools/lexicon/models.py`, `tools/lexicon/build_base.py`, `tools/lexicon/batch_prepare.py`, `tools/lexicon/batch_ingest.py`, `tools/lexicon/canonical_forms.py`, `tools/lexicon/data/canonical_anomalies.json`, `tools/lexicon/data/distinct_variant_entries.json`, `tools/lexicon/data/entity_categories.json`, `tools/lexicon/data/irregular_form_overrides.json`, `tools/lexicon/data/irregular_verb_forms.json`, `tools/lexicon/data/surface_form_overrides.json`, `tools/lexicon/data/tail_exclusions.json`, `tools/lexicon/policy_data.py`, `tools/lexicon/canonical_registry.py`, `tools/lexicon/form_adjudication.py`, `tools/lexicon/phrase_inventory.py`, `tools/lexicon/phrase_pipeline.py`, `tools/lexicon/cli.py`, `tools/lexicon/review_materialize.py`, `tools/lexicon/enrich.py`, `tools/lexicon/enrichment_benchmark.py`, `tools/lexicon/import_db.py`, `tools/lexicon/compile_export.py`, `tools/lexicon/runtime_logging.py`, `tools/lexicon/validate.py`, `tools/lexicon/schemas/phrase_enrichment_schema.py`, `tools/lexicon/README.md`, `tools/lexicon/OPERATOR_GUIDE.md`, `backend/app/api/lexicon_compiled_reviews.py`, `backend/app/api/lexicon_imports.py`, `backend/app/api/lexicon_jsonl_reviews.py`, `backend/app/models/lexicon_artifact_review_batch.py`, `backend/app/models/lexicon_artifact_review_item.py`, `backend/app/models/lexicon_artifact_review_item_event.py`, `backend/app/models/lexicon_regeneration_request.py`, `backend/app/models/phrase_entry.py`, `backend/app/models/word.py`, `backend/app/models/meaning.py`, `backend/app/models/translation.py`, `backend/alembic/versions/010_move_lexicon_tables_to_lexicon_schema.py`, `backend/alembic/versions/012_add_compiled_review_tables.py`, `backend/alembic/versions/013_expand_phrase_entries_for_enrichment.py`, `admin-frontend/src/app/lexicon/ops/page.tsx`, `admin-frontend/src/app/lexicon/compiled-review/page.tsx`, `admin-frontend/src/app/lexicon/jsonl-review/page.tsx`, `admin-frontend/src/app/lexicon/import-db/page.tsx`, `admin-frontend/src/app/lexicon/db-inspector/page.tsx`, `admin-frontend/src/app/lexicon/legacy/page.tsx`, `admin-frontend/src/lib/lexicon-compiled-reviews-client.ts`, `admin-frontend/src/lib/lexicon-imports-client.ts`, `docs/plans/2026-03-13-lexicon-robustness-5k-benchmark-report.md`, `docs/plans/2026-03-14-real-30k-curation-and-variant-enrichment-report.md`, `docs/plans/2026-03-14-lexicon-30k-exhaustive-semantic-audit-notes.md`, `docs/plans/2026-03-14-lexicon-distinct-variant-prompt-hardening-design.md`, `docs/plans/2026-03-14-lexicon-distinct-variant-prompt-hardening-plan.md`, `docs/plans/2026-03-15-lexicon-prompt-benchmark-report.md`, `docs/plans/2026-03-16-lexicon-batched-enrichment-benchmark-design.md`, `docs/plans/2026-03-16-lexicon-batched-enrichment-benchmark-plan.md`, `docs/plans/2026-03-16-lexicon-batched-enrichment-benchmark-report.md`, `docs/plans/2026-03-16-lexicon-wordfreq-only-30k-design.md`, `docs/plans/2026-03-16-lexicon-wordfreq-only-30k-plan.md`, `docs/plans/2026-03-21-lexicon-admin-workflow-cleanup-design.md`, `docs/plans/2026-03-21-lexicon-admin-workflow-cleanup-implementation-plan.md`, `docs/plans/2026-03-22-phrase-enrichment-design.md`, `docs/plans/2026-03-22-phrase-enrichment-implementation-plan.md`, `docs/plans/2026-03-23-lexicon-retry-logging-and-phrase-validation-design.md`, `docs/plans/2026-03-23-lexicon-retry-logging-and-phrase-validation-implementation-plan.md`, `docs/plans/2026-03-23-lexicon-resume-flush-and-failure-history-design.md`, `docs/plans/2026-03-23-lexicon-resume-flush-and-failure-history-implementation-plan.md`, `docs/plans/2026-03-23-lexicon-remove-per-sense-legacy-design.md`, `docs/plans/2026-03-23-lexicon-remove-per-sense-legacy-implementation-plan.md`, `e2e/tests/smoke/admin-compiled-review-flow.smoke.spec.ts`, `e2e/tests/smoke/admin-jsonl-review-flow.smoke.spec.ts`, `e2e/tests/smoke/admin-lexicon-ops-import-flow.smoke.spec.ts` | Decide whether to add richer server-side pagination/filtering if compiled artifacts outgrow the current admin review surface |
| AI/media/listening/stories/admin | PENDING | Phases 5-9 product expansion | Not started | `docs/plans/2026-02-26-full-rebuild.md` | Sequence after beta core readiness |

---

## Current Top Gaps (Priority Order)

1. Implement dictionary API fallback for `/api/words/lookup` misses + tests.
2. Move import source storage from local/container temp paths to object storage (or equivalent shared ephemeral layer) with guaranteed cleanup on completion/failure and periodic TTL cleanup.
3. Beta-release activation: wire real deploy/promote variables and pass full tagged promotion drill.
4. Concept learning (synsets, R/U/L) phase design and first implementation slice.

---

## Release Readiness Snapshot

| Gate | Required | Current | Evidence |
|---|---|---|---|
| Backend lint + tests | Yes | Green | `CI / Backend (lint + test)` |
| Frontend lint + tests | Yes | Green | `CI / Frontend (lint + test)` |
| E2E smoke on PR | Yes | Green | `CI / E2E Smoke (required)` |
| Preprod readiness rehearsal workflow | Yes (for release) | Available with learner + admin frontend coverage | `.github/workflows/preprod-readiness.yml` |
| Deploy preprod workflow | Yes (for release) | Available (placeholder vars) | `.github/workflows/deploy-preprod.yml` |
| Production promote workflow | Yes (for release) | Available (placeholder vars) | `.github/workflows/promote-prod.yml` |
| Rollback runbook | Yes | Ready | `docs/runbooks/rollback.md` |

---

## Required Update Checklist (Every Significant Change)

1. Update relevant workstream row(s) in this board.
2. Add or refresh evidence link(s) (test command, workflow run, PR/commit).
3. Re-check release-readiness table if CI/workflows/runbooks changed.
4. Append one line in `Status Change Log`.

Suggested verification commands before marking a row as improved:

```bash
# Backend
cd backend && pytest -q

# Frontend
cd ../frontend && npm run lint && npm test -- --runInBand --watch=false

# CI workflow syntax (local sanity)
ruby -e 'require "yaml"; YAML.load_file(".github/workflows/ci.yml"); puts "ci.yml OK"'
```

---

## Status Change Log

| 2026-03-23 | Fixed the realtime per-word lexicon durability gap exposed by the live phrase snapshot. Successful lexemes now append immediately to `words.enriched.jsonl`, `enrich.checkpoint.jsonl`, and `enrich.decisions.jsonl` even after earlier failures, so active runs no longer stall canonical artifact growth behind the first failed lexeme. `enrich.failures.jsonl` is now append-only history across `--resume`, while checkpoint remains the only skip ledger for completed lexemes. | Codex | `/Users/johnson/AI/src/words-v2/.venv-lexicon/bin/python -m pytest tools/lexicon/tests/test_enrich.py tools/lexicon/tests/test_cli.py -q` (`132` passed), `/Users/johnson/AI/src/words-v2/.venv-lexicon/bin/python -m pytest tools/lexicon/tests -q` (`352` passed), `/Users/johnson/AI/src/words-v2/.venv-lexicon/bin/python -m py_compile tools/lexicon/enrich.py tools/lexicon/tests/test_enrich.py tools/lexicon/cli.py` (pass). |

| 2026-03-23 | Hardened lexicon operator visibility and retry control across the CLI. Realtime enrichment now exposes lower configurable retry budgets, phrase translated `usage_note` validation only requires locale notes when the source phrase note exists, and the broader lexicon CLI now supports shared `--log-level` / `--log-file` runtime logging with command-level progress plus detailed per-lexeme retry/lifecycle tracing in `enrich`. | Codex | `/Users/johnson/AI/src/words-v2/.venv-lexicon/bin/python -m pytest tools/lexicon/tests/test_cli.py tools/lexicon/tests/test_enrich.py tools/lexicon/tests/test_runtime_logging.py -q` (`131` passed), reviewer confirmation for the enrich logging slice (`Task 5 looks clear`), and docs alignment via `rg -n "retry|logging|phrase" docs/status/project-status.md docs/plans/2026-03-23-lexicon-retry-logging-and-phrase-validation-design.md docs/plans/2026-03-23-lexicon-retry-logging-and-phrase-validation-implementation-plan.md` (pass). |

| 2026-03-23 | Verified the phrase-enrichment rollout against the live realtime path and the local admin smoke stack. A live phrase smoke using the configured realtime `LEXICON_LLM_*` env now succeeds end to end for reviewed CSV phrase inputs, producing valid compiled phrase rows in `words.enriched.jsonl`. During that run, a real-data inventory bug was fixed by mapping reviewed CSV confidence labels (`low`/`medium`/`high`) to bounded floats, and a live phrase contract gap was fixed by tightening phrase example difficulty to CEFR enums plus phrase-side translation example alignment repair. Full backend/admin suites are green, and the local admin smoke flows for Compiled Review, JSONL Review, and Ops -> Import DB -> DB Inspector also pass once `E2E_DB_PASSWORD` is aligned with the local Docker `.env` password. | Codex | `/Users/johnson/AI/src/words-v2/.venv-lexicon/bin/python -m pytest tools/lexicon/tests -q` (`325` passed), `PYTHONPATH=backend /Users/johnson/AI/src/words-v2/.venv-backend/bin/python -m pytest backend/tests -q` (`204` passed), `NODE_PATH=/Users/johnson/AI/src/words-v2/admin-frontend/node_modules PATH=/Users/johnson/AI/src/words-v2/admin-frontend/node_modules/.bin:$PATH npm --prefix admin-frontend test -- --runInBand` (`15` suites / `45` tests passed), live realtime phrase smoke via `set -a && source tools/lexicon/.env.local && set +a && ... build-phrases ... && ... enrich --provider-mode openai_compatible --model \"$LEXICON_LLM_MODEL\" --reasoning-effort low --mode per_word && ... validate --compiled-input ...` (pass; `lexeme_count=2`, `enrichment_count=2`, `error_count=0`, smoke dir `/tmp/lexicon-phrase-live-smoke-NXLH1y`), `docker compose -f docker-compose.yml exec -T backend alembic upgrade head` (pass), and `docker compose -f docker-compose.yml exec -T playwright sh -lc "cd /workspace/e2e && E2E_BASE_URL=http://frontend:3000 E2E_API_URL=http://backend:8000/api E2E_ADMIN_URL=http://admin-frontend:3001 E2E_DB_PASSWORD=change_this_password_in_production ./node_modules/.bin/playwright test tests/smoke/admin-compiled-review-flow.smoke.spec.ts tests/smoke/admin-jsonl-review-flow.smoke.spec.ts tests/smoke/admin-lexicon-ops-import-flow.smoke.spec.ts --project=chromium"` (`3` passed). |

| 2026-03-23 | Executed the approved phrase-enrichment implementation through the backend/admin review slice. The lexicon toolchain now supports reviewed-CSV phrase inventory input, bounded phrase `senses[]` enrichment, phrase-aware realtime compile/import persistence, and structured phrase detail panels in both `/lexicon/compiled-review` and `/lexicon/jsonl-review`, so phrase rows are reviewable without falling back to raw JSON inspection. | Codex | `/Users/johnson/AI/src/words-v2/.venv-backend/bin/python -m pytest backend/tests/test_lexicon_compiled_reviews_api.py backend/tests/test_lexicon_jsonl_reviews_api.py backend/tests/test_lexicon_ops_api.py -q` (`34` passed), `NODE_PATH=/Users/johnson/AI/src/words-v2/admin-frontend/node_modules PATH=/Users/johnson/AI/src/words-v2/admin-frontend/node_modules/.bin:$PATH npm --prefix admin-frontend test -- --runInBand src/app/lexicon/compiled-review/__tests__/page.test.tsx src/app/lexicon/jsonl-review/__tests__/page.test.tsx src/app/lexicon/import-db/__tests__/page.test.tsx` (`3` suites / `8` tests passed), `/Users/johnson/AI/src/words-v2/.venv-lexicon/bin/python -m pytest tools/lexicon/tests/test_enrich.py tools/lexicon/tests/test_validate.py tools/lexicon/tests/test_unified_enrichment_flow.py -q` (`88` passed), `/Users/johnson/AI/src/words-v2/.venv-lexicon/bin/python -m pytest tools/lexicon/tests/test_compile_export.py tools/lexicon/tests/test_review_materialize.py tools/lexicon/tests/test_review_prep.py -q` (`19` passed), `PYTHONPATH=backend /Users/johnson/AI/src/words-v2/.venv-backend/bin/python -m pytest backend/tests/test_lexicon_phrase_reference_models.py -q` (`6` passed), and `/Users/johnson/AI/src/words-v2/.venv-lexicon/bin/python -m pytest tools/lexicon/tests/test_import_db.py -q` (`8` passed). |

| 2026-03-22 | Removed the remaining sense-era and staged-selection operator surface from the active lexicon flow. `build-base` no longer emits `senses.jsonl` or `concepts.jsonl`; the supported CLI/admin workflow is now lexeme-only plus compiled-review/import; `/api/lexicon-ops` and `/lexicon/ops` no longer advertise selection-decision or sense-based artifacts; `/lexicon` and `/lexicon/legacy` now redirect to the current ops-first workflow; and the Docker frontend/admin containers now hard-pin browser-side `NEXT_PUBLIC_API_URL=/api` so live admin E2E uses the same same-origin proxy contract as local browser use. | Codex | `/Users/johnson/AI/src/words-v2/.venv-lexicon/bin/python -m pytest tools/lexicon/tests -q` (`307` passed), `PYTHONPATH=backend /Users/johnson/AI/src/words-v2/.venv-backend/bin/python -m pytest backend/tests -q` (`204` passed), `cd /Users/johnson/AI/src/words-v2/.worktrees/feat_lexicon_remove_sense_era_20260322/admin-frontend && ./node_modules/.bin/jest --runInBand src/app/lexicon/ops/__tests__/page.test.tsx src/app/lexicon/__tests__/page.test.tsx src/app/lexicon/legacy/__tests__/page.test.tsx src/app/__tests__/layout-auth-nav.test.tsx` (`15` tests passed), `docker compose -f /Users/johnson/AI/src/words-v2/.worktrees/feat_lexicon_remove_sense_era_20260322/docker-compose.yml exec -T playwright sh -lc "cd /workspace/e2e && E2E_BASE_URL=http://frontend:3000 E2E_API_URL=http://backend:8000/api E2E_ADMIN_URL=http://admin-frontend:3001 ./node_modules/.bin/playwright test tests/smoke/admin-lexicon-ops-import-flow.smoke.spec.ts tests/smoke/admin-jsonl-review-flow.smoke.spec.ts --project=chromium"` (`2` passed). |

| 2026-03-21 | Streamlined the lexicon admin workflow around the current compiled-artifact path. `Lexicon Ops` now acts as the snapshot-first hub and deep-links into both standalone review modes, the admin tool now includes a first-class final `Import DB` page/panel plus a standalone `DB Inspector`, and the outdated staged `selection_decisions.jsonl` review flow has been demoted under `/lexicon/legacy` instead of competing with the compiled-review path. Backend now exposes admin-only import dry-run/run APIs that wrap the real `tools.lexicon.import_db` logic, the Docker dev/test stack mounts `tools/` so those imports work in-container, and the smoke suite now covers the full Ops -> Import DB -> DB Inspector loop alongside the existing compiled-review and JSONL-review flows. | Codex | `PYTHONPATH=backend /Users/johnson/AI/src/words-v2/.venv-backend/bin/python -m pytest backend/tests/test_lexicon_imports_api.py backend/tests/test_lexicon_ops_api.py backend/tests/test_lexicon_jsonl_reviews_api.py backend/tests/test_lexicon_compiled_reviews_api.py -q` (`24` passed), `npm --prefix admin-frontend test -- --runInBand src/app/lexicon/import-db/__tests__/page.test.tsx src/app/lexicon/ops/__tests__/page.test.tsx src/app/lexicon/db-inspector/__tests__/page.test.tsx src/app/lexicon/legacy/__tests__/page.test.tsx src/app/lexicon/compiled-review/__tests__/page.test.tsx src/app/lexicon/jsonl-review/__tests__/page.test.tsx src/app/__tests__/layout-auth-nav.test.tsx src/app/__tests__/page.test.tsx` (`8` suites / `19` tests passed), `npm --prefix admin-frontend run lint` (pass), `NEXT_PUBLIC_API_URL=http://backend:8000/api npm --prefix admin-frontend run build` (pass), `PYTHONPATH=backend /Users/johnson/AI/src/words-v2/.venv-backend/bin/python -m py_compile backend/app/api/lexicon_imports.py backend/app/api/lexicon_ops.py backend/app/main.py backend/tests/test_lexicon_imports_api.py` (pass), and Docker smoke via `docker compose -f docker-compose.yml exec -T backend alembic upgrade head` plus `docker compose -f docker-compose.yml exec -T playwright sh -lc "cd /workspace/e2e && E2E_DB_PASSWORD=devpassword E2E_BASE_URL=http://frontend:3000 E2E_API_URL=http://backend:8000/api E2E_ADMIN_URL=http://admin-frontend:3001 npm exec playwright test tests/smoke/admin-compiled-review-flow.smoke.spec.ts tests/smoke/admin-jsonl-review-flow.smoke.spec.ts tests/smoke/admin-lexicon-ops-import-flow.smoke.spec.ts --project=chromium"` (`3` passed). |
| 2026-03-21 | Finished the backend schema/import slice for phrase and reference families. New lexicon-scoped ORM models and an Alembic migration now create `phrase_entries`, `reference_entries`, and `reference_localizations` in the `lexicon` schema, `import-db` can ingest compiled phrase/reference rows and update localization rows, and the dry-run counters continue to report all three families. The remaining plan work is now downstream integration, not missing backend schema. | Codex | `/Users/johnson/AI/src/words-v2/.venv-backend/bin/python -m pytest backend/tests/test_models.py backend/tests/test_lexicon_enrichment_models.py backend/tests/test_lexicon_review_models.py backend/tests/test_lexicon_phrase_reference_models.py -q` (`34` passed), `/Users/johnson/AI/src/words-v2/.venv-lexicon/bin/python -m pytest tools/lexicon/tests/test_import_db.py tools/lexicon/tests/test_compile_export.py tools/lexicon/tests/test_validate.py -q` (`28` passed), `/Users/johnson/AI/src/words-v2/.venv-lexicon/bin/python -m pytest tools/lexicon/tests -q` (`300` passed), `/Users/johnson/AI/src/words-v2/.venv-lexicon/bin/python -m py_compile backend/app/models/phrase_entry.py backend/app/models/reference_entry.py backend/app/models/reference_localization.py backend/alembic/versions/011_add_phrase_and_reference_entries.py tools/lexicon/import_db.py tools/lexicon/tests/test_import_db.py backend/tests/test_lexicon_phrase_reference_models.py` (pass). |
| 2026-03-21 | Added the separate JSONL-only lexicon review flow without changing the existing DB-backed compiled-review implementation. Backend now exposes admin-only file-backed endpoints at `/api/lexicon-jsonl-reviews` for loading repo-local compiled artifacts, merging and persisting `review.decisions.jsonl` sidecars, and materializing approved/rejected/regenerate outputs through `review-materialize`; the admin frontend adds `/lexicon/jsonl-review` plus a dedicated nav entry for single-operator load/search/filter/approve/reject/reopen/materialize work. The existing `/api/lexicon-compiled-reviews` and `/lexicon/compiled-review` paths remain intact and are now re-verified in live Docker smoke beside the new route. | Codex | `PYTHONPATH=backend /Users/johnson/AI/src/words-v2/.venv-backend/bin/python -m pytest backend/tests/test_lexicon_compiled_reviews_api.py backend/tests/test_lexicon_jsonl_reviews_api.py -q` (`11` passed), `npm --prefix admin-frontend test -- --runInBand src/lib/__tests__/lexicon-compiled-reviews-client.test.ts src/lib/__tests__/lexicon-jsonl-reviews-client.test.ts src/app/lexicon/compiled-review/__tests__/page.test.tsx src/app/lexicon/jsonl-review/__tests__/page.test.tsx` (`4` suites / `8` tests passed), `PYTHONPATH=backend /Users/johnson/AI/src/words-v2/.venv-backend/bin/python -m py_compile backend/app/api/lexicon_jsonl_reviews.py backend/app/services/lexicon_jsonl_reviews.py backend/app/main.py` (pass), `npm --prefix admin-frontend run lint` (pass), `NEXT_PUBLIC_API_URL=http://backend:8000/api npm --prefix admin-frontend run build` (pass), and Docker smoke via `docker compose -f docker-compose.yml exec -T backend alembic upgrade head` plus `docker compose -f docker-compose.yml exec -T playwright sh -lc "cd /workspace/e2e && E2E_BASE_URL=http://frontend:3000 E2E_API_URL=http://backend:8000/api E2E_ADMIN_URL=http://admin-frontend:3001 npm exec playwright test tests/smoke/admin-compiled-review-flow.smoke.spec.ts tests/smoke/admin-jsonl-review-flow.smoke.spec.ts --project=chromium"` (`2` passed). |
| 2026-03-21 | Mounted the repo `data/` tree into the backend and worker containers so the JSONL-only review UI can resolve snapshot artifacts using container-visible paths like `/app/data/lexicon/snapshots/...` instead of rejecting host-only macOS paths. This keeps the review service path guard intact while making the existing snapshot artifacts accessible inside the Docker stack. | Codex | `docker compose -f docker-compose.yml config` (pass), `docker compose -f docker-compose.yml up -d --force-recreate backend worker` (pass), and backend-side path resolution check against `/app/data/lexicon/snapshots/words-40000-20260316-main-wordfreq-live-target30k/words.enriched.jsonl` (pass). |
| 2026-03-21 | Refined the JSONL-only review UI for wide-monitor use: the page now uses a wider canvas, a stronger hero/header, sticky item navigation, inline path guidance, and a split detail/raw-JSON layout so it behaves more like an operator console than a plain form. The page still uses the same JSONL-only backend contract, but the layout is now more readable and responsive on large screens. | Codex | `npm --prefix admin-frontend test -- --runInBand src/app/lexicon/jsonl-review/__tests__/page.test.tsx` (`1` passed), `npm --prefix admin-frontend run lint` (pass), `NEXT_PUBLIC_API_URL=http://backend:8000/api npm --prefix admin-frontend run build` (pass). |
| 2026-03-21 | Tightened the JSONL-only review detail pane after a layout collision report: the raw compiled JSON inspector now sits below the item metadata/actions instead of competing for horizontal space, which removes overlap on wide monitors while preserving the sticky item list. The page remains file-backed and uses the same JSONL-only backend contract. | Codex | `npm --prefix admin-frontend test -- --runInBand src/app/lexicon/jsonl-review/__tests__/page.test.tsx` (`1` passed), `npm --prefix admin-frontend run lint` (pass), `NEXT_PUBLIC_API_URL=http://backend:8000/api npm --prefix admin-frontend run build` (pass). |
| 2026-03-21 | Added the first triage-review slice to the JSONL-only review flow. The backend now derives machine-visible warning labels and reviewer summary metadata for each compiled row, including warning count/priority, provenance sources, sense/form/confusable counts, and primary definition/example cues; the admin UI now sorts the queue risk-first, exposes warning chips inline, and shows a reviewer summary above the raw JSON so humans can approve easy rows quickly without reading the full payload first. JSONL artifacts and `review.decisions.jsonl` sidecars remain the only source of truth. | Codex | `PYTHONPATH=backend .venv-backend/bin/python -m pytest backend/tests/test_lexicon_jsonl_reviews_api.py -q` (`9` passed), `npm --prefix admin-frontend test -- --runInBand src/app/lexicon/jsonl-review/__tests__/page.test.tsx` (`1` passed), `npm --prefix admin-frontend run lint` (pass), `NEXT_PUBLIC_API_URL=http://backend:8000/api npm --prefix admin-frontend run build` (pass), `PYTHONPATH=backend .venv-backend/bin/python -m py_compile backend/app/services/lexicon_jsonl_reviews.py backend/app/api/lexicon_jsonl_reviews.py` (pass). |
| 2026-03-21 | Added reviewer-speed controls and browser verification to the JSONL-only triage flow. The admin page now supports keyboard triage (`j`/`k` move, `a` approve, `r` reject, `p` reopen) while still disabling shortcuts inside active text inputs for safety, and the JSONL-only Playwright smoke now covers risk-first summary rendering plus keyboard-driven approve/reject alongside materialization. | Codex | `npm --prefix admin-frontend test -- --runInBand src/app/lexicon/jsonl-review/__tests__/page.test.tsx` (`1` passed), `npm --prefix admin-frontend run lint` (pass), `NEXT_PUBLIC_API_URL=http://backend:8000/api npm --prefix admin-frontend run build` (pass), `docker compose -f docker-compose.yml --profile tests up -d --force-recreate backend worker frontend admin-frontend playwright` (pass), and `docker compose -f docker-compose.yml exec -T playwright sh -lc "cd /workspace/e2e && E2E_DB_PASSWORD=change_this_password_in_production E2E_BASE_URL=http://frontend:3000 E2E_API_URL=http://backend:8000/api E2E_ADMIN_URL=http://admin-frontend:3001 npm exec playwright test tests/smoke/admin-jsonl-review-flow.smoke.spec.ts --project=chromium"` (`1` passed). |
| 2026-03-21 | Added the operator-facing `review-apply` alias so the documented manual-override flow maps to an actual CLI command instead of only `batch-qc --overrides`. The batch runbook now matches the current code path, and the QC helper can reapply overrides onto persisted verdict rows before refreshing the review queue. | Codex | `/Users/johnson/AI/src/words-v2/.venv-lexicon/bin/python -m pytest tools/lexicon/tests/test_qc.py tools/lexicon/tests/test_batch_lifecycle.py tools/lexicon/tests/test_cli.py -q` (`53` passed), `/Users/johnson/AI/src/words-v2/.venv-lexicon/bin/python -m pytest tools/lexicon/tests -q` (`301` passed), `/Users/johnson/AI/src/words-v2/.venv-lexicon/bin/python -m py_compile tools/lexicon/qc.py tools/lexicon/cli.py tools/lexicon/tests/test_qc.py` (pass). |
| 2026-03-21 | Fixed the remaining PR blocker by making `import_db` lazily resolve backend phrase/reference models only when those entry families are actually present. This keeps the lexicon-only CI environment backend-free for word-only import tests while preserving backend model loading for real phrase/reference imports. | Codex | `/Users/johnson/AI/src/words-v2/.venv-lexicon/bin/python -m pytest tools/lexicon/tests/test_import_db.py tools/lexicon/tests/test_translations_pipeline.py tools/lexicon/tests/test_qc.py tools/lexicon/tests/test_batch_lifecycle.py -q` (`18` passed), `/Users/johnson/AI/src/words-v2/.venv-lexicon/bin/python -m pytest tools/lexicon/tests -q` (`301` passed), `/Users/johnson/AI/src/words-v2/.venv-lexicon/bin/python -m py_compile tools/lexicon/import_db.py` (pass). |

| 2026-03-20 | Added the first deterministic ingest/QC/override slice for batch-led lexicon runs. `batch-ingest` now appends out-of-order `custom_id`-matched results instead of overwriting ledgers, writes failures to a separate sidecar, and the QC/manual-override helpers can now stage review verdicts over the JSONL result ledger. | Codex | `/Users/johnson/AI/src/words-v2/.venv-lexicon/bin/python -m pytest tools/lexicon/tests/test_batch_ingest.py tools/lexicon/tests/test_qc.py tools/lexicon/tests/test_overrides.py tools/lexicon/tests/test_batch_lifecycle.py -q` (`9` passed), `/Users/johnson/AI/src/words-v2/.venv-lexicon/bin/python -m pytest tools/lexicon/tests/test_cli.py tools/lexicon/tests/test_batch_ledger.py tools/lexicon/tests/test_batch_prepare.py -q` (`51` passed), `/Users/johnson/AI/src/words-v2/.venv-lexicon/bin/python -m py_compile tools/lexicon/batch_ingest.py tools/lexicon/qc.py tools/lexicon/overrides.py tools/lexicon/cli.py tools/lexicon/tests/test_batch_ingest.py tools/lexicon/tests/test_qc.py tools/lexicon/tests/test_overrides.py` (pass). |
| 2026-03-20 | Added the thin mockable `BatchClient` seam with transport-injected `upload_batch_file`, `create_batch`, `get_batch`, and `download_file` methods. This gives the future submit/status flow a testable provider boundary without introducing live network behavior yet. | Codex | `/Users/johnson/AI/src/words-v2/.venv-lexicon/bin/python -m pytest tools/lexicon/tests/test_batch_client_mocked.py -q` (`1` passed), `/Users/johnson/AI/src/words-v2/.venv-lexicon/bin/python -m py_compile tools/lexicon/batch_client.py tools/lexicon/tests/test_batch_client_mocked.py` (pass). |

| 2026-03-20 | Completed the first runnable batch lifecycle slice. `batch-submit`, `batch-status`, `batch-ingest`, `batch-retry`, and `batch-qc` now operate over JSONL ledgers in snapshot directories, with deterministic custom-id lineage, retry attempt bumping, ingest summaries, QC verdict rows, and review-queue generation. The benchmark runner also now accepts both dict-shaped and list-shaped validated payloads during resume/retry tests, which closed the last red tests in `tools/lexicon/tests`. | Codex | `/Users/johnson/AI/src/words-v2/.venv-lexicon/bin/python -m pytest tools/lexicon/tests/test_batch_lifecycle.py tools/lexicon/tests/test_batch_prepare.py tools/lexicon/tests/test_batch_ledger.py tools/lexicon/tests/test_batch_client_mocked.py tools/lexicon/tests/test_contract_schemas.py -q` (`15` passed), `/Users/johnson/AI/src/words-v2/.venv-lexicon/bin/python -m pytest tools/lexicon/tests -q` (`289` passed), `/Users/johnson/AI/src/words-v2/.venv-lexicon/bin/python -m py_compile tools/lexicon/cli.py tools/lexicon/batch_ledger.py tools/lexicon/batch_prepare.py tools/lexicon/batch_ingest.py tools/lexicon/qc.py tools/lexicon/overrides.py tools/lexicon/enrichment_benchmark.py tools/lexicon/tests/test_batch_lifecycle.py` (pass). |

| 2026-03-20 | Added the first deterministic batch-prepare ledger slice. The CLI now includes `batch-prepare` plus explicit stubs for the later batch lifecycle commands, and the batch helper layer can build and persist stable request rows with round-trippable custom IDs for normalized entries. This gives the lexicon pipeline a reproducible request-file boundary for the later submit/status/ingest stages while the legacy word path stays intact. | Codex | `/Users/johnson/AI/src/words-v2/.venv-lexicon/bin/python -m pytest tools/lexicon/tests/test_batch_ledger.py tools/lexicon/tests/test_batch_prepare.py tools/lexicon/tests/test_phrase_pipeline.py tools/lexicon/tests/test_reference_pipeline.py tools/lexicon/tests/test_cli.py -q` (`55` passed), `/Users/johnson/AI/src/words-v2/.venv-lexicon/bin/python -m py_compile tools/lexicon/cli.py tools/lexicon/batch_prepare.py tools/lexicon/batch_ledger.py tools/lexicon/phrase_pipeline.py tools/lexicon/reference_pipeline.py tools/lexicon/inventory.py tools/lexicon/ids.py tools/lexicon/tests/test_cli.py tools/lexicon/tests/test_batch_ledger.py tools/lexicon/tests/test_batch_prepare.py tools/lexicon/tests/test_phrase_pipeline.py tools/lexicon/tests/test_reference_pipeline.py` (pass). |

| 2026-03-20 | Added the first Stage 2 batch-adjacent builders for learner phrases and lightweight reference entries. `phrase-build-base` and `reference-build-base` now emit deterministic JSONL snapshot artifacts from JSONL seed files, using dedicated phrase/reference pipelines plus stable entry IDs and normalized forms. The CLI help and command smoke tests were extended to cover these new operator entrypoints while existing lexicon behavior stayed green. | Codex | `/Users/johnson/AI/src/words-v2/.venv-lexicon/bin/python -m pytest tools/lexicon/tests/test_phrase_pipeline.py tools/lexicon/tests/test_reference_pipeline.py tools/lexicon/tests/test_cli.py -q` (`50` passed), `/Users/johnson/AI/src/words-v2/.venv-lexicon/bin/python -m pytest tools/lexicon/tests/test_enrich.py tools/lexicon/tests/test_validate.py tools/lexicon/tests/test_contract_schemas.py tools/lexicon/tests/test_normalization_preserves_existing_contract.py -q` (`80` passed), `/Users/johnson/AI/src/words-v2/.venv-lexicon/bin/python -m py_compile tools/lexicon/ids.py tools/lexicon/inventory.py tools/lexicon/phrase_pipeline.py tools/lexicon/reference_pipeline.py tools/lexicon/cli.py tools/lexicon/tests/test_cli.py tools/lexicon/tests/test_phrase_pipeline.py tools/lexicon/tests/test_reference_pipeline.py` (pass). |

| 2026-03-20 | Advanced the lexicon batch-enrichment work from Stage 0 into Stage 1 contract extraction. The shared translation-locale contract and the word enrichment schema builder/normalizer now live in dedicated modules, with new offline schema tests covering word, phrase, reference, QC, and compiled-export schema shapes plus a preservation test for the existing word enrichment normalization contract. Existing lexicon CLI, validation, and enrichment behavior stayed green under the repo venv. | Codex | `/Users/johnson/AI/src/words-v2/.venv-lexicon/bin/python -m pytest tools/lexicon/tests/test_contract_schemas.py tools/lexicon/tests/test_normalization_preserves_existing_contract.py -q` (`8` passed), `/Users/johnson/AI/src/words-v2/.venv-lexicon/bin/python -m pytest tools/lexicon/tests/test_enrich.py -q` (`64` passed), `/Users/johnson/AI/src/words-v2/.venv-lexicon/bin/python -m pytest tools/lexicon/tests/test_validate.py -q` (`8` passed), `/Users/johnson/AI/src/words-v2/.venv-lexicon/bin/python -m pytest tools/lexicon/tests/test_cli.py -q` (`44` passed), `/Users/johnson/AI/src/words-v2/.venv-lexicon/bin/python -m py_compile tools/lexicon/contracts.py tools/lexicon/enrich.py tools/lexicon/validate.py tools/lexicon/schemas/word_enrichment_schema.py tools/lexicon/schemas/phrase_enrichment_schema.py tools/lexicon/schemas/reference_entry_schema.py tools/lexicon/schemas/qc_verdict_schema.py tools/lexicon/schemas/compiled_export_schema.py tools/lexicon/tests/test_contract_schemas.py tools/lexicon/tests/test_normalization_preserves_existing_contract.py` (pass). |

| 2026-03-20 | Landed the Stage 0 lexicon batch-enrichment scaffold: new repo skills for batch contracts, schema guardrails, and test harnesses; placeholder batch/phrase/reference modules and schema files under `tools/lexicon`; an initial batch operator doc stub; and offline fixture/test coverage for reference seed packs plus scaffold discovery. Existing lexicon CLI and validation/enrichment behavior remained green under the repo venv. | Codex | `/Users/johnson/AI/src/words-v2/.venv-lexicon/bin/python -m pytest tests/lexicon -q` (`3` passed), `/Users/johnson/AI/src/words-v2/.venv-lexicon/bin/python -m pytest tools/lexicon/tests/test_cli.py -q` (`44` passed), `/Users/johnson/AI/src/words-v2/.venv-lexicon/bin/python -m pytest tools/lexicon/tests/test_validate.py -q` (`8` passed), `/Users/johnson/AI/src/words-v2/.venv-lexicon/bin/python -m pytest tools/lexicon/tests/test_enrich.py -q` (`64` passed). |

| 2026-03-16 | Closed the per-word discard audit gap on the new `wordfreq + LLM` rollout path. Per-word enrichment now persists explicit lexeme-level decisions in `enrich.decisions.jsonl` alongside `enrichments.jsonl`, `enrich.checkpoint.jsonl`, and `enrich.failures.jsonl`, and a fresh live `words-100-20260316-wordfreq-only-live-pilot-v2` run verified incremental disk flush, `0` failures, and explicit discard rows for `is`, `are`, `has`, `it's`, and `i'm`. The same live pilot also confirmed the remaining blocker before a 30K run is semantic selectivity rather than durability: the prompt still kept too many high-frequency function-word entries (`86 keep_standard`, `5 discard`, `2 keep_derived_special`). | Codex | `/Users/johnson/AI/src/words-v2/.venv-lexicon/bin/python -m pytest tools/lexicon/tests -q` (`261` passed); live pilot commands `build-base --rerun-existing --top-words 100 --output-dir data/lexicon/snapshots/words-100-20260316-wordfreq-only-live-pilot-v2/snapshot`, `enrich --snapshot-dir data/lexicon/snapshots/words-100-20260316-wordfreq-only-live-pilot-v2/snapshot --provider-mode auto --mode per_word --max-concurrency 4 --request-delay-seconds 1.0`, and `compile-export --snapshot-dir data/lexicon/snapshots/words-100-20260316-wordfreq-only-live-pilot-v2/snapshot --output data/lexicon/snapshots/words-100-20260316-wordfreq-only-live-pilot-v2/snapshot/words.enriched.jsonl` (artifacts: `93` checkpoint rows, `93` decision rows, `466` enrichment rows, `88` compiled words, `0` failures). |
| 2026-03-16 | Completed the final prompt-hardening pass for the `wordfreq + LLM` 30K rollout path. The per-word prompt now compresses ultra-common function-word inventories harder, merges micro-uses into broader learner senses, preserves important closed-class grammar words like `their` / `its` / `our`, discards plain auxiliary forms like `is` / `are` / `has`, discards plain contractions by default, and prefers `keep_derived_special` for retained inflected forms that survive only because of narrower special meanings. The final bounded live `words-100-20260316-wordfreq-only-live-pilot-v4-final` run finished cleanly and materially reduced sense sprawl without losing the important edge-case boundary. | Codex | `/Users/johnson/AI/src/words-v2/.venv-lexicon/bin/python -m pytest tools/lexicon/tests/test_enrich.py -q` (`64` passed), `/Users/johnson/AI/src/words-v2/.venv-lexicon/bin/python -m pytest tools/lexicon/tests -q` (`264` passed), live pilot commands `build-base --rerun-existing --top-words 100 --output-dir data/lexicon/snapshots/words-100-20260316-wordfreq-only-live-pilot-v4-final/snapshot`, `enrich --snapshot-dir data/lexicon/snapshots/words-100-20260316-wordfreq-only-live-pilot-v4-final/snapshot --provider-mode auto --mode per_word --max-concurrency 4 --request-delay-seconds 1.0`, and `compile-export --snapshot-dir data/lexicon/snapshots/words-100-20260316-wordfreq-only-live-pilot-v4-final/snapshot --output data/lexicon/snapshots/words-100-20260316-wordfreq-only-live-pilot-v4-final/snapshot/words.enriched.jsonl` (artifacts: `93` checkpoint rows, `93` decision rows, `368` enrichment rows, `89` compiled words, `0` failures; compiled sense mean `4.135`, median `5`, max `5`). Focused live boundary recheck at `data/lexicon/snapshots/boundary-20260316-final-recheck/snapshot` confirmed `is/are/has` discard, `their/its/our` keep, and `said` `keep_derived_special`. |
| 2026-03-16 | Switched the lexicon 30K rollout contract to the approved `wordfreq + LLM` direction for the actual operator path. `build-base --top-words` now emits `wordfreq-only` snapshots without WordNet grounding, per-word enrichment validates a decision-based word-level schema (`discard`, `keep_standard`, `keep_derived_special`) instead of requiring model-selected `sense_id` rows, compile/export synthesizes internal meaning IDs after validation to preserve DB import compatibility, and the tail-exclusion dataset now drops out-of-scope admissions such as `a`, `an`, and single-letter alphabet rows before enrichment. A local dry run proved the updated flow can build, enrich in `per_word` mode, and compile from a fresh snapshot under the repo artifact tree without live LLM calls. | Codex | `/Users/johnson/AI/src/words-v2/.venv-lexicon/bin/python -m pytest tools/lexicon/tests -q` (`257` passed); dry-run CLI evidence in `data/lexicon/snapshots/lexicon-wordfreq-only-dryrun/snapshot` from `build-base --top-words 5`, `enrich --provider-mode placeholder --mode per_word`, and `compile-export`. |
| 2026-03-14 | Hardened the lexicon prompt-preparation path for distinct derived variants ahead of the 30K LLM rollout. The deterministic base builder now carries explicit-plus-inferred metadata for words that look like derived forms but remain their own learner entries with standalone meanings, using a tracked dataset first (`building`, `ceiling`, `charming`) and bounded inference fallback for uncovered cases. Per-word enrichment prompts now distinguish these entries from ordinary words, briefly link them to their base words, and instruct the model to avoid duplicating ordinary base-word meanings while entity-category guidance for `name`, `place`, `brand`, and `entity_other` remains intact. | Codex | `/Users/johnson/AI/src/words-v2/.venv-lexicon/bin/python -m pytest tools/lexicon/tests/test_build_base.py tools/lexicon/tests/test_models.py tools/lexicon/tests/test_enrich.py -q` (`88` passed) and `/Users/johnson/AI/src/words-v2/.venv-lexicon/bin/python -m pytest tools/lexicon/tests -q` (`232` passed). |
| 2026-03-14 | Completed the deterministic entity-category and bounded tail-exclusion follow-up for the real 30K lexicon snapshot. The lexicon pipeline now carries explicit `entity_category` metadata on lexeme rows (`general`, `name`, `place`, `brand`, `entity_other`), uses tracked tail-exclusion data for concrete bad common-word admissions instead of broader canonical heuristics, and feeds entity-aware guidance into the later per-word enrichment prompt path. The exact deterministic 30K boundary is now `39485`, and the current hardened snapshot at `data/lexicon/snapshots/words-30000-20260314-main-real-entity-tail-hardened` lands at exactly `30000` lexemes, `63126` senses, `56507` concepts, and `0` ambiguous rows with clean snapshot validation. | Codex | `PYTHONPATH=. /Users/johnson/AI/src/words-v2/.venv-lexicon/bin/python -m pytest tools/lexicon/tests -q` (`227` passed), `wc -l data/lexicon/snapshots/words-30000-20260314-main-real-entity-tail-hardened/{lexemes,senses,concepts,ambiguous_forms}.jsonl` (`30000 / 63126 / 56507 / 0`), `validate_snapshot_files` on `data/lexicon/snapshots/words-30000-20260314-main-real-entity-tail-hardened` (`0` errors), direct boundary search showing `39484 -> 29999`, `39485 -> 30000`, `39487 -> 30001`, final `build-base --top-words 39485 --output-dir data/lexicon/snapshots/words-30000-20260314-main-real-entity-tail-hardened`, and audit summary generation for `data/lexicon/audits/words-30000-20260314-main-real-entity-tail-hardened.audit.summary.json` (`suspicious_count=553`, `linked_variant=1521`, `entity_category_counts={brand:5, entity_other:4, name:36, place:11}`). |
| 2026-03-15 | Ran a live lexicon enrichment prompt/model benchmark against the custom gateway to compare `word_only` versus the production-style grounded WordNet prompt on `gpt-5.1-chat`, `gpt-5.1`, and `gpt-5.4`, and then expanded the benchmark harness to support a tracked `curated_100` dataset with category metadata plus deterministic rubric summaries for broader future runs. On the tracked 4-word set (`right`, `break`, `building`, `kinshasa`), grounded mode was faster than `word_only` for both `gpt-5.1-chat` (`75.5s` vs `98.9s`) and `gpt-5.1` (`79.1s` vs `111.6s`), while `gpt-5.4` remained much slower (`~177-179s`) and required a higher timeout to complete. The repo now also carries a fixed 100-word benchmark set spanning common polysemy, noun/verb crossover, adjective/adverb ambiguity, distinct variants, entities, and harder tail words, with summary counters for dropped rows, variant-link note hits, entity-aware note hits, and suspicious generated forms. `gpt-5.1` remains the best quality/speed tradeoff for this gateway path, `gpt-5.1-chat` is the faster fallback, and the full live `3 x 2` 100-word matrix is now defined but documented as a multi-hour run rather than a quick local benchmark. | Codex | `/Users/johnson/AI/src/words-v2/.venv-lexicon/bin/python -m pytest tools/lexicon/tests/test_enrichment_benchmark.py -q` (`3` passed), `/Users/johnson/AI/src/words-v2/.venv-lexicon/bin/python -m pytest tools/lexicon/tests/test_cli.py tools/lexicon/tests/test_enrich.py tools/lexicon/tests/test_enrichment_benchmark.py -q` (`88` passed), `/Users/johnson/AI/src/words-v2/.venv-lexicon/bin/python -m pytest tools/lexicon/tests -q` (`232` passed), live artifacts under `/tmp/lexicon-prompt-benchmark-20260315-small4`, `/tmp/lexicon-prompt-benchmark-20260315-gpt51-small4`, and report `docs/plans/2026-03-15-lexicon-prompt-benchmark-report.md`. |
| 2026-03-16 | Closed the grouped-request lexicon enrichment experiment and rejected batching as a 30K rollout path. After fixing the fresh-worktree Node dependency issue with `npm --prefix tools/lexicon/node ci`, the live `curated_16` grouped benchmark showed that `gpt-5-nano g2` was much slower than the recommended single-word path (`38.599s` effective seconds per word), both `g4` runs failed half the lexemes under 60-second transport timeouts, `gpt-5-nano g8` failed all `16` lexemes, and the only plausible grouped result (`gpt-5.1 g2` at `22.602s` effective seconds per word) was too small and fragile to justify the higher failure blast radius or extra operational complexity. The rollout recommendation is therefore back to single-word requests only: `gpt-5-nano word_only` default, `gpt-5.1 word_only` fallback, no grouped `64` or `128` follow-up runs. | Codex | Live artifacts under `/tmp/lexicon-batched-benchmark-20260316/curated_16`, report `docs/plans/2026-03-16-lexicon-batched-enrichment-benchmark-report.md`, and branch cleanup that removed the local batching-only implementation changes from `tools/lexicon/*` before documenting the result. |
| 2026-03-16 | Completed the fixed-schema live `curated_100` lexicon enrichment matrix across `gpt-5.1-chat`, `gpt-5.1`, `gpt-5-mini`, and `gpt-5-nano`, each with `word_only` and grounded prompt modes. The key outcome is that schema hardening to match strict structured-output rules unlocked stable end-to-end runs and reversed the earlier partial conclusion: `word_only` now clearly outperforms grounded mode operationally on this gateway. All four `word_only` runs finished with `0` failed lexemes, with `gpt-5-nano word_only` emerging as the best 30K default (`399` valid rows, `4` repairs, `21.965s` avg/lexeme) and `gpt-5.1 word_only` as the conservative fallback (`398` valid rows, `3` repairs, `23.816s` avg/lexeme). Grounded mode remained slower and much more repair-heavy across all models, with real failed lexemes still present for `gpt-5.1-chat` (`right`) and `gpt-5.1` (`case`, `fine`). | Codex | `/Users/johnson/AI/src/words-v2/.venv-lexicon/bin/python -m pytest tools/lexicon/tests/test_enrich.py -q` (`52` passed), `/Users/johnson/AI/src/words-v2/.venv-lexicon/bin/python -m pytest tools/lexicon/tests -q` (`242` passed), live full-matrix artifact `/tmp/lexicon-prompt-benchmark-20260315-curated100-schemafixed/summary.json`, and report `docs/plans/2026-03-15-lexicon-prompt-benchmark-report.md`. |
| 2026-03-14 | Completed the deterministic real-30K hardening pass for irregular forms, compound irregular plurals, curated inventory-noise drops, and linked lexicalized variants. Canonicalization now collapses audited compound irregulars such as `grandchildren -> grandchild`, `schoolchildren -> schoolchild`, and multiple `-men` plurals, expands high-confidence irregular verb coverage (`got`, `heard`, `held`, `led`, `met`, `sold`, `won`, `began`, `kept`, `told`, `understood`, and others), filters bounded noise forms such as `childrens`, `womens`, `dont`, `atleast`, `bl`, `seperate`, `longterm`, and `lyin` before selection, and marks lexicalized irregular forms such as `fed`, `felt`, `lost`, `thought`, `broken`, `driven`, `taken`, and `worn` as linked standalone entries instead of plain self forms. The exact deterministic boundary shifted to `39424`, and the current boundary-hardened snapshot at `data/lexicon/snapshots/words-30000-20260314-main-real-boundary-hardened` lands at exactly `30000` lexemes, `63126` senses, `56497` concepts, and `0` ambiguous rows. Residual risk is now the low-rank tail quality and anomaly-style names/places rather than false irregular duplication. | Codex | `PYTHONPATH=. /Users/johnson/AI/src/words-v2/.venv-lexicon/bin/python -m pytest tools/lexicon/tests/test_build_base.py tools/lexicon/tests/test_canonical_forms.py tools/lexicon/tests/test_audit_30k_semantics.py -q` (`69` passed), `PYTHONPATH=. /Users/johnson/AI/src/words-v2/.venv-lexicon/bin/python -m pytest tools/lexicon/tests -q` (`221` passed), `wc -l data/lexicon/snapshots/words-30000-20260314-main-real-boundary-hardened/{lexemes,senses,concepts,ambiguous_forms}.jsonl` (`30000 / 63126 / 56497 / 0`), direct boundary search showing `39423 -> 29999`, `39424 -> 30000`, `39426 -> 30001`, final `build-base --top-words 39424 --output-dir data/lexicon/snapshots/words-30000-20260314-main-real-boundary-hardened`, and audit summary generation for `data/lexicon/audits/words-30000-20260314-main-real-boundary-hardened.audit.summary.json` (`suspicious_count=497`, `linked_variant=1520`). |
| 2026-03-14 | Completed the deterministic semantic-audit hardening pass for the real 30K lexicon snapshot. Canonicalization now collapses true possessive surface forms to their base lexeme while keeping genuine contractions separate, low-quality apostrophe fragments/abbreviations are normalized or filtered before selection via tracked data, rule-word lists live in tracked lexicon data files, and apostrophized proper-name/brand edge cases such as `levi's`, `o'brien`, and `o'connor` are now explicit anomaly/data decisions rather than incidental heuristic outcomes. The rebuilt snapshot boundary shifted to `requested_top_words=39273`, and the final dated snapshot at `data/lexicon/snapshots/words-30000-20260314-main-real` now lands exactly at `30000` lexemes, `63273` senses, and `0` ambiguous rows. The refreshed exhaustive audit inventory for that snapshot dropped the suspicious review set to `490` rows, with possessive-form duplicates eliminated and the remaining review tail concentrated in lexicalized plurals, productive derived forms, and a small apostrophe/abbreviation bucket. | Codex | `/Users/johnson/AI/src/words-v2/.venv-lexicon/bin/python -m pytest tools/lexicon/tests/test_build_base.py tools/lexicon/tests/test_canonical_forms.py tools/lexicon/tests/test_audit_30k_semantics.py -q` (`56` passed), `/Users/johnson/AI/src/words-v2/.venv-lexicon/bin/python -m pytest tools/lexicon/tests -q` (`208` passed), `PYTHONPATH=. /Users/johnson/AI/src/words-v2/.venv-lexicon/bin/python tools/lexicon/audit_30k_semantics.py` (final summary: `30000` lexemes / `490` suspicious), and the final `build-base --top-words 39273` snapshot output documented in `docs/plans/2026-03-14-real-30k-curation-and-variant-enrichment-report.md`. |
| 2026-03-14 | Added variant-aware interim lexeme metadata for separately learner-worthy forms linked to a base word, so later LLM enrichment can avoid duplicating the base-word meanings and instead generate only the distinct meanings plus a short “another form of X” note. Added a tracked canonical anomaly override layer for bounded non-generalizable tails such as rare name/place/brand-like suffix reductions and a handful of explicit regular plural exceptions, which removed the current 30K rollout’s unresolved ambiguous tail and corrected the observed `glasses -> glass` false positive without broadening the core suffix rules further. Using the deterministic build-base path with the override-enabled logic, the real dated 30K snapshot now lands exactly at `requested_top_words=38344`, producing `data/lexicon/snapshots/words-30000-20260314-main-real` with `30000` lexemes, `62976` senses, and `0` ambiguous forms. | Codex | `/Users/johnson/AI/src/words-v2/.venv-lexicon/bin/python -m pytest tools/lexicon/tests/test_build_base.py tools/lexicon/tests/test_models.py -q` (`35` passed), `/Users/johnson/AI/src/words-v2/.venv-lexicon/bin/python -m pytest tools/lexicon/tests/test_enrich.py -q` (`42` passed), `/Users/johnson/AI/src/words-v2/.venv-lexicon/bin/python -m pytest tools/lexicon/tests/test_canonical_forms.py -q` (`15` passed), `/Users/johnson/AI/src/words-v2/.venv-lexicon/bin/python -m pytest tools/lexicon/tests -q` (`199` passed), plus final snapshot row counts from `wc -l` showing `30000` lexemes / `62976` senses / `0` ambiguous rows for `data/lexicon/snapshots/words-30000-20260314-main-real`, documented in `docs/plans/2026-03-14-real-30k-curation-and-variant-enrichment-report.md`. |
| 2026-03-13 | Ran a mixed deterministic 5K lexicon robustness benchmark and completed one more generic suffix-hardening pass before reassessing 30K readiness. The latest hardening keeps lexicalized plural-like forms with their own meanings separate, removes unsupported non-plural chopped tails such as `something -> someth`, `anything -> anyth`, `everything -> everyth`, `during -> dur`, and `whether -> wheth`, and still preserves valid inflection behavior such as `added -> add`, `coming -> come`, and deferred ambiguity detection for `ringed -> ring`. The resulting real-provider 5K snapshot finished in `8.88s` with `4080` lexemes, `13862` senses, and only `8` unresolved ambiguous forms, which are now concentrated in bounded currency/name/place-like cases rather than broad deterministic drift. | Codex | `/Users/johnson/AI/src/words-v2/.venv-lexicon/bin/python -m pytest tools/lexicon/tests/test_form_adjudication.py tools/lexicon/tests/test_canonical_forms.py -q` (`18` passed), `/Users/johnson/AI/src/words-v2/.venv-lexicon/bin/python -m pytest tools/lexicon/tests -q` (`195` passed), and `/usr/bin/time -p /Users/johnson/AI/src/words-v2/.venv-lexicon/bin/python -m tools.lexicon.cli build-base --rerun-existing --output-dir data/lexicon/snapshots/robustness-border-5000-20260313 $(cat data/lexicon/benchmarks/robustness_border_5000_20260313.txt)` (`real 8.88`, `lexeme_count=4080`, `sense_count=13862`, `ambiguous_form_count=8`), documented in `docs/plans/2026-03-13-lexicon-robustness-5k-benchmark-report.md`. |
| 2026-03-13 | Hardened lexicon suffix-based canonicalization against weak chopped stems and expanded the local benchmark suite with dedicated `suffix_risk_1000_20260313` and `short_stem_risk_1000_20260313` datasets. Deterministic `build-base` behavior now stops selecting bogus suffix links such as `pass -> pas`, `boss -> bos`, `this -> thi`, `his -> hi`, `chris -> chri`, `versus -> versu`, `itunes -> itun`, and `series -> seri/sery` while preserving valid morphology cases such as `things -> thing`, `gives -> give`, `added -> add`, `coming -> come`, `meeting -> meet`, and `left -> leave`. Across the original three 1K benchmark sets, only `pass` and `boss` changed behavior, both from invalid `keep_both_linked` results to `keep_separate`; in the new `short_stem_risk` benchmark, ambiguous tails fell from `131` to `5`. | Codex | `/Users/johnson/AI/src/words-v2/.venv-lexicon/bin/python -m pytest tools/lexicon/tests/test_canonical_forms.py tools/lexicon/tests/test_form_adjudication.py -q` (`16` passed), `/Users/johnson/AI/src/words-v2/.venv-lexicon/bin/python -m pytest tools/lexicon/tests -q` (`193` passed), plus fresh real-provider `build-base --rerun-existing` rebuilds for `tricky_common_1000_20260313`, `morphology_edge_1000_20260313`, `semantic_ambiguity_1000_20260313`, `suffix_risk_1000_20260313`, and `short_stem_risk_1000_20260313` with summary counts `1000/4378/0`, `950/4025/2`, `998/4446/2`, `999/4006/1`, and `989/3628/5` (`lexemes/senses/ambiguous_forms`), documented in `docs/plans/2026-03-13-lexicon-suffix-hardening-benchmark-report.md`. |
| 2026-03-13 | Tightened lexicon canonicalization so deterministic collapse now requires explicit morphology evidence instead of semantic similarity from WordNet canonical labels. This removes bad headword collapses like `almost -> about`, `total -> full`, and `add -> total`, while preserving standalone learner-worthy related forms such as `meeting -> meet` and `left -> leave` as linked entries instead of collapsing them away; true inflections like `added -> add` still collapse. A 1K local rollout check against the real providers reduced deterministic collapses from the prior snapshot's `317` collapse rows to `76`, with the removed cases landing in `keep_separate` or `keep_both_linked` rather than `unknown_needs_llm`. | Codex | `/Users/johnson/AI/src/words-v2/.venv-lexicon/bin/python -m pytest tools/lexicon/tests/test_canonical_forms.py tools/lexicon/tests/test_form_adjudication.py -q` (`13` passed), `/Users/johnson/AI/src/words-v2/.venv-lexicon/bin/python -m pytest tools/lexicon/tests -q` (`190` passed), and a real-provider local 1K `build_base_records` analysis showing decision counts `keep_separate=841`, `keep_both_linked=63`, `collapse_to_canonical=76`, `unknown_needs_llm=9` with `almost`, `total`, and `add` preserved as separate headwords, `meeting` and `left` preserved as linked standalone forms, and `added` collapsing to `add`. |
| 2026-03-12 | Completed PR A for lexicon schema separation on feature branch `feat_lexicon_schema_separation_20260312`. Lexicon-owned persistence now targets the dedicated Postgres `lexicon` schema while runtime/app tables remain outside that schema in the same database, giving the lexicon tool a cleaner long-lived reference-data boundary without introducing a second DB service yet. SQLAlchemy models, runtime foreign keys to lexicon content, and the lexicon import path were verified against the updated schema-aware models. | Codex | `/Users/johnson/AI/src/words-v2/.venv-backend/bin/python -m pytest backend/tests/test_models.py backend/tests/test_lexicon_enrichment_models.py backend/tests/test_lexicon_review_models.py -q` (`28` passed), `/Users/johnson/AI/src/words-v2/.venv-lexicon/bin/python -m pytest tools/lexicon/tests/test_import_db.py tools/lexicon/tests/test_translations_pipeline.py -q` (`10` passed), `PYTHONPATH=backend /Users/johnson/AI/src/words-v2/.venv-backend/bin/python -m pytest backend/tests/test_words.py backend/tests/test_lexicon_reviews_api.py backend/tests/test_lexicon_review_publish_api.py -q` (`38` passed), and `/Users/johnson/AI/src/words-v2/.venv-backend/bin/python -m py_compile backend/app/models/schema_names.py backend/app/models/word.py backend/app/models/meaning.py backend/app/models/translation.py backend/app/models/meaning_example.py backend/app/models/word_relation.py backend/app/models/lexicon_enrichment_job.py backend/app/models/lexicon_enrichment_run.py backend/app/models/lexicon_review_batch.py backend/app/models/lexicon_review_item.py backend/app/models/review.py backend/app/models/word_list_item.py backend/alembic/versions/010_move_lexicon_tables_to_lexicon_schema.py` (pass). |
| 2026-03-12 | Completed the lexicon ambiguous-form adjudication PR 3 slice on feature branch `feat_lexicon_ambiguous_form_adjudication_20260312`. The lexicon tool now emits `ambiguous_forms.jsonl` for `unknown_needs_llm` canonicalization tails, supports bounded `adjudicate-forms` decisions that may only keep the surface form or choose from deterministic `candidate_forms`, and can replay those overrides through `build-base --adjudications` before enrichment. This keeps deterministic canonicalization as the default path while giving operators an auditable, replayable escape hatch for ambiguous forms such as `close`, `light`, and `play`. | Codex | `/Users/johnson/AI/src/words-v2/.venv-lexicon/bin/python -m pytest tools/lexicon/tests -q` (`161` passed), plus real CLI smoke via `/Users/johnson/AI/src/words-v2/.venv-lexicon/bin/python -m tools.lexicon.cli detect-ambiguous-forms --output /tmp/pr3-ambiguous-scan.jsonl close light watch content object scale fine play` (`8` ambiguous rows), `/Users/johnson/AI/src/words-v2/.venv-lexicon/bin/python -m tools.lexicon.cli adjudicate-forms --input /tmp/pr3-ambiguous-scan.jsonl --output /tmp/pr3-form-adjudications.jsonl --provider-mode placeholder`, and `/Users/johnson/AI/src/words-v2/.venv-lexicon/bin/python -m tools.lexicon.cli build-base close light play --adjudications /tmp/pr3-form-adjudications.jsonl --output-dir /tmp/pr3-adjudicated-smoke` (pass). |
| 2026-03-12 | Completed the lexicon learner-translation PR 2 slice on feature branch `feat_lexicon_translations_20260312`. The lexicon enrichment schema now requires learner translations for `zh-Hans`, `es`, `ar`, `pt-BR`, and `ja`; `compile-export` preserves them at the sense level; and `import-db` writes meaning-level translated definitions into the DB `translations` table while leaving translated example/usage-note fields in artifacts for now. This keeps the current PR tool/import-focused without expanding backend/admin API exposure in the same slice. | Codex | `/Users/johnson/AI/src/words-v2/.venv-lexicon/bin/python -m pytest tools/lexicon/tests -q` (`156` passed), plus real placeholder CLI smoke via `/Users/johnson/AI/src/words-v2/.venv-lexicon/bin/python -m tools.lexicon.cli build-base run --output-dir /tmp/lexicon-translations-pr2-smoke`, `/Users/johnson/AI/src/words-v2/.venv-lexicon/bin/python -m tools.lexicon.cli enrich --snapshot-dir /tmp/lexicon-translations-pr2-smoke --provider-mode placeholder --output /tmp/lexicon-translations-pr2-smoke/enrichments.jsonl`, `/Users/johnson/AI/src/words-v2/.venv-lexicon/bin/python -m tools.lexicon.cli validate --snapshot-dir /tmp/lexicon-translations-pr2-smoke`, and `/Users/johnson/AI/src/words-v2/.venv-lexicon/bin/python -m tools.lexicon.cli compile-export --snapshot-dir /tmp/lexicon-translations-pr2-smoke --output /tmp/lexicon-translations-pr2-smoke/words.enriched.jsonl` (compiled row contained all 5 locales). |
| 2026-03-12 | Completed PR B lexicon large-run hardening on feature branch `feat_lexicon_large_run_hardening_20260312`. Per-word enrichment now appends completed lexeme output directly into `enrichments.jsonl`, writes `enrich.checkpoint.jsonl` and `enrich.failures.jsonl` sidecars, supports `--resume`, `--checkpoint-path`, `--failures-output`, `--max-failures`, and `--request-delay-seconds`, and preserves deterministic output ordering so large 1K -> 5K -> 30K runs can recover without losing completed work. | Codex | `/Users/johnson/AI/src/words-v2/.venv-lexicon/bin/python -m pytest tools/lexicon/tests/test_enrich.py tools/lexicon/tests/test_cli.py -q` (`70` passed), `/Users/johnson/AI/src/words-v2/.venv-lexicon/bin/python -m pytest tools/lexicon/tests -q` (`166` passed), plus local resume smoke via `PYTHONPATH=. /Users/johnson/AI/src/words-v2/.venv-lexicon/bin/python /tmp/lexicon_prb_smoke.py` (first run failed after preserving `alpha`; resume completed both lexemes with `2` checkpoint rows and `1` failure row). |
| 2026-03-12 | Completed PR C ambiguous-tail/operator-adoption hardening on feature branch `feat_lexicon_ambiguous_tail_rollout_20260312`. True unresolved ambiguous-form tails like `ringed -> ring` are now deferred from `lexemes.jsonl` / `senses.jsonl` until adjudication, `lookup-entry` / `status-entry` expose pending adjudication metadata, validation catches leaked unresolved forms, and the operator guidance now recommends deterministic-first rollout with adjudication only for the deferred canonicalization tail. | Codex | `/Users/johnson/AI/src/words-v2/.venv-lexicon/bin/python -m pytest tools/lexicon/tests/test_build_base.py tools/lexicon/tests/test_form_adjudication.py tools/lexicon/tests/test_cli_canonical_registry.py tools/lexicon/tests/test_validate.py -q` (`47` passed), `/Users/johnson/AI/src/words-v2/.venv-lexicon/bin/python -m pytest tools/lexicon/tests -q` (`169` passed), `/Users/johnson/AI/src/words-v2/.venv-lexicon/bin/python -m py_compile tools/lexicon/build_base.py tools/lexicon/canonical_registry.py tools/lexicon/validate.py` (pass), plus local CLI smoke on a deferred `ringed` snapshot via `status-entry` + `validate --snapshot-dir` (pending adjudication visible; validation clean). |
| 2026-03-12 | Completed the lexicon canonical-registry PR 1 slice on feature branch `feat_lexicon_canonical_registry_20260312`. `build-base` now emits canonical registry/status sidecars, deterministically collapses obvious inflectional duplicates before sense selection, preserves lexicalized forms as linked standalone entries, and the CLI grows `lookup-entry` / `status-entry` so operators can resolve `things -> thing`-style mappings and check whether the canonical word has reached base-build/enrich/compile stages or the local DB. The multi-PR continuation path for PR 2 (translations) and PR 3 (ambiguous-form LLM adjudication) remains anchored in `docs/plans/2026-03-12-lexicon-canonical-registry-translations-multi-pr-plan.md` instead of chat-only context. | Codex | `/Users/johnson/AI/src/words-v2/.venv-lexicon/bin/python -m pytest tools/lexicon/tests -q` (`152` passed) and real CLI smoke via `/Users/johnson/AI/src/words-v2/.venv-lexicon/bin/python -m tools.lexicon.cli build-base gives giving things left --output-dir /tmp/lexicon-canonical-pr1-smoke`, `/Users/johnson/AI/src/words-v2/.venv-lexicon/bin/python -m tools.lexicon.cli lookup-entry --snapshot-dir /tmp/lexicon-canonical-pr1-smoke things`, and `/Users/johnson/AI/src/words-v2/.venv-lexicon/bin/python -m tools.lexicon.cli status-entry --snapshot-dir /tmp/lexicon-canonical-pr1-smoke left` (confirmed `give`, `thing`, and lexicalized `left` behavior). |
| 2026-03-11 | Shipped the first learner-first common-words slice in the lexicon tool. `build-base` now supports `--top-words` and staged rollout aliases, snapshot/compiled rows now carry shared entry metadata, word inventory filtering removes obvious junk tokens, and per-word enrichment now treats WordNet as grounding context while allowing the LLM to keep a bounded learner-friendly subset of meanings by frequency band. | Codex | `../../.venv-lexicon/bin/python -m pytest tools/lexicon/tests/test_models.py tools/lexicon/tests/test_build_base.py tools/lexicon/tests/test_cli.py tools/lexicon/tests/test_enrich.py tools/lexicon/tests/test_compile_export.py tools/lexicon/tests/test_validate.py -q` (`110` passed), `../../.venv-lexicon/bin/python -m pytest tools/lexicon/tests -q` (`141` passed), and bounded local smoke via `../../.venv-lexicon/bin/python -m tools.lexicon.cli build-base --top-words 10 --output-dir /tmp/lexicon-words-slice-smoke`, `enrich --snapshot-dir /tmp/lexicon-words-slice-smoke --provider-mode placeholder --mode per_word --max-concurrency 4`, `validate --snapshot-dir /tmp/lexicon-words-slice-smoke`, `compile-export --snapshot-dir /tmp/lexicon-words-slice-smoke --output /tmp/lexicon-words-slice-smoke/words.enriched.jsonl`, `validate --compiled-input /tmp/lexicon-words-slice-smoke/words.enriched.jsonl`, and `import-db --input /tmp/lexicon-words-slice-smoke/words.enriched.jsonl --dry-run` (all passed). |
| 2026-03-12 | Hardened the lexicon per-word real-enrichment path and closed two live operator failures. The word prompt now repeats the hard 8/6/4 learner-meaning cap, requires a JSON object only, and retries once with a repair prompt when the first word-level payload is invalid. During live smoke verification, re-imported meanings exposed a duplicate-example autoflush issue; `import-db` now flushes deletions before re-inserting examples/relations and de-duplicates repeated sentences/relations within a meaning. The backend settings loader also now ignores unrelated extra env keys so `import-db` can run from the normal repo-root flow even when the repository `.env` contains non-backend keys. | Codex | `../../.venv-lexicon/bin/python -m pytest tools/lexicon/tests/test_enrich.py -q` (`28` passed), `../../.venv-lexicon/bin/python -m pytest tools/lexicon/tests/test_enrich.py tools/lexicon/tests/test_import_db.py -q` (`34` passed), `../../.venv-lexicon/bin/python -m pytest tools/lexicon/tests -q` (`145` passed), `PYTHONPATH=backend ../../.venv-backend/bin/python -m pytest backend/tests/test_config.py -q` (`4` passed), and real local smoke with `build-base run set play --output-dir /tmp/lexicon-per-word-real-20260312-set-repair`, `enrich --snapshot-dir /tmp/lexicon-per-word-real-20260312-set-repair --provider-mode openai_compatible_node --mode per_word --max-concurrency 1 --model gpt-5.1 --reasoning-effort low`, `validate --snapshot-dir /tmp/lexicon-per-word-real-20260312-set-repair`, `compile-export --snapshot-dir /tmp/lexicon-per-word-real-20260312-set-repair --output /tmp/lexicon-per-word-real-20260312-set-repair/words.enriched.jsonl`, `validate --compiled-input /tmp/lexicon-per-word-real-20260312-set-repair/words.enriched.jsonl`, and `import-db --input /tmp/lexicon-per-word-real-20260312-set-repair/words.enriched.jsonl --source-reference lexicon-per-word-real-20260312-set-repair-rootflow` (pass; `updated_words=3`, `updated_meanings=18`, `created_examples=36`, `deleted_examples=36`, `created_relations=175`, `deleted_relations=175`, `created_enrichment_runs=18`). Direct DB verification via `docker exec words-postgres psql ...` showed `6` meanings each for `run`, `set`, and `play` under `lexicon-per-word-real-20260312-set-repair:%`. |
| 2026-03-12 | Fixed lexicon DB provenance for compiled per-word imports so one shared `generation_run_id` now persists as one `lexicon_enrichment_run` row per word request instead of one row per meaning. After the fix, the local app DB was wiped and a fresh real 100-word rollout was run into `data/lexicon/snapshots/words-100-20260312-fresh`, then imported successfully. Post-import verification showed `100` words, `284` meanings, `633` examples, `2707` relations, `100` enrichment jobs, and `100` enrichment runs, and sampled words like `go`, `good`, `know`, `make`, `time`, and `well` each now show `run_count = 1`. | Codex | `../../.venv-lexicon/bin/python -m pytest tools/lexicon/tests/test_import_db.py -q` (`6` passed), `../../.venv-lexicon/bin/python -m pytest tools/lexicon/tests -q` (`144` passed), local DB reset via `docker exec words-postgres psql ... TRUNCATE TABLE ... RESTART IDENTITY CASCADE` (all app tables back to `0` rows), and real rollout via `build-base --rollout-stage 100 --output-dir data/lexicon/snapshots/words-100-20260312-fresh`, `enrich --snapshot-dir data/lexicon/snapshots/words-100-20260312-fresh --provider-mode openai_compatible_node --mode per_word --max-concurrency 1 --model gpt-5.1 --reasoning-effort low`, `validate`, `compile-export`, `validate --compiled-input`, and `import-db --input data/lexicon/snapshots/words-100-20260312-fresh/words.enriched.jsonl --source-reference words-100-20260312-fresh` (pass; `created_words=100`, `created_meanings=284`, `created_examples=633`, `created_relations=2707`, `created_enrichment_jobs=100`, `created_enrichment_runs=100`). Direct DB verification via `docker exec words-postgres psql ...` confirmed sampled words each have `run_count = 1`. |
| Date (UTC) | Change | Editor | Evidence |
|---|---|---|---|
| 2026-03-22 | Refined both lexicon review UIs around faster operator throughput and consistent bulk controls. Per-row approve/reject/reopen actions in Compiled Review and JSONL Review now save immediately and auto-advance to the next pending row without confirmation, while new snapshot-level `Approve All` / `Reject All` / `Reopen All` actions require explicit confirmation and update the whole batch or JSONL session through dedicated bulk-review APIs. JSONL Review now follows the same top-level layout and review-console structure as Compiled Review, the duplicate output-directory friction was removed from the primary interaction path, both pages now show a wider raw compiled JSON panel below the review detail area, and the review counts update from the live item/session state instead of waiting for a page reload. The Playwright review smokes were updated to the new interaction model (`Refresh` instead of `Load Artifact`, no per-row confirmation button) and pass against the branch-local Docker stack. | Codex | `PYTHONPATH=backend /Users/johnson/AI/src/words-v2/.venv-backend/bin/python -m pytest backend/tests/test_lexicon_jsonl_reviews_api.py backend/tests/test_lexicon_compiled_reviews_api.py -q` (`24` passed), `NODE_PATH=/Users/johnson/AI/src/words-v2/admin-frontend/node_modules PATH=/Users/johnson/AI/src/words-v2/admin-frontend/node_modules/.bin:$PATH npm --prefix admin-frontend test -- --runInBand src/app/lexicon/jsonl-review/__tests__/page.test.tsx src/app/lexicon/compiled-review/__tests__/page.test.tsx` (`2` suites / `4` tests passed), `npm --prefix admin-frontend run lint` (pass after linking worktree `admin-frontend/node_modules` to the root install), `NEXT_PUBLIC_API_URL=/api BACKEND_URL=http://backend:8000/api npm --prefix admin-frontend run build` (pass), `PYTHONPATH=backend /Users/johnson/AI/src/words-v2/.venv-backend/bin/python -m py_compile backend/app/api/lexicon_jsonl_reviews.py backend/app/api/lexicon_compiled_reviews.py backend/app/services/lexicon_jsonl_reviews.py` (pass), `git diff --check` (pass), and branch-local Docker smoke via `docker compose -f /Users/johnson/AI/src/words-v2/.worktrees/feat_lexicon_review_bulk_actions_20260322/docker-compose.yml exec -T backend alembic upgrade head` plus `docker compose -f /Users/johnson/AI/src/words-v2/.worktrees/feat_lexicon_review_bulk_actions_20260322/docker-compose.yml exec -T playwright sh -lc "cd /workspace/e2e && E2E_DB_PASSWORD=devpassword E2E_BASE_URL=http://frontend:3000 E2E_API_URL=http://backend:8000/api E2E_ADMIN_URL=http://admin-frontend:3001 npx playwright test tests/smoke/admin-compiled-review-flow.smoke.spec.ts tests/smoke/admin-jsonl-review-flow.smoke.spec.ts --project=chromium"` (`2` passed). |
| 2026-03-21 | Added the compiled lexicon review admin tool between artifact export and DB import, then closed the missing operator-proofing gap for the DB-backed path. Backend now persists imported compiled-review batches/items/events/regeneration requests in dedicated `lexicon` schema tables and serves admin-only import/list/update/export APIs; the admin frontend adds `/lexicon/compiled-review` for batch import, item review, and decision/export actions; the lexicon CLI includes `review-materialize` to turn canonical review decisions back into approved/rejected/regenerate JSONL artifacts; and the backend import/update path was hardened so compiled-review validation no longer depends on `tools.lexicon` imports and regeneration-request writes avoid async lazy-load failures. The lighter JSONL-only review alternative remains documented as a separate design note rather than shipped implementation on this branch. | Codex | `PYTHONPATH=backend /Users/johnson/AI/src/words-v2/.venv-backend/bin/python -m pytest backend/tests/test_lexicon_reviews_api.py backend/tests/test_lexicon_review_publish_api.py backend/tests/test_lexicon_compiled_reviews_api.py backend/tests/test_lexicon_compiled_review_models.py -q` (`42` passed), `PYTHONPATH=backend /Users/johnson/AI/src/words-v2/.venv-backend/bin/python -m pytest backend/tests/test_lexicon_compiled_reviews_api.py backend/tests/test_lexicon_compiled_review_models.py -q` (`14` passed), `/Users/johnson/AI/src/words-v2/.venv-lexicon/bin/python -m pytest tools/lexicon/tests/test_review_materialize.py tools/lexicon/tests/test_cli.py tools/lexicon/tests/test_compile_export.py tools/lexicon/tests/test_import_db.py -q` (`70` passed), `npm --prefix admin-frontend test -- --runInBand src/lib/__tests__/lexicon-compiled-reviews-client.test.ts src/app/lexicon/compiled-review/__tests__/page.test.tsx` (`5` passed), `npm --prefix admin-frontend run lint` (pass), `npm --prefix admin-frontend run build` (pass), `PYTHONPATH=backend /Users/johnson/AI/src/words-v2/.venv-backend/bin/python -m py_compile backend/app/api/lexicon_compiled_reviews.py backend/app/models/lexicon_artifact_review_batch.py backend/app/models/lexicon_artifact_review_item.py backend/app/models/lexicon_artifact_review_item_event.py backend/app/models/lexicon_regeneration_request.py backend/alembic/versions/012_add_compiled_review_tables.py backend/tests/test_lexicon_compiled_reviews_api.py backend/tests/test_lexicon_compiled_review_models.py` (pass), `/Users/johnson/AI/src/words-v2/.venv-lexicon/bin/python -m py_compile tools/lexicon/review_materialize.py tools/lexicon/cli.py tools/lexicon/tests/test_review_materialize.py` (pass), targeted Docker smoke via `docker compose -f docker-compose.yml exec -T backend alembic upgrade head` plus `docker compose -f docker-compose.yml exec -T playwright sh -lc "cd /workspace/e2e && E2E_BASE_URL=http://frontend:3000 E2E_API_URL=http://backend:8000/api E2E_ADMIN_URL=http://admin-frontend:3001 npm exec playwright test tests/smoke/admin-compiled-review-flow.smoke.spec.ts --project=chromium"` (`1` passed), and doc records at `docs/plans/2026-03-21-lexicon-review-admin-tool-implementation.md` plus `docs/plans/2026-03-21-lexicon-review-admin-tool-jsonl-only-design.md`. |
| 2026-03-09 | Tightened repository agent workflow governance so merged PR branches/worktrees are no longer treated as informal scratchpads. `AGENTS.md` now explicitly requires post-merge cleanup or immediate re-branching for follow-up work, plus intentional preservation (`commit`, `stash`, or patch backup) before discarding dirty worktrees. | Codex | Governance/docs-only change verified via direct file inspection in `AGENTS.md` and local cleanup audit evidence from the same session. |
| 2026-03-09 | Added an in-app lexicon admin portal at `/admin/lexicon` so staged review is no longer API-only. The frontend now exposes staged review batches/items, item-decision editing, publish preview/publish actions, and a local-DB inspector backed by the existing word search/enrichment APIs; auth/nav protection now also covers `/admin/*`. This makes it much easier to understand what still needs human review versus what is already published into the local DB, while leaving backend admin-role enforcement and stricter final review/import gating as follow-up work. | Codex | `cd frontend && npm test -- --runInBand src/app/admin/lexicon/__tests__/page.test.tsx src/app/__tests__/layout-auth-nav.test.tsx src/app/__tests__/page.test.tsx src/lib/__tests__/lexicon-reviews-client.test.ts src/lib/__tests__/words-client.test.ts` (`25` passed), `cd frontend && npm run lint -- src/app/admin/lexicon/page.tsx src/app/admin/lexicon/__tests__/page.test.tsx src/lib/lexicon-reviews-client.ts src/lib/words-client.ts src/lib/__tests__/lexicon-reviews-client.test.ts src/lib/__tests__/words-client.test.ts src/lib/auth-nav.tsx src/lib/auth-route-guard.ts src/middleware.ts src/app/__tests__/layout-auth-nav.test.tsx src/app/__tests__/page.test.tsx` (pass), and plan added at `docs/plans/2026-03-09-lexicon-admin-portal-implementation.md`. |
| 2026-03-06 | Initialized canonical project status board and consolidated tracking sources. | Codex | `docs/plans/2026-02-26-full-rebuild.md`, `docs/plans/2026-03-05-current-state-phase-plan.md` |
| 2026-03-06 | Auth lifecycle hardening implemented (backend refresh/logout with token lifecycle controls, frontend protected routes/logout/401-refresh behavior, auth smoke coverage). | Codex | `docker compose -f docker-compose.test.yml run --rm --build test sh -lc "pip install -q -r requirements-test.txt && pytest -q"` (113 passed), `npm --prefix frontend run lint` (pass), `npm --prefix frontend test -- --runInBand` (7 suites/26 tests passed), `docker compose -f docker-compose.yml --profile tests exec -T playwright ... npm run test:smoke:ci` (6 passed), `docker compose -f docker-compose.yml --profile tests exec -T playwright ... npm run test:full` (7 passed) |
| 2026-03-06 | Word-list import domain delivered: new domain tables/models/APIs/tasks + `/imports` frontend + import-domain smoke/full verification. | Codex | `docker compose -f docker-compose.test.yml run --rm --build test sh -lc "pip install -q -r requirements-test.txt && pytest -q"` (127 passed), `npm --prefix frontend run lint` (pass), `npm --prefix frontend test -- --runInBand` (9 suites/35 tests passed), `docker compose -f docker-compose.yml --profile tests exec -T backend alembic upgrade head` (to 005), `docker compose -f docker-compose.yml --profile tests exec -T playwright sh -lc "cd /workspace/e2e && npm run test:smoke:ci"` (7 passed), `docker compose -f docker-compose.yml --profile tests exec -T playwright sh -lc "cd /workspace/e2e && npm run test:full"` (8 passed) |
| 2026-03-06 | Import completion hardening delivered: fallback NLP for missing spaCy model, shared upload directory for backend/worker, and terminal-state full E2E with valid EPUB fixture. | Codex | `docker compose -f docker-compose.test.yml run --rm --build test sh -lc "pip install -q -r requirements-test.txt && pytest tests/test_epub_processing.py tests/test_word_lists_api.py tests/test_imports_api.py -q"` (17 passed), `docker compose -f docker-compose.test.yml run --rm --build test sh -lc "pip install -q -r requirements-test.txt && pytest -q"` (129 passed), `npm --prefix frontend run lint` (pass), `npm --prefix frontend test -- --runInBand` (9 suites/35 tests passed), `docker compose -f docker-compose.yml --profile tests exec -T backend alembic upgrade head` (to 005), `docker compose -f docker-compose.yml --profile tests exec -T playwright sh -lc "cd /workspace/e2e && npm run test:smoke:ci"` (7 passed), `docker compose -f docker-compose.yml --profile tests exec -T playwright sh -lc "cd /workspace/e2e && npm run test:full"` (9 passed) |
| 2026-03-06 | Added explicit TODO for import storage lifecycle: move upload/temp artifacts to object storage (or equivalent shared temp layer) and enforce cleanup guarantees. | Codex | `docs/status/project-status.md` |
| 2026-03-07 | Lexicon tool foundation implemented in worktree: Python CLI scaffold, WordNet/wordfreq base builder, validation/compiler modules, stable ID helpers, local DB importer foundation, and provenance-ready model/migration changes. | Codex | `python3 -m unittest discover -s tools/lexicon/tests -p "test_*.py"` (27 tests passed), `python3 -m unittest tools.lexicon.tests.test_import_db` (2 tests passed), `PYTHONPYCACHEPREFIX=/tmp/pycache python3 -m py_compile tools/lexicon/import_db.py tools/lexicon/cli.py tools/lexicon/build_base.py tools/lexicon/compile_export.py tools/lexicon/validate.py backend/app/models/word.py backend/app/models/meaning.py backend/alembic/versions/006_add_lexicon_import_provenance.py` (pass); backend runtime model import smoke blocked locally because `sqlalchemy` is not installed in this shell. |
| 2026-03-07 | Lexicon tool snapshot file-I/O + CLI wiring delivered: normalized snapshot write support, snapshot validation from disk, compiled export generation, and `import-db --dry-run` CLI summary path. | Codex | `python3 -m unittest discover -s tools/lexicon/tests -p "test_*.py"` (31 tests passed), `python3 -m unittest tools.lexicon.tests.test_cli` (5 tests passed), `PYTHONPYCACHEPREFIX=/tmp/pycache python3 -m py_compile tools/lexicon/jsonl_io.py tools/lexicon/build_base.py tools/lexicon/validate.py tools/lexicon/compile_export.py tools/lexicon/cli.py tools/lexicon/import_db.py` (pass) |
| 2026-03-07 | Lexicon tool admin import path hardened: real sync-session DB import wiring now mirrors backend patterns, CLI docs/tests reflect `--compiled-input`/`--compiled-path` and `--language`, and provenance columns are ready for compiled snapshot loads. | Codex | `python3 -m unittest discover -s tools/lexicon/tests -p "test_*.py"` (35 tests passed), `python3 -m unittest tools.lexicon.tests.test_cli` (9 tests passed), `PYTHONPYCACHEPREFIX=/tmp/pycache python3 -m py_compile tools/lexicon/import_db.py tools/lexicon/cli.py tools/lexicon/build_base.py tools/lexicon/compile_export.py tools/lexicon/validate.py backend/app/models/word.py backend/app/models/meaning.py backend/alembic/versions/006_add_lexicon_import_provenance.py` (pass); real DB-path smoke still depends on local backend DB deps/settings. |
| 2026-03-07 | Lexicon provider/enrichment slice landed: `build-base` now resolves real WordNet + `wordfreq` providers with fail-loud dependency errors on the operator path, and a separate offline `enrich` command now writes `enrichments.jsonl` before validation/compile/import. | Codex | `python3 -m unittest discover -s tools/lexicon/tests -p "test_*.py"` (40 tests passed), `python3 -m unittest tools.lexicon.tests.test_cli tools.lexicon.tests.test_enrich` (13 tests passed), `PYTHONPYCACHEPREFIX=/tmp/pycache python3 -m py_compile tools/lexicon/build_base.py tools/lexicon/wordnet_provider.py tools/lexicon/wordfreq_provider.py tools/lexicon/enrich.py tools/lexicon/cli.py tools/lexicon/compile_export.py tools/lexicon/validate.py tools/lexicon/import_db.py backend/app/models/word.py backend/app/models/meaning.py backend/alembic/versions/006_add_lexicon_import_provenance.py` (pass); live operator smoke with installed `nltk`/WordNet and `wordfreq` is not run in this shell because those packages/corpora are absent locally. |
| 2026-03-07 | Lexicon operator smoke verified in a repo-local venv: installed `tools/lexicon/requirements.txt`, downloaded WordNet corpora, and ran `build-base -> enrich -> validate -> compile-export` successfully against a real snapshot directory. | Codex | `./.venv-lexicon/bin/python -m pip install -r tools/lexicon/requirements.txt` (pass), `./.venv-lexicon/bin/python -m nltk.downloader wordnet omw-1.4` (pass), `./.venv-lexicon/bin/python -m tools.lexicon.cli build-base run set lead --output-dir /tmp/lexicon-smoke` (3 lexemes / 12 senses / 12 concepts), `./.venv-lexicon/bin/python -m tools.lexicon.cli enrich --snapshot-dir /tmp/lexicon-smoke` (12 enrichments), `./.venv-lexicon/bin/python -m tools.lexicon.cli validate --snapshot-dir /tmp/lexicon-smoke` (0 errors), `./.venv-lexicon/bin/python -m tools.lexicon.cli compile-export --snapshot-dir /tmp/lexicon-smoke --output /tmp/lexicon-smoke/words.enriched.jsonl` (3 compiled rows), `python3 -m unittest discover -s tools/lexicon/tests -p "test_*.py"` (40 tests passed), `PYTHONPYCACHEPREFIX=/tmp/pycache python3 -m py_compile tools/lexicon/build_base.py tools/lexicon/wordnet_provider.py tools/lexicon/wordfreq_provider.py tools/lexicon/enrich.py tools/lexicon/cli.py tools/lexicon/compile_export.py tools/lexicon/validate.py tools/lexicon/import_db.py backend/app/models/word.py backend/app/models/meaning.py backend/alembic/versions/006_add_lexicon_import_provenance.py` (pass). |
| 2026-03-07 | Added dedicated lexicon CI coverage: a new workflow job now installs tool-local deps, caches/downloads WordNet corpora, runs `tools/lexicon` unit tests, and exercises the offline `build-base -> enrich -> validate -> compile-export` smoke flow without requiring a real LLM key. | Codex | `ruby -e 'require "yaml"; YAML.load_file(".github/workflows/ci.yml"); puts "ci.yml OK"'` (pass), `python3 -m unittest discover -s tools/lexicon/tests -p "test_*.py"` (40 tests passed), local repo-local-venv smoke flow (pass). |
| 2026-03-07 | Added a separate manual/nightly lexicon OpenAI-compatible smoke workflow: it validates secret-backed endpoint config in a protected environment, installs tool-local deps, downloads WordNet corpora, and runs a bounded offline smoke flow without exposing the real LLM key in PR CI. | Codex | `ruby -e 'require "yaml"; YAML.load_file(".github/workflows/lexicon-openai-compatible-smoke.yml"); puts "lexicon-openai-compatible-smoke.yml OK"'` (pass), `python3 -m unittest discover -s tools/lexicon/tests -p "test_*.py"` (40 tests passed). |
| 2026-03-07 | Real endpoint-backed lexicon enrichment landed: `enrich` now supports `auto|placeholder|openai_compatible` provider modes, `LEXICON_LLM_BASE_URL` config for OpenAI-compatible Responses endpoints, backward-compatible fallback from `LEXICON_LLM_PROVIDER`, and a manual/nightly workflow that exercises the real endpoint path while PR CI stays explicit placeholder-only. | Codex | `python3 -m unittest tools.lexicon.tests.test_enrich tools.lexicon.tests.test_config tools.lexicon.tests.test_cli` (22 tests passed), `python3 -m unittest discover -s tools/lexicon/tests -p "test_*.py"` (47 tests passed), `PYTHONPYCACHEPREFIX=/tmp/pycache python3 -m py_compile tools/lexicon/config.py tools/lexicon/enrich.py tools/lexicon/cli.py tools/lexicon/build_base.py tools/lexicon/wordnet_provider.py tools/lexicon/wordfreq_provider.py tools/lexicon/compile_export.py tools/lexicon/validate.py tools/lexicon/import_db.py` (pass), `ruby -e 'require "yaml"; YAML.load_file(".github/workflows/ci.yml"); YAML.load_file(".github/workflows/lexicon-openai-compatible-smoke.yml"); puts "workflow yaml OK"'` (pass). |
| 2026-03-07 | Added a tiny local real-endpoint smoke command: `smoke-openai-compatible` now runs `build-base -> enrich(openai_compatible) -> validate -> compile-export` for a tiny seed set, and was verified end-to-end against a local OpenAI-compatible mock endpoint. | Codex | `python3 -m unittest tools.lexicon.tests.test_cli tools.lexicon.tests.test_enrich tools.lexicon.tests.test_config` (24 tests passed), `python3 -m unittest discover -s tools/lexicon/tests -p "test_*.py"` (49 tests passed), `PYTHONPYCACHEPREFIX=/tmp/pycache python3 -m py_compile tools/lexicon/config.py tools/lexicon/enrich.py tools/lexicon/cli.py tools/lexicon/build_base.py tools/lexicon/wordnet_provider.py tools/lexicon/wordfreq_provider.py tools/lexicon/compile_export.py tools/lexicon/validate.py tools/lexicon/import_db.py` (pass), local mock smoke for `python -m tools.lexicon.cli smoke-openai-compatible --output-dir /tmp/lexicon-openai-compatible-local-smoke run set` (2 compiled rows, pass). |
| 2026-03-07 | OpenAI-compatible lexicon enrichment now validates required learner-facing response fields before record creation, failing loudly with field-specific errors for malformed `definition`, `examples`, or `confidence` payloads instead of silently falling back. | Codex | `python3 -m unittest tools.lexicon.tests.test_enrich` (9 tests passed), `python3 -m unittest tools.lexicon.tests.test_cli tools.lexicon.tests.test_enrich tools.lexicon.tests.test_config` (27 tests passed), `python3 -m unittest discover -s tools/lexicon/tests -p "test_*.py"` (52 tests passed), `PYTHONPYCACHEPREFIX=/tmp/lexicon-pycache python3 -m py_compile tools/lexicon/enrich.py tools/lexicon/cli.py tools/lexicon/config.py` (pass). |
| 2026-03-07 | OpenAI-compatible lexicon enrichment P1 schema hardening landed: the real provider path now validates CEFR/register enums, list-of-strings fields, `forms` shape, and `confusable_words` item structure before record creation, surfacing field-specific payload errors instead of silent coercion. | Codex | `python3 -m unittest tools.lexicon.tests.test_enrich` (14 tests passed), `python3 -m unittest tools.lexicon.tests.test_cli tools.lexicon.tests.test_enrich tools.lexicon.tests.test_config` (32 tests passed), `python3 -m unittest discover -s tools/lexicon/tests -p "test_*.py"` (57 tests passed), `PYTHONPYCACHEPREFIX=/tmp/lexicon-pycache python3 -m py_compile tools/lexicon/enrich.py tools/lexicon/cli.py tools/lexicon/config.py` (pass). |
| 2026-03-07 | Added lexicon pytest support for local admin/dev use and CI: a dedicated dev requirements file now installs pytest into the repo-local lexicon venv, both lexicon workflows run `python -m pytest tools/lexicon/tests -q`, and the existing bounded smoke flow remains in place after tests. | Codex | `PIP_NO_CACHE_DIR=1 ./.venv-lexicon/bin/python -m pip install -r tools/lexicon/requirements-dev.txt` (pass), `./.venv-lexicon/bin/python -m pytest tools/lexicon/tests -q` (57 passed), repo-local venv smoke flow for `build-base -> enrich --provider-mode placeholder -> validate -> compile-export` (3 lexemes / 12 enrichments / 3 compiled rows, pass), `ruby -e 'require "yaml"; YAML.load_file(".github/workflows/ci.yml"); YAML.load_file(".github/workflows/lexicon-openai-compatible-smoke.yml"); puts "workflow yaml OK"'` (pass). |
| 2026-03-07 | Real local DB import smoke verified for the lexicon tool against an isolated temporary Postgres instance: the worktree migration chain applied cleanly, a compiled export imported successfully, a second import updated in place without duplicate rows, and persisted provenance fields matched the expected `source_type` / `source_reference` values. | Codex | isolated DB smoke on alternate local ports with `alembic upgrade head` (001 -> 006 applied), `python -m tools.lexicon.cli import-db --input /tmp/lexicon-db-import-smoke/words.enriched.jsonl --source-type lexicon_snapshot --source-reference lexicon-smoke-20260307 --language en` (first run: 2 words / 4 meanings created), second identical import (2 words / 4 meanings updated), direct DB inspection confirmed 2 `words` rows and 4 `meanings` rows with `lexicon_snapshot` provenance. |
| 2026-03-07 | Added a Node-backed OpenAI-compatible lexicon transport for Cloudflare-fronted custom gateways: `openai_compatible_node` now uses the official Node OpenAI SDK, `LEXICON_LLM_TRANSPORT=node` can auto-select it, the manual/nightly smoke workflow installs tool-local Node deps when needed, and a real smoke against `https://api.nwai.cc` compiled 2 words successfully without changing the offline/admin pipeline shape. | Codex | `python3 -m unittest discover -s tools/lexicon/tests -p "test_*.py"` (63 tests passed), `npm --prefix tools/lexicon ci` (pass), `ruby -e 'require "yaml"; YAML.load_file(".github/workflows/ci.yml"); YAML.load_file(".github/workflows/lexicon-openai-compatible-smoke.yml"); puts "workflow yaml OK"'` (pass), real smoke via `/tmp/lexicon-db-smoke-venv/bin/python -m tools.lexicon.cli smoke-openai-compatible --provider-mode openai_compatible_node --output-dir /tmp/lexicon-openai-node-smoke run set` with `LEXICON_LLM_BASE_URL=https://api.nwai.cc`, `LEXICON_LLM_MODEL=gpt-5.1`, and env-provided key (lexeme_count 2 / compiled_count 2, pass). |
| 2026-03-07 | Added operator-facing lexicon setup docs: a tool-local `tools/lexicon/.env.example` now shows safe offline/admin env configuration, `tools/lexicon/OPERATOR_GUIDE.md` documents setup and the full `build-base -> enrich -> validate -> compile-export -> import-db` flow, and the root `.env.example` now points operators to those tool-local references instead of duplicating the full admin configuration. | Codex | `python3 -m unittest discover -s tools/lexicon/tests -p "test_*.py"` (63 tests passed), `git diff --check` (pass). |
| 2026-03-07 | Lexicon base selection now uses learner-oriented, frequency-aware sense ranking with adaptive `4/6/8` coverage inside an `--max-senses` ceiling of `8`: WordNet lemma counts and gloss heuristics still surface high-value noun meanings, and a new soft adjective/adverb viability layer now expands broad mixed-POS lemmas when useful adjective/adverb senses would otherwise be squeezed out, without hard POS quotas. Specialized adjective senses are also demoted more aggressively for cases like `direct`. | Codex | `python3 -m unittest discover -s tools/lexicon/tests -p "test_*.py"` (77 tests passed), `PYTHONPYCACHEPREFIX=/tmp/lexicon-ranking-pycache python3 -m py_compile tools/lexicon/wordnet_utils.py tools/lexicon/wordnet_provider.py tools/lexicon/build_base.py tools/lexicon/cli.py` (pass), 50-word `build-base --max-senses 8` sweep (distribution: 11×4 / 33×6 / 7×8; mixed-POS coverage improved for `right`, `clear`, `open`, `direct`, `present`, `light`, while `break` remained noun/verb-only), real gateway smoke via `/tmp/lexicon-db-smoke-venv/bin/python -m tools.lexicon.cli smoke-openai-compatible --provider-mode openai_compatible_node --max-senses 8 --output-dir /tmp/lexicon-ranking-openai-smoke-pos right open direct break` (4 lexemes / 32 enrichments / 4 compiled rows, pass), isolated temp Postgres import smoke on that compiled output (first run: 4 words / 32 meanings created; second run: 4 words / 32 meanings updated; provenance persisted as `lexicon_snapshot` / `lexicon-ranking-pos-20260307`). |
| 2026-03-07 | Dedicated lexicon model benchmarks now document low-effort enrichment quality and latency across `gpt-5.1`, `gpt-5.2`, `gpt-5.3`, and `gpt-5.4`: both the 4-prompt and 14-prompt runs concluded `gpt-5.4 ≈ gpt-5.3 > gpt-5.1 > gpt-5.2`, with `gpt-5.2` also the weakest speed/quality tradeoff. Operator-facing details, artifact locations, and 20k-word planning caveats now live in `tools/lexicon/MODEL_BENCHMARKS.md`. | Codex | 4-prompt validated benchmark artifacts under `/tmp/lexicon-model-compare`, 14-prompt validated benchmark artifacts under `/tmp/lexicon-model-compare-14`, schema validation passed for all generated JSON payloads, and shared-load 14-prompt timing rerun completed for all four models (`gpt-5.1`: 106s, `gpt-5.2`: 218s, `gpt-5.3`: 172s, `gpt-5.4`: 134s). |
| 2026-03-07 | Lexicon benchmark controls and sense ranking were refined further: the admin tool now supports optional `LEXICON_LLM_REASONING_EFFORT` plus per-run `--model` / `--reasoning-effort` overrides for `enrich` and `smoke-openai-compatible`, the OpenAI-compatible Python and Node transports forward reasoning effort correctly, and learner-facing sense ranking now boosts core concrete meanings while demoting low-value control senses, body-part-only adjective senses, and tournament-style specialized meanings. | Codex | `python3 -m unittest tools.lexicon.tests.test_config tools.lexicon.tests.test_enrich tools.lexicon.tests.test_cli` (44 tests passed), `python3 -m unittest tools.lexicon.tests.test_build_base` (20 tests passed), `python3 -m unittest discover -s tools/lexicon/tests -p "test_*.py"` (83 tests passed), `PYTHONPYCACHEPREFIX=/tmp/lexicon-ranking-pycache python3 -m py_compile tools/lexicon/wordnet_utils.py tools/lexicon/wordnet_provider.py tools/lexicon/build_base.py tools/lexicon/cli.py tools/lexicon/enrich.py tools/lexicon/config.py` (pass), synthetic selector spot-check after patch (`break`: `break.v.core` outranks `break.v.obedience`; `open`: `open.a.general` is retained while `open.n.tournament` drops out of the first four). |
| 2026-03-08 | Lexicon admin tooling now supports a truly bounded real smoke path plus grounded selector-vs-rerank evaluation: `smoke-openai-compatible` is capped by default to `1` word and `2` senses per word, the selector was further hardened for alias-like tails and practical learner meanings, `rerank-senses` now asks the LLM to choose only from bounded WordNet candidate `wn_synset_id`s, and `compare-selection` reports deterministic vs reranked swaps without altering the base snapshot schema. Operator docs, workflow smoke bounds, and a learner-priority rubric now match the new flow. | Codex | `python3 -m unittest discover -s tools/lexicon/tests -p "test_*.py"` (98 tests passed), `PYTHONPYCACHEPREFIX=/tmp/lexicon-rerank-pycache python3 -m py_compile tools/lexicon/wordnet_utils.py tools/lexicon/wordnet_provider.py tools/lexicon/build_base.py tools/lexicon/cli.py tools/lexicon/enrich.py tools/lexicon/rerank.py tools/lexicon/compare_selection.py` (pass), real bounded smoke via `./.venv-lexicon/bin/python -m tools.lexicon.cli smoke-openai-compatible --provider-mode openai_compatible_node --output-dir /tmp/lexicon-bounded-smoke-20260308 --max-words 1 --max-senses 2 --model gpt-5.1 --reasoning-effort low run` (1 lexeme / 2 senses / 2 enrichments / 1 compiled row, pass), real rerank + compare on `check` via `/tmp/lexicon-rerank-check-20260308` (`rerank-senses` wrote 1 row; `compare-selection` reported 1 changed lexeme, dropping deterministic tail `check.n.09` and adding grounded candidate `check.v.22`), `ruby -e 'require "yaml"; YAML.load_file(".github/workflows/lexicon-openai-compatible-smoke.yml"); puts "workflow yaml OK"'` (pass). |
| 2026-03-08 | Real selector-vs-rerank benchmark on tricky lemmas suggests the new grounded `rerank-senses` step is useful as a quality mode, but not a full replacement for deterministic ranking. On the 10-word batch (`right`, `direct`, `common`, `fine`, `fair`, `plain`, `check`, `charge`, `scale`, `break`), rerank changed all 10 selections and often removed obvious deterministic tails, but it also introduced some debatable substitutes, so the best near-term path is to keep improving deterministic ranking and use rerank selectively for high-polysemy review or premium-quality builds. | Codex | deterministic baseline via `./.venv-lexicon/bin/python -m tools.lexicon.cli build-base --max-senses 6 --output-dir /tmp/lexicon-rerank-benchmark-20260308 right direct common fine fair plain check charge scale break` (10 lexemes / 52 senses, pass), real rerank via `./.venv-lexicon/bin/python -m tools.lexicon.cli rerank-senses --snapshot-dir /tmp/lexicon-rerank-benchmark-20260308 --provider-mode openai_compatible_node --model gpt-5.4 --reasoning-effort low --candidate-limit 8 right direct common fine fair plain check charge scale break` (10 rerank rows, pass), compare via `./.venv-lexicon/bin/python -m tools.lexicon.cli compare-selection --snapshot-dir /tmp/lexicon-rerank-benchmark-20260308 --rerank-file /tmp/lexicon-rerank-benchmark-20260308/sense_reranks.jsonl --output /tmp/lexicon-rerank-benchmark-20260308/comparison.json` (`10/10` lexemes changed; clearer wins included removing `complain.v.01` from `plain`, dropping `check.n.09`, and replacing `break.v.40` / `break.n.11` tails). |

| 2026-03-08 | Lexicon benchmarking and rerank controls now cover unseen holdout evaluation too: the admin tool ships built-in `tuning` and `holdout` word lists plus a new `benchmark-selection` command, `rerank-senses` exposes explicit `selected_only`, `candidates`, and `full_wordnet` candidate-source modes, and the deterministic selector picked up another generic scoring pass for finance/document boosts plus sports/legal/religious/geometry tail demotions. The fresh real gateway run suggests `candidates` is the best practical rerank mode, `selected_only` is safest for audit-only review, and `full_wordnet` is useful mainly as an exploratory stress mode because it more often introduces debatable substitutions. | Codex | `python3 -m unittest discover -s tools/lexicon/tests -p "test_*.py"` (112 tests passed), `PYTHONPYCACHEPREFIX=/tmp/lexicon-holdout-benchmark-pycache python3 -m py_compile tools/lexicon/wordnet_utils.py tools/lexicon/wordnet_provider.py tools/lexicon/build_base.py tools/lexicon/cli.py tools/lexicon/enrich.py tools/lexicon/rerank.py tools/lexicon/compare_selection.py tools/lexicon/benchmark_selection.py` (pass), real deterministic holdout rebuild via `./.venv-lexicon/bin/python -m tools.lexicon.cli build-base --max-senses 6 --output-dir /tmp/lexicon-holdout-det-20260308 $(jq -r '.[]' tools/lexicon/benchmarks/holdout_words.json)` (20 lexemes / 106 senses, with visible improvements such as `file.n.01` surfacing and sports/biblical tails dropping), real mode comparison via `./.venv-lexicon/bin/python -m tools.lexicon.cli benchmark-selection --output-dir /tmp/lexicon-bench-suite-20260308 --dataset tuning --dataset holdout --with-rerank --provider-mode openai_compatible_node --model gpt-5.4 --reasoning-effort low --candidate-source selected_only --candidate-source candidates --candidate-source full_wordnet` (tuning: `selected_only 9/10`, `candidates 10/10`, `full_wordnet 10/10` changed; holdout: `selected_only 18/20`, `candidates 20/20`, `full_wordnet 20/20` changed; representative `candidates` wins included `case` dropping robbery/enclose tails and `change` dropping clothing/change-coverings tails, while `full_wordnet` also surfaced noisier substitutions like `event.n.02`, `travel_by.v.01`, and `orderliness.n.02`). |
| 2026-03-08 | Documented the next lexicon review architecture and deferred UI path: new design/spec docs now define a staged lexicon-review workflow in the main stack, exact `selection_decisions.jsonl` contract, the first-pass risk-score formula, bounded rerank routing and auto-accept rules, and the decision to keep a future admin review UI as TODO until the rebuild actually includes `admin-frontend/`. The accepted architecture is offline generation -> staged review storage -> targeted human review -> explicit publish, not raw JSONL inspection and not direct publish from snapshot files. | Codex | Design doc added at `docs/plans/2026-03-08-lexicon-review-staging-design.md`, implementation/spec plan added at `docs/plans/2026-03-08-lexicon-review-staging-implementation.md`, ADR added at `docs/decisions/ADR-003-lexicon-review-staging.md`, and live status updated here after verifying the new files exist and reference the current lexicon/admin integration anchors. |
| 2026-03-08 | Implemented the first offline review-staging slice for lexicon sense selection: the tool now ships `score-selection-risk` to write `selection_decisions.jsonl` from deterministic WordNet choices plus risk metadata, and `prepare-review` to rerank only risky lexemes, auto-accept stable changes, and emit `review_queue.jsonl` for remaining human review. Operator docs now describe the staged deterministic -> rerank -> human-review flow, and the review contract includes generation metadata plus stricter review gating for substantial high-frequency rerank changes. | Codex | `python3 -m unittest discover -s tools/lexicon/tests -p "test_*.py"` (120 tests passed), `PYTHONPYCACHEPREFIX=/tmp/lexicon-selection-review-pycache python3 -m py_compile tools/lexicon/selection_review.py tools/lexicon/cli.py tools/lexicon/rerank.py tools/lexicon/benchmark_selection.py tools/lexicon/wordnet_utils.py` (pass), real `score-selection-risk` smoke via `./.venv-lexicon/bin/python -m tools.lexicon.cli score-selection-risk --snapshot-dir /tmp/lexicon-holdout-det-20260308 --output /tmp/lexicon-holdout-det-20260308/selection_decisions.jsonl` (20 decisions; 19 `rerank_and_review_candidate`, 1 `deterministic_only`, pass), bounded offline `prepare-review` smoke via `./.venv-lexicon/bin/python -m tools.lexicon.cli prepare-review --snapshot-dir /tmp/lexicon-holdout-det-20260308 --decisions /tmp/lexicon-holdout-det-20260308/selection_decisions.deterministic_only.jsonl --output /tmp/lexicon-holdout-det-20260308/selection_decisions.deterministic_only.reviewed.jsonl --review-queue-output /tmp/lexicon-holdout-det-20260308/review_queue.deterministic_only.jsonl` (1 deterministic-only decision; 0 reranked; 0 review rows, pass). |
| 2026-03-08 | Implemented backend staging for lexicon review artifacts before final publish/import: dedicated `lexicon_review_batches` and `lexicon_review_items` tables now store imported `selection_decisions.jsonl` rows, and new owner-scoped API endpoints support batch import, batch list/detail, item list with filters, and per-item review decisions. This keeps staged lexicon review separate from the current direct `import-db` path, with publish-to-main tables still intentionally deferred. | Codex | `PYTHONPYCACHEPREFIX=/tmp/lexicon-review-backend-pycache python3 -m py_compile backend/app/api/lexicon_reviews.py backend/app/main.py backend/app/models/lexicon_review_batch.py backend/app/models/lexicon_review_item.py backend/alembic/versions/007_add_lexicon_review_staging.py backend/tests/test_lexicon_review_models.py backend/tests/test_lexicon_reviews_api.py` (pass), independent container verification via `docker run --rm -v /Users/johnson/AI/src/words-v2/.worktrees/lexicon-sense-ranking/backend:/app -w /app python:3.11-slim bash -lc "pip install -q fastapi sqlalchemy pydantic pydantic-settings email-validator redis python-multipart slowapi structlog 'python-jose[cryptography]' bcrypt asyncpg psycopg2-binary pytest pytest-asyncio httpx ebooklib beautifulsoup4 celery && PYTHONPATH=/app pytest -q tests/test_lexicon_review_models.py tests/test_lexicon_reviews_api.py"` (`13` passed), backend design doc added at `docs/plans/2026-03-08-lexicon-review-backend-staging-design.md`, implementation plan added at `docs/plans/2026-03-08-lexicon-review-backend-staging-implementation.md`. |
| 2026-03-08 | Added `publish-review-batch` on top of the staged lexicon review backend: approved staged items can now publish into the main `Word` / `Meaning` tables using a replace-by-source strategy that updates or creates `Word` rows, replaces only prior `lexicon_review_publish` meanings, and preserves meanings from other sources. This keeps staged review as the gate before publish while still supporting iterative lexicon improvement on the current schema. | Codex | `PYTHONPYCACHEPREFIX=/tmp/lexicon-review-publish-pycache python3 -m py_compile backend/app/api/lexicon_reviews.py backend/app/main.py backend/app/models/lexicon_review_batch.py backend/app/models/lexicon_review_item.py backend/alembic/versions/007_add_lexicon_review_staging.py backend/tests/test_lexicon_review_models.py backend/tests/test_lexicon_reviews_api.py` (pass), focused publish verification via `docker run --rm -v /Users/johnson/AI/src/words-v2/.worktrees/lexicon-sense-ranking/backend:/app -w /app python:3.11-slim bash -lc "pip install -q fastapi sqlalchemy pydantic pydantic-settings email-validator redis python-multipart slowapi structlog 'python-jose[cryptography]' bcrypt asyncpg psycopg2-binary pytest pytest-asyncio httpx ebooklib beautifulsoup4 celery && PYTHONPATH=/app pytest -q tests/test_lexicon_reviews_api.py -k publish"` (`4` passed), full staged-review backend verification via `docker run --rm -v /Users/johnson/AI/src/words-v2/.worktrees/lexicon-sense-ranking/backend:/app -w /app python:3.11-slim bash -lc "pip install -q fastapi sqlalchemy pydantic pydantic-settings email-validator redis python-multipart slowapi structlog 'python-jose[cryptography]' bcrypt asyncpg psycopg2-binary pytest pytest-asyncio httpx ebooklib beautifulsoup4 celery && PYTHONPATH=/app pytest -q tests/test_lexicon_review_models.py tests/test_lexicon_reviews_api.py tests/test_lexicon_review_publish_api.py"` (`20` passed), publish design doc added at `docs/plans/2026-03-08-lexicon-review-publish-design.md`, implementation plan added at `docs/plans/2026-03-08-lexicon-review-publish-implementation.md`. |
| 2026-03-08 | Added a read-only lexicon publish preview plus the first learner-facing enrichment schema slice. The backend now exposes `GET /api/lexicon-reviews/batches/{batch_id}/publish-preview` to dry-run staged publish impact without mutating the DB, and the schema now includes `meaning_examples`, `word_relations`, `lexicon_enrichment_jobs`, `lexicon_enrichment_runs`, plus `words.phonetic_source`, `words.phonetic_confidence`, and `words.phonetic_enrichment_run_id` for later LLM enrichment persistence. This slice also adds confidence-range constraints on new enrichment confidence fields; richer public APIs and the full enrichment pipeline remain deferred. | Codex | `PYTHONPYCACHEPREFIX=/tmp/lexicon-preview-enrichment-pycache python3 -m py_compile backend/app/api/lexicon_reviews.py backend/tests/test_lexicon_reviews_api.py backend/app/models/word.py backend/app/models/__init__.py backend/app/models/meaning_example.py backend/app/models/word_relation.py backend/app/models/lexicon_enrichment_job.py backend/app/models/lexicon_enrichment_run.py backend/alembic/versions/008_add_lexicon_enrichment_schema.py backend/tests/test_lexicon_enrichment_models.py` (pass), container verification via `docker run --rm -v /Users/johnson/AI/src/words-v2/.worktrees/lexicon-sense-ranking/backend:/app -w /app python:3.11-slim bash -lc "pip install -q fastapi sqlalchemy pydantic pydantic-settings email-validator redis python-multipart slowapi structlog 'python-jose[cryptography]' bcrypt asyncpg psycopg2-binary pytest pytest-asyncio httpx ebooklib beautifulsoup4 celery && PYTHONPATH=/app pytest -q tests/test_lexicon_review_models.py tests/test_lexicon_reviews_api.py tests/test_lexicon_review_publish_api.py tests/test_lexicon_enrichment_models.py"` (`28` passed). |
| 2026-03-08 | Extended the offline lexicon compiled/import path so learner-facing enrichment now writes through to the local DB instead of stopping at core words/meanings. `compile-export` now preserves sense-level provenance (`wn_synset_id`, enrichment/model/prompt/run/confidence metadata), and `import-db` now upserts `words`/`meanings`, replaces source-scoped `meaning_examples`, replaces source-scoped `word_relations` for `synonym`/`antonym`/`collocation`, creates or reuses word-scoped enrichment jobs plus sense-scoped enrichment runs, and reports dry-run example/relation counts for operators. Phrase linking, raw generator payload storage, and fuller learner-facing meaning fields remain deferred. | Codex | `python3 -m unittest tools.lexicon.tests.test_cli tools.lexicon.tests.test_compile_export tools.lexicon.tests.test_import_db` (40 tests passed), `python3 -m unittest discover -s tools/lexicon/tests -p 'test_*.py'` (122 tests passed), `PYTHONPYCACHEPREFIX=/tmp/lexicon-import-writeback-pycache python3 -m py_compile tools/lexicon/import_db.py tools/lexicon/compile_export.py tools/lexicon/cli.py tools/lexicon/tests/test_import_db.py tools/lexicon/tests/test_compile_export.py tools/lexicon/tests/test_cli.py` (pass), design doc added at `docs/plans/2026-03-08-lexicon-enrichment-import-writeback-design.md`, implementation plan added at `docs/plans/2026-03-08-lexicon-enrichment-import-writeback-implementation.md`. |
| 2026-03-08 | Added a dedicated read-only enrichment inspection endpoint at `GET /api/words/{word_id}/enrichment` so imported learner-facing data can be inspected without querying tables directly or changing the main word-detail contract. The endpoint returns word-level phonetic provenance, meanings, meaning examples, meaning-linked relations, and referenced enrichment runs; it follows the repo’s existing authenticated read pattern, while explicit role-based admin gating remains deferred. | Codex | `PYTHONPYCACHEPREFIX=/tmp/word-enrichment-api-pycache python3 -m py_compile backend/app/api/words.py backend/tests/test_words.py` (pass), container verification via `docker run --rm -v /Users/johnson/AI/src/words-v2/.worktrees/lexicon-sense-ranking/backend:/app -w /app python:3.11-slim bash -lc "pip install -q fastapi sqlalchemy pydantic pydantic-settings email-validator redis python-multipart slowapi structlog 'python-jose[cryptography]' bcrypt asyncpg psycopg2-binary pytest pytest-asyncio httpx ebooklib beautifulsoup4 celery && PYTHONPATH=/app pytest -q tests/test_words.py"` (`10` passed), design doc added at `docs/plans/2026-03-08-word-enrichment-inspection-api-design.md`, implementation plan added at `docs/plans/2026-03-08-word-enrichment-inspection-api-implementation.md`. |
| 2026-03-08 | Closed the lexicon tool as a working local-DB admin tool by finishing the three closure slices: the canonical final learner-enrichment DB write path is now explicitly `compile-export -> import-db`, a clean isolated end-to-end smoke proved `build-base -> enrich -> validate -> compile-export -> validate --compiled-input -> import-db -> API inspection`, and a reusable working-gate runbook plus future-improvements TODO list now define what is in-scope for closure vs deferred hardening. Staged review remains the review/decision layer; the narrower staged-review publish path is transitional until/unless it delegates to importer-equivalent writeback. | Codex | isolated DB creation via `docker exec words-postgres psql -U vocabapp -d postgres -c "DROP DATABASE IF EXISTS lexicon_working_gate_20260308;"` and `... -c "CREATE DATABASE lexicon_working_gate_20260308;"` (pass), isolated migration via `docker run --rm --network host -e DATABASE_URL=postgresql+asyncpg://vocabapp:devpassword@localhost:5432/lexicon_working_gate_20260308 -e DATABASE_URL_SYNC=postgresql://vocabapp:devpassword@localhost:5432/lexicon_working_gate_20260308 -v /Users/johnson/AI/src/words-v2/.worktrees/lexicon-sense-ranking/backend:/app -w /app python:3.11-slim bash -lc "pip install -q -r requirements.txt -r requirements-test.txt && alembic upgrade head"` (pass), offline smoke via `./.venv-lexicon/bin/python -m tools.lexicon.cli build-base run set lead --output-dir /tmp/lexicon-working-gate-20260308`, `... enrich --snapshot-dir /tmp/lexicon-working-gate-20260308 --provider-mode placeholder`, `... validate --snapshot-dir /tmp/lexicon-working-gate-20260308`, `... compile-export --snapshot-dir /tmp/lexicon-working-gate-20260308 --output /tmp/lexicon-working-gate-20260308/words.enriched.jsonl`, `... validate --compiled-input /tmp/lexicon-working-gate-20260308/words.enriched.jsonl` (all pass; 3 lexemes / 18 senses / 18 enrichments / 3 compiled rows / 0 validation errors), real import via `docker run --rm --network host -e DATABASE_URL=postgresql+asyncpg://vocabapp:devpassword@localhost:5432/lexicon_working_gate_20260308 -e DATABASE_URL_SYNC=postgresql://vocabapp:devpassword@localhost:5432/lexicon_working_gate_20260308 -v /Users/johnson/AI/src/words-v2/.worktrees/lexicon-sense-ranking:/workspace -v /tmp:/tmp -w /workspace python:3.11-slim bash -lc "pip install -q -r backend/requirements.txt && python -m tools.lexicon.cli import-db --input /tmp/lexicon-working-gate-20260308/words.enriched.jsonl --source-type lexicon_snapshot --source-reference lexicon-working-gate-20260308 --language en"` (3 words, 18 meanings, 18 examples, 3 jobs, 18 runs), API verification via temporary backend on `:18081` plus auth/search/enrichment read returned `run` with `6` meanings, `1` example on first meaning, and `6` referenced enrichment runs, docs added at `docs/decisions/ADR-004-lexicon-canonical-final-ingestion-path.md`, `docs/runbooks/lexicon-working-gate.md`, and `docs/plans/2026-03-08-lexicon-future-improvements-todo.md`. |
| 2026-03-08 | Implemented the learner-facing lexicon schema slice on top of the working admin pipeline: new DB columns now persist word-level CEFR/POS/confusables/timestamps plus meaning-level WordNet/domain/register/grammar/usage metadata and example difficulty, `import-db` now writes those compiled learner fields through on create/update paths, and `GET /api/words/{word_id}/enrichment` now exposes the enriched learner-facing shape for inspection. This closes the focused schema/API persistence gap for compiled lexicon output while leaving broader phrase/idiom expansion and fuller review/publish unification as future work. | Codex | `/Users/johnson/AI/src/words-v2/.venv-lexicon/bin/python -m pytest tools/lexicon/tests/test_import_db.py -q` (`4` passed), `PYTHONPATH=backend /Users/johnson/AI/src/words-v2/.venv-backend/bin/python -m pytest backend/tests/test_lexicon_enrichment_models.py backend/tests/test_words.py -q` (`16` passed), migration added at `backend/alembic/versions/009_add_lexicon_learner_fields.py`, and focused implementation landed in `tools/lexicon/import_db.py`, `backend/app/models/word.py`, `backend/app/models/meaning.py`, `backend/app/models/meaning_example.py`, and `backend/app/api/words.py`. |
| 2026-03-08 | Verified the learner-facing lexicon schema slice against real local infrastructure, not just unit tests: an isolated Postgres DB was migrated to revision `009`, a real OpenAI-compatible bounded enrichment smoke (`https://api.nwai.cc`, `gpt-5.1`, Node transport, low reasoning) was compiled and imported, and the imported learner-facing fields were read back successfully through `GET /api/words/{word_id}/enrichment`. This confirms the branch’s schema/import/API wiring works end to end on a populated local backend path. | Codex | isolated DBs `lexicon_learner_schema_smoke_20260308` and `lexicon_learner_schema_real_smoke_20260308` created locally, migration via `DATABASE_URL_SYNC=postgresql://vocabapp:devpassword@localhost:5432/... /Users/johnson/AI/src/words-v2/.venv-backend/bin/python -m alembic upgrade head` (pass to `009`), placeholder smoke import via `.../.venv-lexicon/bin/python -m tools.lexicon.cli build-base/enrich/validate/compile-export` plus Docker-host import (3 words / 18 meanings / 18 examples), real smoke via `LEXICON_LLM_BASE_URL=https://api.nwai.cc LEXICON_LLM_MODEL=gpt-5.1 LEXICON_LLM_TRANSPORT=node .../.venv-lexicon/bin/python -m tools.lexicon.cli smoke-openai-compatible --provider-mode openai_compatible_node --output-dir /tmp/lexicon-real-openai-smoke-20260308 --max-words 1 --max-senses 2 --model gpt-5.1 --reasoning-effort low run` (1 word / 2 senses / 2 enrichments / 1 compiled row), real import via Docker-host path (1 word / 2 meanings / 9 examples / 27 relations / 2 enrichment runs), and real API readback returned `run` with `cefr_level=A1`, two meanings, populated confusables, and example difficulties. |
| 2026-03-08 | Completed the first real staged lexicon rollout on local infrastructure: a checkpointed 100-word batch was built from WordNet+wordfreq, enriched against the custom OpenAI-compatible gateway (`https://api.nwai.cc`, `gpt-5.1`, Node transport, low reasoning), validated/compiled per chunk, and incrementally imported into an isolated DB for API readback. The run surfaced two real operator hardening gaps in `tools/lexicon/enrich.py` (JSON-with-wrapper parsing and non-critical confidence robustness); both were fixed with regression coverage before resuming and finishing Stage 1. | Codex | parser/confidence hardening verified via `/Users/johnson/AI/src/words-v2/.venv-lexicon/bin/python -m pytest tools/lexicon/tests/test_enrich.py tools/lexicon/tests/test_import_db.py -q` (`28` passed); isolated DB `lexicon_stage100_20260308` reset + migrated to `009` via `docker exec words-postgres psql -U vocabapp -d postgres -c "DROP DATABASE IF EXISTS lexicon_stage100_20260308;"`, `... -c "CREATE DATABASE lexicon_stage100_20260308;"`, and `DATABASE_URL_SYNC=postgresql://vocabapp:devpassword@localhost:5432/lexicon_stage100_20260308 /Users/johnson/AI/src/words-v2/.venv-backend/bin/python -m alembic upgrade head` (pass); chunked real rollout via `.../.venv-lexicon/bin/python -m tools.lexicon.cli build-base --max-senses 4`, `... enrich --provider-mode openai_compatible_node --model gpt-5.1 --reasoning-effort low`, `... validate --snapshot-dir`, `... compile-export`, `... validate --compiled-input`, and backend-venv `import-db` across `chunk01`-`chunk10` (final aggregate: `100` words / `377` meanings / `1731` examples / `5491` relations / `100` enrichment jobs / `377` enrichment runs); API verification against temporary backend on `:18081` returned imported learner-facing enrichment for `run` (`A1`, `4` meanings, `4` runs in early chunk check) and `teacher` (`A1`, `2` meanings, `2` runs) from the isolated DB. |
| 2026-03-08 | Implemented the per-word concurrent lexicon enrichment path for staged rollouts: `enrich` now supports `--enrichment-unit word` plus bounded concurrency/retry/resume/force controls, writes `word_enrichments.jsonl` + `enrich.manifest.json`, `validate` understands word-mode selected senses, and `compile-export` consumes the per-word output while preserving the canonical `words.enriched.jsonl -> import-db` path. A tiny real-gateway smoke also passed on the new path, so the next rollout gate is Stage 1.5 (`100` words) before the clean-reset `vocabapp_dev` Stage 2 run. | Codex | `/Users/johnson/AI/src/words-v2/.venv-lexicon/bin/python -m pytest tools/lexicon/tests -q` (`133` passed), placeholder word-mode smoke via `build-base -> enrich --enrichment-unit word -> validate -> compile-export -> validate` on `/tmp/lexicon-per-word-smoke-*` (pass), real gateway smoke via `LEXICON_LLM_BASE_URL=https://api.nwai.cc LEXICON_LLM_MODEL=gpt-5.1 LEXICON_LLM_TRANSPORT=node ... /Users/johnson/AI/src/words-v2/.venv-lexicon/bin/python -m tools.lexicon.cli enrich --snapshot-dir /tmp/lexicon-per-word-real-smoke-*/ --provider-mode openai_compatible_node --model gpt-5.1 --reasoning-effort low --enrichment-unit word --max-concurrency 2` followed by `validate`, `compile-export`, and compiled validation (2 words / 2 `word_enrichments` / 2 compiled rows). |
| 2026-03-09 | Executed the real Stage 1.5 proving run on the new per-word path using the exact 100-word Stage 1 corpus. The per-word gateway batch completed with zero failed lexemes and produced `100` word enrichments; snapshot validation and compiled validation both stayed clean, and the compiled dry-run shape was `100` words / `478` senses / `824` examples / `3879` relations. The staged review gate also ran end to end at the artifact layer: deterministic risk scoring initially marked `61` direct review candidates and `24` rerank-recommended lexemes, bounded rerank auto-accepted `35`, and `50` lexemes still remained in `review_queue.jsonl`, so Stage 2 is not yet a clear go without review triage or more selector/risk tuning. | Codex | `/Users/johnson/AI/src/words-v2/.venv-lexicon/bin/python -m tools.lexicon.cli build-base --snapshot-id lexicon-stage15-20260309-word --output-dir /tmp/lexicon-stage15-20260309/snapshot $(cat /tmp/lexicon-stage15-20260309/words.txt)` (`100` lexemes / `481` senses / `481` concepts), real enrichment via `LEXICON_LLM_BASE_URL=https://api.nwai.cc LEXICON_LLM_MODEL=gpt-5.1 LEXICON_LLM_TRANSPORT=node ... /Users/johnson/AI/src/words-v2/.venv-lexicon/bin/python -m tools.lexicon.cli enrich --snapshot-dir /tmp/lexicon-stage15-20260309/snapshot --provider-mode openai_compatible_node --model gpt-5.1 --reasoning-effort low --enrichment-unit word --max-concurrency 4 --max-retries 1 --resume` (`100` `word_enrichments` / `0` failures), validation + compile + dry-run via `validate`, `compile-export`, `validate --compiled-input`, and `import-db --dry-run` (clean; `100` rows / `478` senses / `824` examples / `3879` relations), risk scoring via `score-selection-risk` (`61` `rerank_and_review_candidate`, `24` `rerank_recommended`, `15` `deterministic_only`), and bounded rerank via `prepare-review --candidate-source candidates --candidate-limit 8` (`35` auto-accepted / `50` still review-required). |
| 2026-03-09 | Investigated why staged review smoke failed against the local dev stack and found two separate operational blockers, not a route-definition bug: the shared `words-backend` container was mounted from a different worktree (`word-lookup-attestation`), so it returned `404` for `/api/lexicon-reviews/*`, and once a temporary backend was started from the current worktree on `127.0.0.1:18082`, `publish-preview` failed against `vocabapp_dev` because that DB is schema-drifted and missing `words.source_type`. Review import and item update now work against the current worktree backend, but Stage 2 should not reuse the current `vocabapp_dev` blindly; it needs the planned clean reset/current backend pairing first. | Codex | `docker inspect words-backend` showed mount `/Users/johnson/AI/src/words-v2/.worktrees/word-lookup-attestation/backend -> /app`, while current source still mounts the review router at `backend/app/main.py`; temporary current-worktree backend launched via `/Users/johnson/AI/src/words-v2/.venv-backend/bin/python -m alembic upgrade head` plus `uvicorn app.main:app --host 127.0.0.1 --port 18082`, review import succeeded on `POST /api/lexicon-reviews/batches/import`, item patch succeeded on `PATCH /api/lexicon-reviews/items/{item_id}`, and `GET /api/lexicon-reviews/batches/{batch_id}/publish-preview` failed with `asyncpg.exceptions.UndefinedColumnError: column words.source_type does not exist` against `vocabapp_dev`. |
| 2026-03-09 | Rebuilt the shared docker dev stack cleanly from the current lexicon worktree and proved the staged-review backend flow against the fresh database. The old `words-*` containers and attached data volumes were removed, a clean `words-v2` stack was started from `feat_lexicon_batch_rollout_plan_20260308`, migrations ran through `009`, and the review smoke then succeeded on the rebuilt backend: importing the Stage 1.5 reviewed decisions, listing the `50` flagged items, approving one pending item, and computing a publish preview that would publish `36` approved items on the clean DB. This closes the stale-backend/schema-drift blocker; the remaining Stage 2 blocker is content/review burden, not local infra correctness. | Codex | destructive reset via `docker rm -f words-playwright words-frontend words-worker words-backend words-postgres words-redis` plus volume/network removal for `word-lookup-attestation_*`, `words-v2_*`, and stale words networks; clean stack start via `docker compose -p words-v2 -f docker-compose.yml up -d --build postgres redis backend worker frontend`; migration via `docker compose -p words-v2 -f docker-compose.yml exec -T backend alembic upgrade head` (pass through `009`); rebuilt backend verification via `curl http://localhost:8000/api/health` (`ok`) and `docker inspect words-backend` showing bind mount `/Users/johnson/AI/src/words-v2/.worktrees/lexicon-batch-rollout-plan/backend -> /app`; review smoke via `POST /api/lexicon-reviews/batches/import`, `GET /api/lexicon-reviews/batches/{batch_id}/items?review_required=true`, `PATCH /api/lexicon-reviews/items/{item_id}`, and `GET /api/lexicon-reviews/batches/{batch_id}/publish-preview` on `localhost:8000` (batch `47fd68b1-3c93-407a-9ceb-631abe0eba5f`, `50` review-required, `35` auto-accepted, preview `36` publishable create-word items after one manual approval). |
| 2026-03-09 | Applied the first selector/rerank hardening pass for the Stage 1.5 holdout: deterministic scoring now penalizes sports/team-player tails, geographic/gully-region tails, abstract inquiry tails, and alias-label drift more aggressively, while causative core verbs are demoted less harshly; review prep also now blocks auto-accept when rerank introduces a new suspicious tail flag even if total suspicious-flag count stays flat. On a fresh rebuild of the same 100-word Stage 1.5 corpus, this cleaned up several known misses (`direct` dropped alias `calculate.v.05`, `field` promoted `field.v.03`, `draw` dropped `draw.n.01`/`draw.v.09`, `move` demoted the game-turn noun tail) and shifted the deterministic risk mix from `61/24/15` to `59/26/15` (`rerank_and_review_candidate` / `rerank_recommended` / `deterministic_only`). The real rerank/review rerun then improved the queue from `50` review-required / `35` auto-accepted to `46` / `39`; among the 15 high-polysemy trouble words, `direct`, `house`, `left`, and `pass` moved out of review, while `field`, `draw`, `move`, `near`, `still`, and `turn` remain useful tuning targets. Stage 2 stays on hold, but the burden is lower and the next selector pass can focus on the remaining stubborn patterns instead of the already-fixed ones. | Codex | `/Users/johnson/AI/src/words-v2/.venv-lexicon/bin/python -m pytest tools/lexicon/tests` (`136` passed), targeted selector/rerank regressions for `field`, `draw`, and suspicious-tail-introduction (pass), local rebuild via `xargs /Users/johnson/AI/src/words-v2/.venv-lexicon/bin/python -m tools.lexicon.cli build-base --output-dir /tmp/lexicon-stage15b-20260309/snapshot < /tmp/lexicon-stage15-20260309/words.txt` (`100` lexemes / `475` senses / `475` concepts), risk rerun via `/Users/johnson/AI/src/words-v2/.venv-lexicon/bin/python -m tools.lexicon.cli score-selection-risk --snapshot-dir /tmp/lexicon-stage15b-20260309/snapshot --output /tmp/lexicon-stage15b-20260309/snapshot/selection_decisions.jsonl` (`59` review-candidate / `26` rerank-recommended / `15` deterministic-only), real rerank review via `LEXICON_LLM_BASE_URL=https://api.nwai.cc`, `LEXICON_LLM_MODEL=gpt-5.1`, `LEXICON_LLM_TRANSPORT=node`, and `/Users/johnson/AI/src/words-v2/.venv-lexicon/bin/python -m tools.lexicon.cli prepare-review --snapshot-dir /tmp/lexicon-stage15b-20260309/snapshot --decisions /tmp/lexicon-stage15b-20260309/snapshot/selection_decisions.jsonl --output /tmp/lexicon-stage15b-20260309/snapshot/selection_decisions.reviewed.jsonl --review-queue-output /tmp/lexicon-stage15b-20260309/snapshot/review_queue.jsonl --provider-mode openai_compatible_node --model gpt-5.1 --reasoning-effort low --candidate-limit 8 --candidate-source candidates` (`85` reranked / `46` review-required / `39` auto-accepted), plus before/after comparison against `/tmp/lexicon-stage15-20260309/snapshot/selection_decisions.reviewed.jsonl`. |
| 2026-03-09 | Applied a second Stage 1.5 rerank-hardening pass focused on the remaining review queue: `rerank` now exposes `canonical_label` to the model, explicitly instructs exact-label preference and mixed-POS preservation, and reorders `candidates` mode toward safer exact-label non-tail senses before alias/tail candidates. On the same 100-word Stage 1.5 snapshot, the real rerank/review rerun improved the queue from `46` review-required / `39` auto-accepted to `45` / `40`, while materially reducing alias-driven regressions (`label_drift_introduced` fell from `12` in the original tuned run to `5` here). This pass also shifted several words out of review (`around`, `care`, `check`, `early`, `fair`, `late`, `light`, `mean`, `space`, `world`) but surfaced some new high-frequency-only review cases (`day`, `free`, `left`, `match`, `order`, `scale`, `watch`), so the next step should likely separate rerank-label/tail prevention from high-frequency auto-accept policy rather than widening rerank freedom further. | Codex | `/Users/johnson/AI/src/words-v2/.venv-lexicon/bin/python -m pytest tools/lexicon/tests` (`139` passed), new rerank prompt/candidate regressions in `tools/lexicon/tests/test_rerank.py` (pass), and real rerank review via `LEXICON_LLM_BASE_URL=https://api.nwai.cc`, `LEXICON_LLM_MODEL=gpt-5.1`, `LEXICON_LLM_TRANSPORT=node`, and `/Users/johnson/AI/src/words-v2/.venv-lexicon/bin/python -m tools.lexicon.cli prepare-review --snapshot-dir /tmp/lexicon-stage15b-20260309/snapshot --decisions /tmp/lexicon-stage15b-20260309/snapshot/selection_decisions.jsonl --output /tmp/lexicon-stage15c-20260309.selection_decisions.reviewed.jsonl --review-queue-output /tmp/lexicon-stage15c-20260309.review_queue.jsonl --provider-mode openai_compatible_node --model gpt-5.1 --reasoning-effort low --candidate-limit 8 --candidate-source candidates` (`85` reranked / `45` review-required / `40` auto-accepted), compared against `/tmp/lexicon-stage15b-20260309/snapshot/selection_decisions.reviewed.jsonl` with `/tmp/compare_stage15_review_runs.py`. |
| 2026-03-09 | Applied a third Stage 1.5 hardening pass that combined deterministic cleanup with a very narrow high-frequency auto-accept exception. Deterministic scoring now boosts exact-label high-frequency adverbs, demotes biographical/eponym noun glosses, and treats distillation-style noun tails as specialized; review prep now auto-accepts only a mixed-POS, exact-label, near-cutoff high-frequency reshuffle when the top two senses are preserved and no new flags are introduced. On a fresh rebuild of the same 100-word Stage 1.5 corpus, the real review queue improved from `45` review-required / `40` auto-accepted to `41` / `44`. Concrete deterministic wins on the rebuilt snapshot include `still.r.01` being selected, `low.n.02` (the person-name sense) disappearing, and `back.r.01` / `back.r.02` surfacing into the selected set; the remaining hardest deterministic words are now mainly `near` and `field`, while the remaining review-policy burden concentrates in a smaller set of high-frequency reshuffles. | Codex | `/Users/johnson/AI/src/words-v2/.venv-lexicon/bin/python -m pytest tools/lexicon/tests` (`142` passed), fresh rebuild via `xargs /Users/johnson/AI/src/words-v2/.venv-lexicon/bin/python -m tools.lexicon.cli build-base --output-dir /tmp/lexicon-stage15d-20260309/snapshot < /tmp/lexicon-stage15-20260309/words.txt` (`100` lexemes / `471` senses / `471` concepts), risk rerun via `/Users/johnson/AI/src/words-v2/.venv-lexicon/bin/python -m tools.lexicon.cli score-selection-risk --snapshot-dir /tmp/lexicon-stage15d-20260309/snapshot --output /tmp/lexicon-stage15d-20260309/snapshot/selection_decisions.jsonl` (`59` review-candidate / `26` rerank-recommended / `15` deterministic-only), and real rerank review via `LEXICON_LLM_BASE_URL=https://api.nwai.cc`, `LEXICON_LLM_MODEL=gpt-5.1`, `LEXICON_LLM_TRANSPORT=node`, and `/Users/johnson/AI/src/words-v2/.venv-lexicon/bin/python -m tools.lexicon.cli prepare-review --snapshot-dir /tmp/lexicon-stage15d-20260309/snapshot --decisions /tmp/lexicon-stage15d-20260309/snapshot/selection_decisions.jsonl --output /tmp/lexicon-stage15d-20260309/snapshot/selection_decisions.reviewed.jsonl --review-queue-output /tmp/lexicon-stage15d-20260309/snapshot/review_queue.jsonl --provider-mode openai_compatible_node --model gpt-5.1 --reasoning-effort low --candidate-limit 8 --candidate-source candidates` (`85` reranked / `41` review-required / `44` auto-accepted), compared against `/tmp/lexicon-stage15c-20260309.selection_decisions.reviewed.jsonl` with `/tmp/compare_stage15_review_runs_d.py`. |
| 2026-03-09 | Applied a fourth Stage 1.5 hardening pass that keeps a conservative stance but reduces review pain: `prepare_review` now auto-accepts only exact-label, no-flag, same-POS high-frequency reshuffles when a six-sense deterministic set keeps four of the six senses and the new senses were already near the deterministic cutoff. The latest real rerank run on `/tmp/lexicon-stage15e-20260309/snapshot` nudged the queue from `41` / `44` to `40` / `45`; replaying that saved rerank artifact through the tightened policy then reduced the queue further to `34` review-required / `51` auto-accepted, specifically removing `change`, `check`, `line`, `order`, `time`, and `watch` from human review without any new LLM calls. This leaves Stage 2 still best treated as staged rather than fully unattended, but the auto-accepted-first path is now much more practical. | Codex | `/Users/johnson/AI/src/words-v2/.venv-lexicon/bin/python -m pytest tools/lexicon/tests` (`145` passed), real stage15e artifacts at `/tmp/lexicon-stage15e-20260309/snapshot/selection_decisions.reviewed.jsonl` and `/tmp/lexicon-stage15e-20260309/snapshot/review_queue.jsonl` (`40` review-required / `45` auto-accepted across `85` reranked lexemes), and no-network policy replay via `prepare_review` patched against `/tmp/lexicon-stage15e-20260309/snapshot/sense_reranks.review.jsonl` producing `/tmp/lexicon-stage15e-20260309/snapshot/selection_decisions.reviewed.policy-replay.jsonl` plus `/tmp/lexicon-stage15e-20260309/snapshot/review_queue.policy-replay.jsonl` (`34` review-required / `51` auto-accepted). |
| 2026-03-09 | Executed the Stage 2 auto-accepted-first rollout into reset `vocabapp_dev` from the current worktree. A `1,000`-word top-frequency WordNet-backed seed produced a real review gate of `399` review-required / `313` auto-accepted / `288` deterministic-only, yielding `601` importable lexemes. Those accepted decisions were materialized into a rerank-aware published snapshot, enriched in per-word mode (`601` completed after one validator hardening fix), compiled cleanly, then filtered down to `585` imported words by removing `16` obvious short-form abbreviation/proper-name contaminants such as `it -> information technology`, `me -> Maine`, and `us -> United States`. The dev DB is now populated and verified from Postgres plus container-local app health, but Stage 2 also surfaced a concrete next fix: seed-list generation should exclude or quarantine these short-form contaminants before enrichment so we do not spend LLM calls on them. | Codex | `/Users/johnson/AI/src/words-v2/.venv-lexicon/bin/python -m pytest tools/lexicon/tests` (`146` passed), stage2 base build + risk on `/tmp/lexicon-stage2-20260309/snapshot` (`1000` lexemes / `3893` senses; `712` rerank-needed with `382` review-candidate and `330` rerank-recommended), real rerank review on `/tmp/lexicon-stage2-20260309/snapshot/selection_decisions.reviewed.jsonl` (`399` review-required / `313` auto-accepted / `288` deterministic-only), materialized publish subset at `/tmp/lexicon-stage2-20260309/published-autoaccepted` (`601` lexemes / `2189` senses / `2062` concepts), real word enrichment to `word_enrichments.jsonl` (`601` completed), compiled export validation on `words.enriched.jsonl` (pass), filtered import file `/tmp/lexicon-stage2-20260309/published-autoaccepted/words.enriched.filtered.jsonl` (`585` kept / `16` removed), real import summary (`585` words / `2111` meanings / `3595` examples / `17370` relations / `585` enrichment jobs / `2111` enrichment runs), Postgres verification via `docker compose -f docker-compose.yml exec -T postgres psql ...` (same counts), and backend health via `docker compose -f docker-compose.yml exec -T backend python ...` (`/api/health` => `status=ok`, `database=ok`, `redis=ok`). |
| 2026-03-09 | Added first-class seed hardening for staged lexicon rollouts. The new `generate-seed-words` CLI emits a reproducible WordNet-backed seed file plus optional rejected-candidate JSONL with explicit reason codes, filtering the known short-form contamination bucket before `build-base` and enrichment. A real `1,000`-word seed-generation run accepted `1000` lemmas and rejected `265`, and the previously observed short-form junk set (`it`, `an`, `me`, `who`, `us`, `mr`, `oh`, `de`, `co`, `tv`, `al`, `ok`, `uk`, `ii`, `hi`, plus one-letter fragments) was absent from the accepted output. | Codex | Focused verification via `/Users/johnson/AI/src/words-v2/.venv-lexicon/bin/python -m pytest tools/lexicon/tests/test_seed_words.py tools/lexicon/tests/test_cli.py -q` (`38` passed), real seed run via `/Users/johnson/AI/src/words-v2/.venv-lexicon/bin/python -m tools.lexicon.cli generate-seed-words --count 1000 --scan-limit 12000 --output /tmp/lexicon-stage3-seed-20260309/words.txt --rejected-output /tmp/lexicon-stage3-seed-20260309/rejected.jsonl` (`1000` accepted / `265` rejected), and accepted-output presence checks returning `False` for `it`, `an`, `me`, `who`, `us`, `mr`, `oh`, `de`, `co`, `tv`, `al`, `ok`, `uk`, `ii`, `hi`, and one-letter fragments. |
| 2026-03-09 | Hardened lexicon review preparation for larger staged runs. `prepare-review` now supports bounded rerank previews via `--max-lemmas`, resume semantics via `--resume-rerank`, and rerank checkpoint persistence/progress reporting so operators no longer need to wait for a single opaque end-written rerank pass before estimating Stage 3 review burden. No new review endpoints were required because the existing backend lexicon review API already covers the minimal import/list/items/update/publish-preview/publish path for MVP staged review. | Codex | Focused verification via `/Users/johnson/AI/src/words-v2/.venv-lexicon/bin/python -m pytest tools/lexicon/tests/test_selection_review.py tools/lexicon/tests/test_cli.py tools/lexicon/tests/test_rerank.py -q` (`57` passed), with new coverage for bounded `prepare_review()` rerank scope, CLI flag forwarding, rerank checkpoint writes, and rerank resume skipping checkpointed lemmas. |
| 2026-03-09 | Added the first in-app lexicon admin portal at `/admin/lexicon` so operators can review staged lexicon batches/items, save review decisions, preview/publish approved staged rows, import staged review JSONL into the review tables, and inspect learner-facing word enrichment already imported into the local DB. Frontend auth protection now covers `/admin/*`, navigation links to the portal, and the operator docs now make the import semantics explicit: `import-db` imports the compiled JSONL it is given and does not automatically exclude review-needed rows. | Codex | `cd frontend && npm test -- --runInBand src/lib/__tests__/lexicon-reviews-client.test.ts src/lib/__tests__/words-client.test.ts src/app/admin/lexicon/__tests__/page.test.tsx src/app/__tests__/layout-auth-nav.test.tsx src/app/__tests__/page.test.tsx src/app/review/__tests__/page.test.tsx src/lib/__tests__/api-client.auth-lifecycle.test.ts` (`7` suites / `36` tests passed), `cd frontend && npm run lint` (pass), docs updated in `tools/lexicon/OPERATOR_GUIDE.md` and `docs/plans/2026-03-09-lexicon-admin-portal-implementation.md`. |
| 2026-03-09 | No change to lexicon rollout status. Consolidated the lexicon tool testing/evaluation evidence into a single document covering automated milestones, benchmark/rerank comparisons, real Stage 1.5/2/3 runs, low-poly review-burden results, conclusions, and the release-readiness split between internal/admin alpha vs broader unattended rollout; also refreshed the future-improvements TODO list to reflect what is now implemented versus the next hardening priorities. | Codex | Documentation-only update in `docs/plans/2026-03-09-lexicon-testing-and-evaluation-summary.md` and `docs/plans/2026-03-08-lexicon-future-improvements-todo.md`; evidence sources consolidated from `docs/status/project-status.md`, `tools/lexicon/OPERATOR_GUIDE.md`, and saved real-run artifacts under `/tmp/lexicon-stage15e-20260309`, `/tmp/lexicon-stage2-20260309`, `/tmp/lexicon-stage3-20260309`, `/tmp/lexicon-stage3b-20260309`, and `/tmp/lexicon-stage3-lowpoly-20260309`. |
| 2026-03-09 | Documentation correction only: the consolidated lexicon evaluation summary now also captures the real provider/model benchmark evidence, and the live lexicon status/operator docs now distinguish the historical learner-app `/admin/lexicon` MVP from the current split-in-progress `admin-frontend` implementation. This is a docs/status alignment pass only; runtime verification for the split admin app is still pending. | Codex | Fresh lexicon-suite verification via `/Users/johnson/AI/src/words-v2/.venv-lexicon/bin/python -m pytest tools/lexicon/tests -q` (`154` passed), plus docs/status updates in `docs/plans/2026-03-09-lexicon-testing-and-evaluation-summary.md`, `docs/plans/2026-03-08-lexicon-future-improvements-todo.md`, `tools/lexicon/OPERATOR_GUIDE.md`, and `docs/status/project-status.md`. |
| 2026-03-09 | Completed the dedicated admin-frontend integration slice for lexicon operations. The admin portal now runs as its own Next app on port `3001`, CI verifies both learner and admin frontends with lint/test/build jobs, compose wiring includes the separate admin service plus backend CORS coverage for `http://admin-frontend:3001`, and the local compose stack was runtime-smoked successfully (`/api/health` returned OK and `http://localhost:3001/login` returned `200`). Historical learner-app `/admin/lexicon` references remain only as evidence of the earlier MVP. | Codex | `npm --prefix admin-frontend run lint` (pass), `npm --prefix admin-frontend test -- --runInBand` (`7` suites / `26` tests passed), `NEXT_PUBLIC_API_URL=http://backend:8000/api npm --prefix admin-frontend run build` (pass), `npm --prefix frontend run lint` (pass), `npm --prefix frontend test -- --runInBand` (`9` suites / `36` tests passed), `NEXT_PUBLIC_API_URL=http://backend:8000/api npm --prefix frontend run build` (pass), `ruby -e 'require "yaml"; YAML.load_file(".github/workflows/ci.yml"); puts "ci.yml OK"'` (pass), `docker compose -f docker-compose.yml config` (pass), and local runtime smoke via `docker compose -f docker-compose.yml up -d --build postgres redis backend admin-frontend && docker compose -f docker-compose.yml exec -T backend alembic upgrade head` with `curl http://localhost:8000/api/health` (`status=ok`) and `curl -I http://localhost:3001/login` (`HTTP/1.1 200 OK`). |
| 2026-03-08 | Clarified the pre-prod governance split: the existing GitHub Actions `preprod-readiness` workflow is now documented as a disposable rehearsal against an ephemeral Docker stack, while a separate real pre-prod verification path is defined for the actual deployed environment and persistent populated DB. The runbooks now explicitly call out where lexicon-specific bounded smoke imports belong in real pre-prod verification rather than in ordinary PR CI or disposable rehearsal runs. | Codex | Workflow wording updated in `.github/workflows/preprod-readiness.yml`, checklist clarified in `docs/runbooks/preprod-readiness-checklist.md`, real-environment runbook added at `docs/runbooks/real-preprod-verification.md`, and release-promotion links updated in `docs/runbooks/release-promotion.md`. |
| 2026-03-09 | Added the first admin-specific Playwright smoke for the split operator app. The smoke suite now boots `admin-frontend` alongside the learner app, verifies unauthenticated `/lexicon` redirects to `/login?next=%2Flexicon`, verifies real login reaches the admin dashboard and lexicon shell, and keeps the same smoke command path for local and CI runs. The client-side auth-failure redirect helper was also hardened so `next` is preserved even when the fallback redirect path is triggered after hydration. | Codex | red-green verification via `npm --prefix admin-frontend test -- --runInBand admin-frontend/src/lib/__tests__/auth-redirect.test.ts` (first failed, then passed), targeted admin smoke via `docker compose -f docker-compose.yml --profile tests up -d --build postgres redis backend worker frontend admin-frontend playwright` plus `npx playwright test tests/smoke/admin-auth.smoke.spec.ts --project=chromium` (`2` passed), full local smoke via `E2E_SMOKE_CLEANUP=1 ./e2e/scripts/run-local-smoke.sh` (`9` smoke tests passed), plus fresh admin app regression checks: `npm --prefix admin-frontend run lint` (pass), `npm --prefix admin-frontend test -- --runInBand` (`8` suites / `28` tests passed), `NEXT_PUBLIC_API_URL=http://backend:8000/api npm --prefix admin-frontend run build` (pass), `ruby -e 'require "yaml"; YAML.load_file(".github/workflows/ci.yml"); puts "ci.yml OK"'` (pass), and `docker compose -f docker-compose.yml config` (pass). |
| 2026-03-09 | Expanded the admin Playwright smoke from shell/auth checks into a real staged-review interaction. The admin smoke suite now uploads a minimal `selection_decisions.jsonl` batch through the real UI, approves the imported item, saves the review decision, and verifies persisted review state via the review-items API; the full smoke pack remains green with the admin app included in the test stack. | Codex | targeted review-flow smoke via `docker compose -f docker-compose.yml --profile tests up -d --build postgres redis backend worker frontend admin-frontend playwright` plus `npx playwright test tests/smoke/admin-review-flow.smoke.spec.ts --project=chromium` (`1` passed), full local smoke via `E2E_SMOKE_CLEANUP=1 ./e2e/scripts/run-local-smoke.sh` (`10` smoke tests passed), and the existing admin redirect-helper regression remains green in `admin-frontend/src/lib/__tests__/auth-redirect.test.ts`. |
| 2026-03-09 | Expanded the preprod readiness gate so the workflow now starts `admin-frontend`, waits for learner + admin frontend readiness, passes `E2E_ADMIN_URL` into the Playwright smoke container, and aligns the preprod checklist wording with the dual-frontend stack. | Codex | Local no-network verification recorded in this change set: workflow YAML parse, `docker compose -f docker-compose.yml --profile tests config --services`, and targeted `rg` assertions against the updated workflow/runbook. |
| 2026-03-09 | Completed the admin hardening slice for the split lexicon operator app. Lexicon-review APIs now require admin role, the admin frontend adds a client-side auth guard as defense in depth, the staged-review smoke now covers import -> approve -> publish-preview -> publish, and the preprod readiness workflow now boots/tests `admin-frontend` with the same dual-frontend URLs used locally. | Codex | `docker compose -f docker-compose.yml exec -T backend pip install --no-cache-dir -r requirements-test.txt` (pass), `docker compose -f docker-compose.yml exec -T backend pytest tests/test_lexicon_reviews_api.py` (`21` passed), `npm --prefix admin-frontend test -- --runInBand` (`8` suites / `28` tests passed), `npm --prefix admin-frontend run lint` (pass), `NEXT_PUBLIC_API_URL=http://backend:8000/api npm --prefix admin-frontend run build` (pass), targeted admin smoke via Playwright container (`admin-auth` + `admin-review-flow`, pass), full local smoke via `E2E_SMOKE_CLEANUP=1 NEXT_PUBLIC_API_URL=http://backend:8000/api ALLOWED_ORIGINS=http://localhost:3000,http://localhost:3001,http://frontend:3000,http://admin-frontend:3001 ./e2e/scripts/run-local-smoke.sh` (`10` passed), `docker compose -f docker-compose.yml --profile tests config` (pass), and `ruby -e 'require "yaml"; YAML.load_file(".github/workflows/ci.yml"); YAML.load_file(".github/workflows/preprod-readiness.yml"); puts "workflow yaml OK"'` (pass). |
| 2026-03-10 | Completed the lexicon per-word enrichment and review-usability slice. The lexicon CLI now supports `enrich --mode per_word --max-concurrency N` for one LLM call per lexeme with bounded parallelism, `compile-export --decisions ... --decision-filter mode_c_safe` for direct safe-only exports after review prep, and the admin review API/UI now expose reviewer-readable selected-sense/source/candidate detail instead of forcing raw metadata inspection. Added small `.dockerignore` files for app build contexts so local smoke builds do not drag repo-local `node_modules` into Docker images. | Codex | `../../.venv-lexicon/bin/python -m pytest tools/lexicon/tests -q` (`130` passed), `PYTHONPATH=backend ../../.venv-backend/bin/python -m pytest backend/tests/test_lexicon_reviews_api.py -q` (`21` passed), `npm --prefix admin-frontend test -- --runInBand` (`8` suites / `28` tests passed), `npm --prefix admin-frontend run lint` (pass), `NEXT_PUBLIC_API_URL=http://backend:8000/api npm --prefix admin-frontend run build` (pass), `ruby -e 'require "yaml"; YAML.load_file(".github/workflows/ci.yml"); puts "ci.yml OK"'` (pass), `docker compose -f docker-compose.yml --profile tests config` (pass), and targeted admin smoke via clean local Docker stack plus `docker compose -f docker-compose.yml exec -T backend alembic upgrade head` and `docker compose -f docker-compose.yml exec -T playwright sh -lc "cd /workspace/e2e && E2E_BASE_URL=http://frontend:3000 E2E_API_URL=http://backend:8000/api E2E_ADMIN_URL=http://admin-frontend:3001 npm exec playwright test tests/smoke/admin-review-flow.smoke.spec.ts --project=chromium"` (`1` passed). |
| 2026-03-10 | Completed a small post-merge hardening follow-up for the lexicon review/export slice. Local `main` was safely reconciled by preserving the pre-sync dirty checkout on `backup/main-dirty-20260310` plus `/tmp/words-v2-dirty-main-backup.patch`, then the code path was hardened so `compile-export` coerces boolean-like decision values safely, rejects `--decisions` without `--decision-filter`, and the review API/publish-preview paths preserve compatibility with legacy staged candidate metadata that only had `label` / `gloss`. | Codex | `../../.venv-lexicon/bin/python -m pytest tools/lexicon/tests/test_compile_export.py tools/lexicon/tests/test_cli.py -q` (`41` passed), `PYTHONPATH=backend ../../.venv-backend/bin/python -m pytest backend/tests/test_lexicon_reviews_api.py -q` (`23` passed), and `../../.venv-lexicon/bin/python -m pytest tools/lexicon/tests -q` (`132` passed). |
| 2026-03-10 | Added two stable local-development test users for manual QA and admin/frontend testing: `admin@admin.com` / `12345678` (admin role) and `user@user.com` / `12345678` (user role). The backend now supports explicit dev-only seeding behind `DEV_TEST_USERS_ENABLED`, the local Docker backend enables it by default, and seeding happens lazily on the first request so a fresh pre-migration DB does not break backend startup. | Codex | `PYTHONPATH=backend ../../.venv-backend/bin/python -m pytest backend/tests/test_config.py backend/tests/test_dev_test_users.py backend/tests/test_auth.py -q` (`22` passed), plus live Docker verification on this branch: `POST /api/auth/login` and `GET /api/auth/me` both passed for `admin@admin.com` and `user@user.com` with expected roles. |
| 2026-03-10 | Finalized the salvaged admin lexicon review usability slice as a fresh integrated PR branch from current `main`. The admin review API now normalizes legacy and richer staged candidate metadata into stable reviewer-facing `candidate_entries` without losing selected override senses, the split admin UI renders clearer selected-source/candidate detail, and the targeted admin smoke now drives the real UI for save + preview + publish instead of depending on a transient confirmation toast. | Codex | `PYTHONPYCACHEPREFIX=/tmp/pycache-admin-review PYTHONPATH=backend ../../.venv-backend/bin/python -m pytest backend/tests/test_lexicon_reviews_api.py -q` (`25` passed), `npm --prefix admin-frontend test -- --runInBand src/app/lexicon/__tests__/page.test.tsx` (`7` passed), `npm --prefix admin-frontend test -- --runInBand` (`8` suites / `28` tests passed), `npm --prefix admin-frontend run lint` (pass), `NEXT_PUBLIC_API_URL=http://backend:8000/api npm --prefix admin-frontend run build` (pass), and targeted Docker smoke via `docker compose -f docker-compose.yml --profile tests up -d --build postgres redis backend worker frontend admin-frontend playwright`, `docker compose -f docker-compose.yml exec -T backend alembic upgrade head`, and `docker compose -f docker-compose.yml exec -T playwright sh -lc "cd /workspace/e2e && E2E_BASE_URL=http://frontend:3000 E2E_API_URL=http://backend:8000/api E2E_ADMIN_URL=http://admin-frontend:3001 npm exec playwright test tests/smoke/admin-review-flow.smoke.spec.ts --project=chromium"` (`1` passed). |
| 2026-03-12 | Added a separate read-only Lexicon Ops admin slice for inspecting offline snapshot runs without mixing operator tools into the learner frontend. Backend now exposes admin-only snapshot listing/detail endpoints under `/api/lexicon-ops`, while the admin frontend adds `/lexicon/ops` with snapshot status, artifact counts, and tracked-file detail for rollout monitoring. This keeps the CLI as the execution engine but gives operators a first-class monitoring surface for snapshot/artifact state. | Codex | `PYTHONPATH=backend /Users/johnson/AI/src/words-v2/.venv-backend/bin/python -m pytest backend/tests/test_lexicon_ops_api.py -q` (`6` passed), `npm --prefix admin-frontend test -- --runInBand src/app/lexicon/ops/__tests__/page.test.tsx src/app/__tests__/layout-auth-nav.test.tsx` (`6` passed), `npm --prefix admin-frontend run lint` (pass), and `NEXT_PUBLIC_API_URL=http://backend:8000/api npm --prefix admin-frontend run build` (pass). |
| 2026-03-12 | Added DB-backed pre-generation skip behavior for the lexicon words pipeline. `build-base` now bulk-checks canonical headwords against the local word DB when `--database-url` or `DATABASE_URL_SYNC` is configured and skips already-published words from new base/enrichment work while still recording them in snapshot metadata as published+skipped; operators can force regeneration with `--rerun-existing` and optionally override the sync DB URL with `--database-url`. This makes staged top-word rollouts cheaper and lets the DB act as the persistent global truth for already-published headwords instead of relying only on the current snapshot folder, without breaking offline/CI builds that have no DB configured. | Codex | `/Users/johnson/AI/src/words-v2/.venv-lexicon/bin/python -m pytest tools/lexicon/tests -q` (`172` passed), including new coverage in `tools/lexicon/tests/test_build_base.py` and `tools/lexicon/tests/test_cli.py` for canonical-word DB skip behavior, no-DB fallback behavior, and `--rerun-existing` CLI override. |
| 2026-03-12 | Hardened the lexicon per-word enrichment transport/validation path for real 1K rollouts. The CLI now supports configurable LLM timeouts, retries transient timeout/non-JSON word-generation failures before giving up, and consistently wraps malformed extracted JSON as retryable runtime errors instead of leaking raw `JSONDecodeError`s. Prompt instructions were also tightened around required translation locales and translated-example count alignment. During live rollout recovery on `words-1000-20260312-adjudicated`, the resumed run recovered previously stalled words and advanced from `169` completed lexemes / `25` failures to at least `217` completed lexemes with the failure sidecar cleared before the next active batch continued. | Codex | `/Users/johnson/AI/src/words-v2/.venv-lexicon/bin/python -m pytest tools/lexicon/tests/test_config.py tools/lexicon/tests/test_enrich.py tools/lexicon/tests/test_cli.py tools/lexicon/tests/test_translations_pipeline.py -q` (`90` passed), plus live artifact evidence from `data/lexicon/snapshots/words-1000-20260312-adjudicated/` showing checkpoint growth from `169` to `217` completed lexemes and resumed completion of formerly failed items such as `damn`, `dead`, `do`, `down`, `event`, and `enough`. |
| 2026-03-22 | Added shared post-normalization review-prep parity for realtime and batch lexicon artifacts. `tools.lexicon.review_prep` now computes the deterministic warning labels, review priority, reviewer summary, and review queue shape for normalized `word`, `phrase`, and `reference` rows; batch QC routes through that helper, `compile-export` now writes artifact-specific sidecars such as `words.enriched.review_qc.jsonl` and `words.enriched.review_queue.jsonl`, and the JSONL-review backend consumes the same shared contract instead of duplicating warning heuristics. Realtime keeps its immediate schema validation, but once a compiled row exists it now goes through the same review-prep layer that batch uses before human review. | Codex | `/Users/johnson/AI/src/words-v2/.venv-lexicon/bin/python -m pytest tools/lexicon/tests/test_review_prep.py tools/lexicon/tests/test_qc.py tools/lexicon/tests/test_batch_lifecycle.py tools/lexicon/tests/test_compile_export.py tools/lexicon/tests/test_validate.py tools/lexicon/tests/test_review_materialize.py tools/lexicon/tests/test_cli.py -q` (pass), `PYTHONPATH=backend /Users/johnson/AI/src/words-v2/.venv-backend/bin/python -m pytest backend/tests/test_lexicon_jsonl_reviews_api.py -q` (pass), `NODE_PATH=/Users/johnson/AI/src/words-v2/admin-frontend/node_modules PATH=/Users/johnson/AI/src/words-v2/admin-frontend/node_modules/.bin:$PATH npm --prefix admin-frontend test -- --runInBand src/app/lexicon/jsonl-review/__tests__/page.test.tsx` (pass), `PYTHONPATH=backend /Users/johnson/AI/src/words-v2/.venv-backend/bin/python -m py_compile backend/app/services/lexicon_jsonl_reviews.py` (pass). |
| 2026-03-22 | Streamlined the admin lexicon workflow shell around snapshot-derived workflow metadata. `/api/lexicon-ops` now returns stage, recommended action, preferred review/import artifact paths, and outside-portal step guidance; `/lexicon/ops` renders that metadata as the canonical workflow shell instead of a plain launcher; the standalone review/import/inspection pages now show workflow context and next-step framing; `/lexicon/compiled-review` now supports import-by-path for an existing compiled artifact selected from `Lexicon Ops`, removing the need to re-upload files that already exist on disk; the Ops handoff now carries real query params reliably, auto-imports the selected artifact into Compiled Review staging, auto-loads JSONL Review, auto-runs Import DB dry-run while keeping final import explicit, shows snapshot/source-reference metadata in Compiled Review batch cards, and disables review/import actions entirely when the required compiled or approved artifacts are not present so operators are not sent into empty downstream pages; and the admin frontend now uses same-origin `/api` plus server-side `BACKEND_URL` proxying so manual browser use and Docker-internal Playwright no longer require flipping `NEXT_PUBLIC_API_URL` between `localhost` and `backend`. | Codex | `PYTHONPATH=backend /Users/johnson/AI/src/words-v2/.venv-backend/bin/python -m pytest backend/tests/test_lexicon_ops_api.py backend/tests/test_lexicon_compiled_reviews_api.py -q` (`16` passed), `NODE_PATH=/Users/johnson/AI/src/words-v2/admin-frontend/node_modules PATH=/Users/johnson/AI/src/words-v2/admin-frontend/node_modules/.bin:$PATH npm --prefix admin-frontend test -- --runInBand src/app/lexicon/ops/__tests__/page.test.tsx src/app/lexicon/compiled-review/__tests__/page.test.tsx src/app/lexicon/jsonl-review/__tests__/page.test.tsx src/app/lexicon/import-db/__tests__/page.test.tsx src/app/lexicon/db-inspector/__tests__/page.test.tsx src/app/__tests__/page.test.tsx` (`6` suites / `16` tests passed), `NODE_PATH=/Users/johnson/AI/src/words-v2/admin-frontend/node_modules PATH=/Users/johnson/AI/src/words-v2/admin-frontend/node_modules/.bin:$PATH npm --prefix admin-frontend test -- --runInBand src/app/lexicon/ops/__tests__/page.test.tsx src/app/lexicon/compiled-review/__tests__/page.test.tsx src/app/lexicon/jsonl-review/__tests__/page.test.tsx src/app/lexicon/import-db/__tests__/page.test.tsx` (`4` suites / `11` tests passed), `npm --prefix admin-frontend run lint` (pass), `NEXT_PUBLIC_API_URL=/api BACKEND_URL=http://backend:8000/api npm --prefix admin-frontend run build` (pass), `PYTHONPATH=backend /Users/johnson/AI/src/words-v2/.venv-backend/bin/python -m ruff check backend/app/api/lexicon_compiled_reviews.py` (pass), and `docker compose -f docker-compose.yml exec -T playwright sh -lc "cd /workspace/e2e && ./node_modules/.bin/playwright test tests/smoke/admin-compiled-review-flow.smoke.spec.ts tests/smoke/admin-jsonl-review-flow.smoke.spec.ts tests/smoke/admin-lexicon-ops-import-flow.smoke.spec.ts --project=chromium"` (`3` passed). |
| 2026-03-22 | Clarified the reviewed-artifact contract across `/lexicon/compiled-review`, `/lexicon/jsonl-review`, and `/lexicon/import-db`. The admin UI now states explicitly that `words.enriched.jsonl` is the pre-review compiled artifact, `approved.jsonl` is the reviewed output for final DB import, `review.decisions.jsonl` is the decision ledger, and `regenerate.jsonl` is derived from rejected rows rather than a separate review status. Compiled Review export actions now use descriptive labels and download snapshot/source-aware filenames, JSONL Review now explains the same output mapping when materializing files, and Import DB now points operators at `approved.jsonl` instead of the raw compiled artifact by default. | Codex | `NODE_PATH=/Users/johnson/AI/src/words-v2/admin-frontend/node_modules PATH=/Users/johnson/AI/src/words-v2/admin-frontend/node_modules/.bin:$PATH npm --prefix admin-frontend test -- --runInBand src/app/lexicon/compiled-review/__tests__/page.test.tsx src/app/lexicon/jsonl-review/__tests__/page.test.tsx src/app/lexicon/import-db/__tests__/page.test.tsx` (`3` suites / `3` tests passed). |
| 2026-03-22 | Unified the word-level lexicon finalize contract across realtime and batch. Realtime `enrich` now defaults to `per_word`, reads `lexemes.jsonl` without requiring `senses.jsonl`, validates/materializes accepted word rows immediately, and writes final `words.enriched.jsonl` directly while preserving `enrich.checkpoint.jsonl`, `enrich.decisions.jsonl`, and `enrich.failures.jsonl` for resume. Batch ingest now records request bodies in `batch_results.jsonl`, uses the same word-level payload validation/materialization path for accepted `word` rows, appends those accepted rows directly to `words.enriched.jsonl`, and writes failed materializations to `words.regenerate.jsonl` for human rerun decisions. The lexicon docs now describe realtime as a direct-to-compiled path and batch as a later materialization path into the same review artifact. Follow-up hardening switched the Python realtime/batch path to the official OpenAI SDK, kept the Node path on the official OpenAI SDK, removed schema downgrades for schema-backed enrichment calls so structured outputs now stay `json_schema`-strict instead of degrading to weaker JSON modes, and set the default lexicon reasoning effort to explicit `none`. The smoke command also no longer tries to recompile missing legacy `enrichments.jsonl` after a successful realtime run. | Codex | `/Users/johnson/AI/src/words-v2/.venv-lexicon/bin/python -m pytest tools/lexicon/tests -q` (`316` passed), `/Users/johnson/AI/src/words-v2/.venv-lexicon/bin/python -m pytest tools/lexicon/tests/test_config.py tools/lexicon/tests/test_enrich.py tools/lexicon/tests/test_cli.py -q` (`124` passed), `/Users/johnson/AI/src/words-v2/.venv-lexicon/bin/python -m py_compile tools/lexicon/config.py tools/lexicon/enrich.py tools/lexicon/form_adjudication.py tools/lexicon/batch_ingest.py tools/lexicon/compile_export.py tools/lexicon/cli.py` (pass), live non-node smoke via `LEXICON_LLM_TIMEOUT_SECONDS=180 /Users/johnson/AI/src/words-v2/.venv-lexicon/bin/python -m tools.lexicon.cli smoke-openai-compatible --output-dir /tmp/lexicon-realtime-smoke-20260322-nonnode-pass --max-words 1 --provider-mode openai_compatible --model "$LEXICON_LLM_MODEL" --reasoning-effort low run` (pass; `compiled_count=1`), live node smoke via `LEXICON_LLM_TIMEOUT_SECONDS=180 /Users/johnson/AI/src/words-v2/.venv-lexicon/bin/python -m tools.lexicon.cli smoke-openai-compatible --output-dir /tmp/lexicon-realtime-smoke-20260322-node-pass --max-words 1 --provider-mode openai_compatible_node --model "$LEXICON_LLM_MODEL" --reasoning-effort low run` (pass; `compiled_count=1`), live non-node smoke with explicit `none` via `LEXICON_LLM_TIMEOUT_SECONDS=180 /Users/johnson/AI/src/words-v2/.venv-lexicon/bin/python -m tools.lexicon.cli smoke-openai-compatible --output-dir /tmp/lexicon-realtime-none-python-20260322 --max-words 1 --provider-mode openai_compatible --model "$LEXICON_LLM_MODEL" --reasoning-effort none run` (pass; `compiled_count=1`), live node smoke with explicit `none` via `LEXICON_LLM_TIMEOUT_SECONDS=180 /Users/johnson/AI/src/words-v2/.venv-lexicon/bin/python -m tools.lexicon.cli smoke-openai-compatible --output-dir /tmp/lexicon-realtime-none-node-20260322 --max-words 1 --provider-mode openai_compatible_node --model "$LEXICON_LLM_MODEL" --reasoning-effort none run` (pass; `compiled_count=1`), and official OpenAI strict-schema batch-style smoke via `BATCH_LLM_*` against `gpt-5.4-mini` writing accepted output to `/tmp/lexicon-batch-live-smoke-20260322f/words.enriched.jsonl` with no regenerate row (pass). |
| 2026-03-22 | Unified reviewed-output handling across Compiled Review, JSONL Review, Lexicon Ops, and Import DB under a shared `reviewed/` snapshot subdirectory. `Lexicon Ops` now tracks `reviewed/approved.jsonl`, `reviewed/review.decisions.jsonl`, `reviewed/rejected.jsonl`, and `reviewed/regenerate.jsonl`; JSONL Review defaults its sidecar and materialize paths into that directory and can now download the same four artifacts it materializes; Compiled Review keeps its download exports and now also materializes the same artifact set into `reviewed/`; and Import DB plus Ops now consistently prefer `reviewed/approved.jsonl` as the final reviewed input instead of looking at root-level review outputs. | Codex | `PYTHONPATH=backend /Users/johnson/AI/src/words-v2/.venv-backend/bin/python -m pytest backend/tests/test_lexicon_jsonl_reviews_api.py backend/tests/test_lexicon_ops_api.py backend/tests/test_lexicon_compiled_reviews_api.py -q` (`30` passed), `npm --prefix admin-frontend test -- --runInBand src/app/lexicon/jsonl-review/__tests__/page.test.tsx src/app/lexicon/compiled-review/__tests__/page.test.tsx src/app/lexicon/ops/__tests__/page.test.tsx src/app/lexicon/import-db/__tests__/page.test.tsx` (`4` suites / `11` tests passed), `npm --prefix admin-frontend run lint` (pass), `NEXT_PUBLIC_API_URL=/api BACKEND_URL=http://backend:8000/api npm --prefix admin-frontend run build` (pass), `PYTHONPATH=backend /Users/johnson/AI/src/words-v2/.venv-backend/bin/python -m py_compile backend/app/api/lexicon_ops.py backend/app/services/lexicon_jsonl_reviews.py backend/app/api/lexicon_jsonl_reviews.py backend/app/api/lexicon_compiled_reviews.py backend/tests/test_lexicon_jsonl_reviews_api.py backend/tests/test_lexicon_ops_api.py backend/tests/test_lexicon_compiled_reviews_api.py` (pass), `git diff --check` (pass), and Docker smoke via `docker compose -f /Users/johnson/AI/src/words-v2/.worktrees/feat_lexicon_review_output_clarity_20260322/docker-compose.yml exec -T backend alembic upgrade head` plus `docker compose -f /Users/johnson/AI/src/words-v2/.worktrees/feat_lexicon_review_output_clarity_20260322/docker-compose.yml exec -T playwright sh -lc "cd /workspace/e2e && E2E_BASE_URL=http://frontend:3000 E2E_API_URL=http://backend:8000/api E2E_ADMIN_URL=http://admin-frontend:3001 npx playwright test tests/smoke/admin-compiled-review-flow.smoke.spec.ts tests/smoke/admin-jsonl-review-flow.smoke.spec.ts tests/smoke/admin-lexicon-ops-import-flow.smoke.spec.ts --project=chromium"` (`3` passed). |
| 2026-03-22 | Extended the strict word-enrichment contract to require grouped accent phonetics at `phonetics.us`, `phonetics.uk`, and `phonetics.au` across realtime and batch. The word schema, payload validator, per-word runtime, compiled export, compiled validation, and batch materialization now all preserve grouped phonetics in `words.enriched.jsonl`; `import-db` remains backward compatible with the current single-phonetic DB model by selecting a stable preferred accent order `us -> uk -> au` into the legacy `words.phonetic` fields while keeping grouped phonetics in the review artifact. | Codex | `/Users/johnson/AI/src/words-v2/.venv-lexicon/bin/python -m pytest tools/lexicon/tests -q` (`316` passed), `/Users/johnson/AI/src/words-v2/.venv-lexicon/bin/python -m py_compile tools/lexicon/schemas/word_enrichment_schema.py tools/lexicon/models.py tools/lexicon/compile_export.py tools/lexicon/validate.py tools/lexicon/import_db.py tools/lexicon/enrich.py` (pass), and live realtime smoke against `/Users/johnson/AI/src/words-v2/tools/lexicon/.env.local` via `LEXICON_LLM_TRANSPORT=python LEXICON_LLM_REASONING_EFFORT=none LEXICON_LLM_TIMEOUT_SECONDS=180 /Users/johnson/AI/src/words-v2/.venv-lexicon/bin/python -m tools.lexicon.cli smoke-openai-compatible --output-dir /tmp/lexicon-realtime-phonetics-smoke-20260322 --provider-mode openai_compatible` (pass; `compiled_count=1`, output at `/tmp/lexicon-realtime-phonetics-smoke-20260322/words.enriched.jsonl` includes grouped `phonetics.us/uk/au`). |
| 2026-03-22 | Extended backend lexicon storage and read APIs to keep grouped accent phonetics instead of only the collapsed legacy string. `Word` now has additive JSON storage for grouped `phonetics`, Alembic revision `013` adds the new column under the `lexicon` schema, the lexicon importer now writes grouped phonetics into backend models while still preserving the legacy single-phonetic compatibility fields, and `/api/words/search`, `/api/words/{word_id}`, `/api/words/lookup`, and `/api/words/{word_id}/enrichment` now expose grouped `phonetics` alongside the older `phonetic` field. | Codex | `PYTHONPATH=backend /Users/johnson/AI/src/words-v2/.venv-backend/bin/python -m pytest backend/tests/test_models.py backend/tests/test_lexicon_enrichment_models.py backend/tests/test_words.py -q` (`33` passed), `/Users/johnson/AI/src/words-v2/.venv-lexicon/bin/python -m pytest tools/lexicon/tests/test_import_db.py -q` (`8` passed), `/Users/johnson/AI/src/words-v2/.venv-lexicon/bin/python -m pytest tools/lexicon/tests -q` (`316` passed), `PYTHONPATH=backend /Users/johnson/AI/src/words-v2/.venv-backend/bin/python -m py_compile backend/app/models/word.py backend/app/api/words.py backend/alembic/versions/013_add_word_grouped_phonetics.py` (pass), local DB migration via `DATABASE_URL_SYNC=postgresql://vocabapp:change_this_password_in_production@localhost:5432/vocabapp_dev PYTHONPATH=. /Users/johnson/AI/src/words-v2/.venv-backend/bin/python -m alembic upgrade head` from `backend/` (pass), one-word backend import smoke via `DATABASE_URL=postgresql+asyncpg://vocabapp:change_this_password_in_production@localhost:5432/vocabapp_dev DATABASE_URL_SYNC=postgresql://vocabapp:change_this_password_in_production@localhost:5432/vocabapp_dev /Users/johnson/AI/src/words-v2/.venv-lexicon/bin/python -m tools.lexicon.cli import-db --input /tmp/lexicon-realtime-phonetics-smoke-20260322/words.enriched.jsonl --source-reference lexicon-realtime-phonetics-smoke-20260322` (pass; updated existing `run` row), and DB verification via `docker compose exec -T postgres psql -U vocabapp -d vocabapp_dev -c "select word, phonetic, phonetics::text, phonetic_source, phonetic_confidence from lexicon.words where word = 'run';"` (pass; grouped `phonetics` JSON stored on `lexicon.words`). |
| 2026-03-22 | Aligned the JSONL Review interaction model with Compiled Review so operators now use the same staged decision flow in both tools: approve/reject/reopen actions expose explicit confirmation controls, shortcut hints now include confirm/cancel (`enter` / `esc`), and both review modes automatically advance to the next pending row after a confirmed decision instead of leaving the reviewer on a completed item. JSONL Review also drops the more decorative standalone styling in favor of the same plain review-console structure used by Compiled Review so the two pages feel like the same workflow with different storage backends. | Codex | `NODE_PATH=/Users/johnson/AI/src/words-v2/admin-frontend/node_modules PATH=/Users/johnson/AI/src/words-v2/admin-frontend/node_modules/.bin:$PATH npm --prefix admin-frontend test -- --runInBand src/app/lexicon/jsonl-review/__tests__/page.test.tsx src/app/lexicon/compiled-review/__tests__/page.test.tsx` (`2` suites / `2` tests passed), `npm --prefix admin-frontend run lint` (pass), `NEXT_PUBLIC_API_URL=/api BACKEND_URL=http://backend:8000/api npm --prefix admin-frontend run build` (pass), `git diff --check` (pass), `docker compose -f /Users/johnson/AI/src/words-v2/.worktrees/feat_lexicon_review_ui_alignment_20260322/docker-compose.yml exec -T backend alembic upgrade head` (pass), and `docker compose -f /Users/johnson/AI/src/words-v2/.worktrees/feat_lexicon_review_ui_alignment_20260322/docker-compose.yml exec -T playwright sh -lc "cd /workspace/e2e && npm ci && E2E_BASE_URL=http://frontend:3000 E2E_API_URL=http://backend:8000/api E2E_ADMIN_URL=http://admin-frontend:3001 npx playwright test tests/smoke/admin-compiled-review-flow.smoke.spec.ts tests/smoke/admin-jsonl-review-flow.smoke.spec.ts --project=chromium"` (`2` passed). |
| 2026-03-22 | Hardened the verification matrix around the new lexicon contract. The offline CLI regression suite now covers the direct realtime `words.enriched.jsonl` smoke path without a legacy compile step, CI’s lexicon smoke job now asserts grouped `phonetics.us/uk/au` directly from the generated artifact, and the Playwright admin import smoke now verifies that a reviewed import carrying grouped phonetics is exposed through the backend word enrichment API after DB import. | Codex | `/Users/johnson/AI/src/words-v2/.venv-lexicon/bin/python -m pytest tools/lexicon/tests/test_cli.py tools/lexicon/tests/test_unified_enrichment_flow.py -q` (`50` passed), `PYTHONPATH=backend /Users/johnson/AI/src/words-v2/.venv-backend/bin/python -m pytest backend/tests/test_words.py -q` (`10` passed), `/Users/johnson/AI/src/words-v2/.venv-lexicon/bin/python -m pytest tools/lexicon/tests -q` (`317` passed), `PYTHONPATH=backend /Users/johnson/AI/src/words-v2/.venv-backend/bin/python -m pytest backend/tests/test_words.py backend/tests/test_models.py backend/tests/test_lexicon_enrichment_models.py -q` (`33` passed), and `npm ci --prefix e2e` plus `./e2e/node_modules/.bin/playwright test e2e/tests/smoke/admin-lexicon-ops-import-flow.smoke.spec.ts --list` (pass; 1 test discovered). A full live local Playwright run remains blocked by Docker Desktop failing to start containers on this machine (`Docker Desktop is unable to start` after the daemon hit an input/output error while creating `words-frontend`). |
| 2026-03-22 | Completed the lexicon verification hardening follow-up. The manual/nightly OpenAI-compatible smoke workflow now enforces the same grouped `phonetics.us/uk/au` artifact contract as CI, and the targeted Playwright smoke for the admin Ops -> Import DB -> DB Inspector path passed against the worktree Docker stack with grouped phonetics preserved through import and exposed by the backend API. | Codex | `ruby -e 'require "yaml"; YAML.load_file(".github/workflows/ci.yml"); YAML.load_file(".github/workflows/lexicon-openai-compatible-smoke.yml"); puts "ok"'` (pass), `cd /Users/johnson/AI/src/words-v2/.worktrees/feat_lexicon_realtime_batch_unification_20260322 && docker compose -f docker-compose.yml exec -T backend alembic upgrade head` (pass), and `cd /Users/johnson/AI/src/words-v2/.worktrees/feat_lexicon_realtime_batch_unification_20260322 && docker compose -f docker-compose.yml exec -T playwright sh -lc "cd /workspace/e2e && E2E_BASE_URL=http://frontend:3000 E2E_API_URL=http://backend:8000/api E2E_ADMIN_URL=http://admin-frontend:3001 npx playwright test tests/smoke/admin-lexicon-ops-import-flow.smoke.spec.ts --project=chromium"` (`1` passed). |
| 2026-03-22 | Added a shared `Path format` guidance card across Compiled Review, JSONL Review, and Import DB so the admin tool now presents one canonical artifact vocabulary for compiled inputs, reviewed outputs, and final import paths. The compiled-review path now also supports deleting imported review batches from review DB staging, and `/lexicon/db-inspector` has been upgraded from a search-only word lookup into a browseable multi-family inspector over final `word`, `phrase`, and `reference` entries with filters, sorting, pagination, and family-specific detail panels. | Codex | `PYTHONPATH=/Users/johnson/AI/src/words-v2/.worktrees/feat_lexicon_admin_inspector_browse_20260322/backend /Users/johnson/AI/src/words-v2/.venv-backend/bin/python -m pytest /Users/johnson/AI/src/words-v2/.worktrees/feat_lexicon_admin_inspector_browse_20260322/backend/tests/test_lexicon_compiled_reviews_api.py /Users/johnson/AI/src/words-v2/.worktrees/feat_lexicon_admin_inspector_browse_20260322/backend/tests/test_lexicon_inspector_api.py -q` (`14` passed), `NODE_PATH=/Users/johnson/AI/src/words-v2/admin-frontend/node_modules PATH=/Users/johnson/AI/src/words-v2/admin-frontend/node_modules/.bin:$PATH npm --prefix /Users/johnson/AI/src/words-v2/.worktrees/feat_lexicon_admin_inspector_browse_20260322/admin-frontend test -- --runInBand src/app/lexicon/db-inspector/__tests__/page.test.tsx src/app/lexicon/compiled-review/__tests__/page.test.tsx src/app/lexicon/jsonl-review/__tests__/page.test.tsx src/app/lexicon/import-db/__tests__/page.test.tsx src/lib/__tests__/lexicon-compiled-reviews-client.test.ts` (`5` suites / `10` tests passed). |
| 2026-03-23 | Landed the bounded lexicon admin portal enhancement: the compiled-review materialize path now normalizes DB-backed payload values before JSONL write so operator materialization no longer crashes on non-primitive stored values, JSONL Review and Compiled Review now share reusable lexicon workspace primitives for paged entry rails, Compiled Review batches now browse horizontally, Ops tracked artifacts are grouped by flow stage and purpose, and DB Inspector now exposes richer word detail with stored phonetics, top-level learner metadata, per-meaning usage structure, examples, and persisted translations. Follow-up verification hardening then closed the earlier evidence gap: `admin-frontend` now declares the ESLint compat package its flat config imports, the worktree install/build path is verified locally, and admin smoke specs now wait for the admin frontend to be reachable before navigation so startup readiness no longer causes flaky first-hit timeouts. A second follow-up fixed the real snapshot-backed review path: both JSONL Review and Compiled Review were trying to write reviewed outputs into `/app/data/lexicon/snapshots/...` while the backend container mounted `data` read-only, which produced opaque `500`s and made bulk approval appear to do nothing. The backend now converts write failures into actionable API errors, the dev Docker stack mounts snapshot data writable for the admin workflow, and smoke coverage now exercises real snapshot-backed JSONL bulk approve/materialize plus compiled-review materialize instead of only temporary writable fixtures. | Codex | `PYTHONPATH=backend /Users/johnson/AI/src/words-v2/.venv-backend/bin/python -m pytest backend/tests/test_lexicon_compiled_reviews_api.py backend/tests/test_lexicon_jsonl_reviews_api.py backend/tests/test_lexicon_inspector_api.py backend/tests/test_lexicon_ops_api.py -q` (`39` passed), `npm --prefix /Users/johnson/AI/src/words-v2/.worktrees/feat_lexicon_admin_portal_enhancement_20260323/admin-frontend run lint` (pass), `NEXT_PUBLIC_API_URL=/api BACKEND_URL=http://backend:8000/api npm --prefix /Users/johnson/AI/src/words-v2/.worktrees/feat_lexicon_admin_portal_enhancement_20260323/admin-frontend run build` (pass), `PYTHONPATH=backend /Users/johnson/AI/src/words-v2/.venv-backend/bin/python -m pytest backend/tests/test_lexicon_compiled_reviews_api.py backend/tests/test_lexicon_jsonl_reviews_api.py -q` (`29` passed), `npm --prefix /Users/johnson/AI/src/words-v2/.worktrees/feat_lexicon_admin_portal_enhancement_20260323/e2e run typecheck` (pass), `docker compose -f /Users/johnson/AI/src/words-v2/.worktrees/feat_lexicon_admin_portal_enhancement_20260323/docker-compose.yml up -d --force-recreate backend` (pass), and `docker compose -f /Users/johnson/AI/src/words-v2/.worktrees/feat_lexicon_admin_portal_enhancement_20260323/docker-compose.yml exec -T playwright sh -lc "cd /workspace/e2e && E2E_BASE_URL=http://frontend:3000 E2E_API_URL=http://backend:8000/api E2E_ADMIN_URL=http://admin-frontend:3001 npx playwright test tests/smoke/admin-auth.smoke.spec.ts tests/smoke/admin-compiled-review-flow.smoke.spec.ts tests/smoke/admin-jsonl-review-flow.smoke.spec.ts tests/smoke/admin-db-inspector-flow.smoke.spec.ts tests/smoke/admin-lexicon-ops-import-flow.smoke.spec.ts --project=chromium"` (`6` passed). |
| 2026-03-23 | DB Inspector follow-up: word detail now renders all stored accent phonetics explicitly as `AU`, `US`, and `UK` instead of collapsing to a single preview, and phrase/idiom detail now projects the stored `compiled_payload.senses[]` structure into grouped definition panels with usage notes, grammar patterns, examples, translations, plus top-level phrase metadata (`source_type`, `confidence_score`, `generated_at`, `seed_metadata`). This makes phrase inspection behave like an entry browser rather than a thin summary card. | Codex | `PYTHONPATH=backend /Users/johnson/AI/src/words-v2/.venv-backend/bin/python -m pytest backend/tests/test_lexicon_inspector_api.py -q` (`4` passed) and `npm --prefix /Users/johnson/AI/src/words-v2/.worktrees/feat_lexicon_admin_portal_enhancement_20260323/admin-frontend test -- --runInBand src/app/lexicon/db-inspector/__tests__/page.test.tsx` (`1` suite / `2` tests passed). |
| 2026-03-23 | Removed the last active lexicon `per_sense` path from the toolchain. `lexicon enrich` is now unambiguously per-word only, the CLI explicitly rejects the removed `--mode` flag, realtime and snapshot-side helpers no longer treat `enrichments.jsonl` as a supported artifact, and operator docs now point only at the direct `words.enriched.jsonl` workflow. This sits on top of the same-day durability fix: successful realtime completions still flush immediately, `enrich.failures.jsonl` remains append-only across `--resume`, and `compile-export` now works from the current compiled word artifact instead of the retired sense-era file. | Codex | `/Users/johnson/AI/src/words-v2/.venv-lexicon/bin/python -m pytest tools/lexicon/tests/test_cli.py tools/lexicon/tests/test_enrich.py tools/lexicon/tests/test_validate.py tools/lexicon/tests/test_canonical_registry.py tools/lexicon/tests/test_cli_canonical_registry.py tools/lexicon/tests/test_compile_export.py -q` (`161` passed), `/Users/johnson/AI/src/words-v2/.venv-lexicon/bin/python -m pytest tools/lexicon/tests -q` (`352` passed), and `/Users/johnson/AI/src/words-v2/.venv-lexicon/bin/python -m py_compile tools/lexicon/cli.py tools/lexicon/enrich.py tools/lexicon/compile_export.py tools/lexicon/validate.py tools/lexicon/canonical_registry.py` (pass). |
| 2026-03-23 | Adopted a simpler local-data policy for lexicon operations: everything under `data/` is now treated as local-only operational storage rather than Git-synced source content. The repo ignore rule was widened from `data/lexicon/` to `data/`, operator docs now explicitly separate code sync from runtime artifact churn, and tracked `data/**` entries are being removed from Git control so long-running enrich jobs cannot leave the code checkout in a misleading “dirty but allegedly synced” state. | Codex | `.gitignore`, `tools/lexicon/OPERATOR_GUIDE.md`, `docs/plans/2026-03-23-local-data-only-lexicon-policy-design.md`, `docs/plans/2026-03-23-local-data-only-lexicon-policy-implementation-plan.md`, plus Git index cleanup verification in this change set. |
| 2026-03-23 | Hardened the lexicon CLI against stale `enrich --mode ...` invocations. The root parser already disabled abbreviation, but subcommands were still accepting abbreviated flags, so `--mode per_word` was being silently parsed as `--model per_word` in real terminal runs. All subparsers now also require exact flag matches, the ad hoc enrich-only guard was removed, and regression coverage now exercises the real `sys.argv` path so this cannot slip through again. | Codex | `/Users/johnson/AI/src/words-v2/.venv-lexicon/bin/python -m pytest tools/lexicon/tests/test_cli.py -k "mode_flag" -q` (`2` passed), `/Users/johnson/AI/src/words-v2/.venv-lexicon/bin/python -m pytest tools/lexicon/tests/test_cli.py -q` (pending in this change set), and manual repro command `python -m tools.lexicon.cli enrich ... --mode per_word` now exits with `unrecognized arguments: --mode per_word`. |
| 2026-03-23 | Admin portal follow-up: `Import DB` now starts a backend-managed lexicon import job instead of holding the page on one opaque request, exposes live progress (`current entry`, done, remaining, total), and reconnects from browser storage after navigation so admins can leave and return without losing visibility into the running import. `DB Inspector` browse pagination is tightened to `10` rows per page so detail stays dominant. `Compiled Review` and `JSONL Review` now both use a shared reviewer-summary panel and tighter `5`-row entry paging so the key lexical facts and raw JSON remain visible without the list rail dominating the page. This is still backend-process-managed rather than worker-managed; browsing away no longer kills the job, but a backend restart still would. Follow-up smoke coverage now explicitly verifies the `Import DB` reconnect path by reloading the page after start and asserting the persisted progress/result state before final DB verification. | Codex | `PYTHONPATH=backend /Users/johnson/AI/src/words-v2/.venv-backend/bin/python -m pytest backend/tests/test_lexicon_imports_api.py -q` (`4` passed), `NODE_PATH=/Users/johnson/AI/src/words-v2/admin-frontend/node_modules PATH=/Users/johnson/AI/src/words-v2/admin-frontend/node_modules/.bin:$PATH npm --prefix /Users/johnson/AI/src/words-v2/.worktrees/feat_lexicon_admin_portal_enhancement_20260323/admin-frontend test -- --runInBand src/app/lexicon/import-db/__tests__/page.test.tsx src/app/lexicon/compiled-review/__tests__/page.test.tsx src/app/lexicon/jsonl-review/__tests__/page.test.tsx src/app/lexicon/db-inspector/__tests__/page.test.tsx` (`4` suites / `13` tests passed), `NODE_PATH=/Users/johnson/AI/src/words-v2/admin-frontend/node_modules PATH=/Users/johnson/AI/src/words-v2/admin-frontend/node_modules/.bin:$PATH npm --prefix /Users/johnson/AI/src/words-v2/.worktrees/feat_lexicon_admin_portal_enhancement_20260323/admin-frontend run lint` (pass), `NODE_PATH=/Users/johnson/AI/src/words-v2/admin-frontend/node_modules PATH=/Users/johnson/AI/src/words-v2/admin-frontend/node_modules/.bin:$PATH npm --prefix /Users/johnson/AI/src/words-v2/.worktrees/feat_lexicon_admin_portal_enhancement_20260323/admin-frontend run build` (pass), and `docker compose -f /Users/johnson/AI/src/words-v2/.worktrees/feat_lexicon_admin_portal_enhancement_20260323/docker-compose.yml exec -T playwright sh -lc "cd /workspace/e2e && E2E_BASE_URL=http://frontend:3000 E2E_API_URL=http://backend:8000/api E2E_ADMIN_URL=http://admin-frontend:3001 npx playwright test tests/smoke/admin-lexicon-ops-import-flow.smoke.spec.ts --project=chromium"` (`1` passed). |
| 2026-03-23 | Worker-backed lexicon jobs landed for the heavy admin operations. Added a dedicated `lexicon.lexicon_jobs` table plus lifecycle helpers, Celery tasks, and `/api/lexicon-jobs/*` control-plane endpoints so `Import DB`, JSONL Review `materialize`, and Compiled Review `materialize` now enqueue durable jobs instead of running inline in the admin request. The admin pages were migrated to poll those jobs; `Import DB` still reconnects after navigation, and both review pages now wait for job completion before surfacing materialized output paths. Two stack-level issues surfaced during verification and were fixed as part of the same slice: the worker container needed write access to `/app/data` for reviewed artifacts, and Docker dev databases needed the new Alembic migration applied so `lexicon.lexicon_jobs` exists before smoke runs. A separate worker runtime bug also surfaced and was fixed by correcting the `tools.lexicon.*` fallback import path resolution from `/` to the actual repo root inside containers (`/app`), which unblocked queue-backed JSONL materialize and import execution. | Codex | `PYTHONPATH=backend /Users/johnson/AI/src/words-v2/.venv-backend/bin/python -m pytest backend/tests/test_models.py backend/tests/test_lexicon_jobs_service.py backend/tests/test_lexicon_worker_tasks.py backend/tests/test_lexicon_jobs_api.py backend/tests/test_lexicon_compiled_reviews_api.py backend/tests/test_lexicon_jsonl_reviews_api.py backend/tests/test_lexicon_imports_api.py -q` (`62` passed), `NODE_PATH=/Users/johnson/AI/src/words-v2/admin-frontend/node_modules PATH=/Users/johnson/AI/src/words-v2/admin-frontend/node_modules/.bin:$PATH npm --prefix /Users/johnson/AI/src/words-v2/.worktrees/feat_lexicon_admin_portal_enhancement_20260323/admin-frontend test -- --runInBand src/app/lexicon/import-db/__tests__/page.test.tsx src/app/lexicon/jsonl-review/__tests__/page.test.tsx src/app/lexicon/compiled-review/__tests__/page.test.tsx` (`3` suites / `11` tests passed), `NODE_PATH=/Users/johnson/AI/src/words-v2/admin-frontend/node_modules PATH=/Users/johnson/AI/src/words-v2/admin-frontend/node_modules/.bin:$PATH npm --prefix /Users/johnson/AI/src/words-v2/.worktrees/feat_lexicon_admin_portal_enhancement_20260323/admin-frontend run lint` (pass), `NODE_PATH=/Users/johnson/AI/src/words-v2/admin-frontend/node_modules PATH=/Users/johnson/AI/src/words-v2/admin-frontend/node_modules/.bin:$PATH npm --prefix /Users/johnson/AI/src/words-v2/.worktrees/feat_lexicon_admin_portal_enhancement_20260323/admin-frontend run build` (pass), `docker compose -f /Users/johnson/AI/src/words-v2/.worktrees/feat_lexicon_admin_portal_enhancement_20260323/docker-compose.yml exec -T backend sh -lc "cd /app && alembic upgrade head"` (applied `015_add_lexicon_jobs`), and `docker compose -f /Users/johnson/AI/src/words-v2/.worktrees/feat_lexicon_admin_portal_enhancement_20260323/docker-compose.yml exec -T playwright sh -lc "cd /workspace/e2e && E2E_BASE_URL=http://frontend:3000 E2E_API_URL=http://backend:8000/api E2E_ADMIN_URL=http://admin-frontend:3001 npx playwright test tests/smoke/admin-compiled-review-flow.smoke.spec.ts tests/smoke/admin-jsonl-review-flow.smoke.spec.ts tests/smoke/admin-lexicon-ops-import-flow.smoke.spec.ts --project=chromium"` (`3` passed). |
