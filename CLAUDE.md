@AGENTS.md

# Claude Code compatibility
- Use `AGENTS.md` as the canonical shared repo instructions.
- Put personal Claude-only notes in `CLAUDE.local.md` (gitignored).
- Add more specific subdirectory `CLAUDE.md` files only when a slice genuinely needs Claude-only behavior.