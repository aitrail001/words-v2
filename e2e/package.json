{
  "name": "words-e2e",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "test": "playwright test",
    "test:smoke": "playwright test --grep @smoke",
    "test:smoke:ci": "playwright test --grep @smoke --max-failures=1",
    "test:full": "playwright test",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@playwright/test": "1.52.0",
    "@types/node": "^22.13.10",
    "typescript": "^5.8.2"
  }
}
